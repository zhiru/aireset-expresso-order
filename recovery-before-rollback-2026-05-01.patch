diff --git a/CHANGELOG.md b/CHANGELOG.md
index 8b7ac11..d6ad861 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -2,6 +2,67 @@
 
 Todas as alteracoes relevantes do plugin `Aireset Expresso Order` devem ser registradas aqui.
 
+## 1.1.59 - 2026-05-01
+
+- revertida a direcao do admin principal para um shell SPA unico, restaurando a navegacao por `view` dentro da mesma pagina em vez de continuar abrindo subpaginas reais do WordPress
+- aparencia e painel, configuracoes gerais, fluxo complementar, operacao, pedidos e licenca voltaram a conviver como views internas separadas dentro do mesmo SPA
+- o plano de reestruturacao foi corrigido para refletir a arquitetura esperada: melhorar a experiencia de administracao dentro do SPA, com menos densidade por tela e sem quebrar o shell visual existente
+
+## 1.1.58 - 2026-05-01
+
+- criada a subpagina real Aparencia e painel para isolar identidade visual, tipografia, textos do shell e visual da proposta publica
+- Configuracoes gerais ficou mais enxuta e passou a concentrar apenas fluxo operacional, checkout, paginas publicas e dados institucionais do PDF nativo
+- o menu lateral e o flyout administrativo foram atualizados para refletir a nova separacao entre operacao, aparencia e fluxo complementar
+
+## 1.1.57 - 2026-05-01
+
+- iniciado o rollout da reestruturacao do admin com subpaginas reais para Operacao, Pedidos, Configuracoes gerais, Fluxo complementar e Licenca
+- o modulo PDF deixou de depender do roteamento interno por `view=pdf` e passou a registrar abas reais no menu administrativo do WordPress
+- a navegacao do shell passou a abrir paginas dedicadas por area e a edicao de pedidos a partir da listagem agora redireciona para a pagina Operacao com URL explicita
+
+## 1.1.56 - 2026-05-01
+
+- o preview PDF do admin passou a usar uma URL dedicada e read-only, sem consumir numeracao, sem gravar metadados de emissao e sem gerar tokens privados apenas para homologacao
+- o modulo PDF agora carrega todas as fontes efetivamente usadas pelos perfis tipograficos do documento no admin e no HTML de impressao
+- alteracoes na fonte global do plugin agora invalidam o cache do PDF, evitando servir arquivos antigos com tipografia desatualizada
+
+## 1.1.55 - 2026-05-01
+
+- adicionados filtros da listagem da SPA para isolar pedidos com fluxo complementar ativo, pendente ou concluido
+- criada rota REST de colecao paginada para o fluxo complementar, com busca, status do pedido e filtro por status do proprio fluxo
+
+## 1.1.54 - 2026-04-30
+
+- a listagem de pedidos da SPA passou a exibir um resumo compacto do fluxo complementar, incluindo etapa atual, contrato, campos, anexo e progresso dos produtos
+- adicionado payload leve especifico para cards de listagem, evitando reutilizar o export completo do fluxo complementar onde ele nao era necessario
+
+## 1.1.53 - 2026-04-29
+
+- a SPA administrativa do Pedido Expresso passou a consumir a rota REST do fluxo complementar ao abrir um pedido em modo de edicao
+- o shell administrativo ganhou um card proprio para resumir contrato, campos, anexo, produtos e links do fluxo complementar sem depender apenas do retorno AJAX legado
+
+## 1.1.52 - 2026-04-29
+
+- adicionada rota REST autenticada para consultar o payload estruturado do fluxo complementar por pedido, com validacao de permissao por usuario e por acesso ao pedido
+- enriquecido o payload exportado com contexto, timestamp de geracao e metadados basicos do pedido para consumo mais direto em integracoes
+
+## 1.1.51 - 2026-04-29
+
+- adicionado um export estruturado e filtravel do fluxo complementar por pedido, pronto para reaproveito em integracoes futuras sem depender do HTML do frontend ou do PDF
+- enriquecido o retorno AJAX de carregamento do pedido com o payload do fluxo complementar para uso no painel interno
+- criada uma leitura visual do fluxo complementar na tela administrativa de edicao do pedido, com etapa atual, contrato, campos preenchidos, anexo, produtos e links rapidos
+
+## 1.1.50 - 2026-04-29
+
+- adicionado PDF complementar do fluxo pos-confirmacao, com contrato, campos documentais, status do anexo e personalizacao dos produtos, disponivel no frontend concluido e no admin do pedido
+- trocado o campo textual de produtos bloqueados por um seletor visual com busca por nome ou SKU no settings do plugin, mantendo compatibilidade com a option existente
+- refinada a experiencia publica do fluxo complementar com barra de progresso, cards de status laterais, resumo de upload e mensagens orientando cada etapa
+
+## 1.1.49 - 2026-04-29
+
+- adicionada a base do fluxo complementar opcional apos a confirmacao da proposta, com contrato inline, 11 campos documentais configuraveis, upload de anexo, personalizacao de nomes por item e resumo no admin do pedido
+- a pagina publica da proposta agora consegue retomar a jornada depois do pagamento usando o mesmo link do cliente
+
 ## 1.1.48 - 2026-04-23
 
 - ocultado o container de notices do shell quando estiver vazio, inclusive apos dispensar mensagens no admin e na tela de pedidos
diff --git a/aireset-expresso-order.php b/aireset-expresso-order.php
index c0634ab..feb1a0c 100644
--- a/aireset-expresso-order.php
+++ b/aireset-expresso-order.php
@@ -2,7 +2,7 @@
 /**
  * Plugin Name: Aireset — Expresso Order
  * Description: Pedido expresso para vendedores com busca de cliente, inclusao rapida de produtos e geracao de pedido no WooCommerce.
- * Version: 1.1.48
+ * Version: 1.1.59
  * Author: Aireset Agencia Web
  * Author URI: https://aireset.com.br
  * Requires Plugins: woocommerce
@@ -14,7 +14,7 @@
 
 defined( 'ABSPATH' ) || exit;
 
-define( 'EOP_VERSION', '1.1.48' );
+define( 'EOP_VERSION', '1.1.59' );
 define( 'EOP_PLUGIN_FILE', __FILE__ );
 define( 'EOP_PLUGIN_DIR', plugin_dir_path( __FILE__ ) );
 define( 'EOP_PLUGIN_URL', plugin_dir_url( __FILE__ ) );
@@ -87,6 +87,7 @@ require_once EOP_PLUGIN_DIR . 'includes/class-pdf-settings.php';
 require_once EOP_PLUGIN_DIR . 'includes/class-settings.php';
 require_once EOP_PLUGIN_DIR . 'includes/class-shipping-calculator.php';
 require_once EOP_PLUGIN_DIR . 'includes/class-shortcode.php';
+require_once EOP_PLUGIN_DIR . 'includes/class-post-confirmation-flow.php';
 require_once EOP_PLUGIN_DIR . 'includes/class-public-proposal.php';
 require_once EOP_PLUGIN_DIR . 'includes/class-orders-page.php';
 require_once EOP_PLUGIN_DIR . 'includes/class-wc-pdf-integration.php';
@@ -125,6 +126,7 @@ function eop_init() {
 	EOP_Shipping_Calculator::init();
 	EOP_Settings::init();
 	EOP_Shortcode::init();
+	EOP_Post_Confirmation_Flow::init();
 	EOP_Public_Proposal::init();
 	EOP_Orders_Page::init();
 	EOP_Role::restrict_menus();
diff --git a/assets/css/admin.css b/assets/css/admin.css
index 25c7594..bd82f56 100644
--- a/assets/css/admin.css
+++ b/assets/css/admin.css
@@ -508,13 +508,6 @@ body.eop-admin-spa-screen #wpbody-content > .fs-notice {
   color: rgba(255, 255, 255, 0.82);
 }
 
-#eop-notices:empty,
-.eop-notices:empty,
-#eop-notices[hidden],
-.eop-notices[hidden] {
-  display: none !important;
-}
-
 #eop-notices .notice,
 #eop-notices div.notice {
   display: block !important;
@@ -703,13 +696,102 @@ body.eop-admin-spa-screen #wpbody-content > .fs-notice {
   line-height: 1.6;
 }
 
-.eop-admin-panel-head {
-  margin-bottom: 0;
+.eop-post-flow-card {
+  display: grid;
+  gap: 16px;
+}
+
+.eop-post-flow-card__head {
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: 12px;
+}
+.eop-post-flow-card__head h2 {
+  margin: 0 0 4px;
+}
+.eop-post-flow-card__head p {
+  margin: 0;
+  color: #5f6b7a;
+}
+
+.eop-post-flow-badge {
+  display: inline-flex;
   align-items: center;
+  justify-content: center;
+  padding: 6px 12px;
+  border-radius: 999px;
+  font-size: 12px;
+  font-weight: 700;
+  background: #eef2f7;
+  color: #324154;
+}
+.eop-post-flow-badge.is-active {
+  background: #e8f4eb;
+  color: #1f6a38;
+}
+
+.eop-post-flow-card__stats {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
   gap: 12px;
-  padding: 14px 18px;
-  border-radius: 16px;
-  box-shadow: 0 14px 30px rgba(15, 37, 87, 0.14);
+}
+
+.eop-post-flow-stat {
+  padding: 12px 14px;
+  border: 1px solid #dde5ef;
+  border-radius: 14px;
+  background: #fafcff;
+}
+.eop-post-flow-stat span {
+  display: block;
+  margin-bottom: 6px;
+  color: #667487;
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: 0.06em;
+  text-transform: uppercase;
+}
+.eop-post-flow-stat strong {
+  font-size: 16px;
+}
+
+.eop-post-flow-card__section {
+  padding-top: 14px;
+  border-top: 1px solid #edf1f5;
+}
+.eop-post-flow-card__section h3 {
+  margin: 0 0 8px;
+}
+
+.eop-post-flow-list {
+  display: grid;
+  gap: 8px;
+}
+
+.eop-post-flow-row {
+  display: grid;
+  gap: 2px;
+  padding: 10px 12px;
+  border: 1px solid #e6ebf2;
+  border-radius: 12px;
+  background: #fff;
+}
+.eop-post-flow-row strong {
+  font-size: 13px;
+}
+.eop-post-flow-row span {
+  color: #586577;
+}
+
+.eop-post-flow-card__actions {
+  display: flex;
+  gap: 8px;
+  flex-wrap: wrap;
+}
+
+.eop-admin-panel-head {
+  margin-bottom: 0;
 }
 .eop-admin-panel-head h2 {
   display: flex;
@@ -717,17 +799,17 @@ body.eop-admin-spa-screen #wpbody-content > .fs-notice {
   gap: 10px;
   margin: 0;
   color: #fff;
-  font-size: 22px;
+  font-size: 26px;
   font-weight: 700;
-  line-height: 1.12;
+  line-height: 1.08;
   letter-spacing: -0.03em;
 }
 .eop-admin-panel-head p {
   margin: 0;
-  max-width: 58ch;
+  max-width: 70ch;
   color: rgba(255, 255, 255, 0.86);
-  font-size: 13px;
-  line-height: 1.45;
+  font-size: 14px;
+  line-height: 1.6;
 }
 
 .eop-admin-view-main {
@@ -788,6 +870,52 @@ body.eop-admin-spa-screen #wpbody-content > .fs-notice {
   color: #55607f;
 }
 
+.eop-order-card__flow {
+  display: grid;
+  gap: 10px;
+  margin-top: 14px;
+  padding: 14px;
+  border: 1px solid #e3e9f6;
+  border-radius: 16px;
+  background: linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);
+}
+
+.eop-order-card__flow-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+}
+.eop-order-card__flow-head span {
+  color: #607089;
+  font-size: 11px;
+  font-weight: 800;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+}
+.eop-order-card__flow-head strong {
+  color: #12203a;
+  font-size: 13px;
+}
+
+.eop-order-card__flow-list {
+  display: flex;
+  gap: 8px;
+  flex-wrap: wrap;
+}
+
+.eop-order-card__flow-pill {
+  display: inline-flex;
+  align-items: center;
+  min-height: 32px;
+  padding: 0 12px;
+  border-radius: 999px;
+  background: #eef3fb;
+  color: #344764;
+  font-size: 12px;
+  font-weight: 600;
+}
+
 .eop-admin-license-shell {
   max-width: 980px;
 }
@@ -954,24 +1082,71 @@ body.eop-admin-spa-screen #wpbody-content > .fs-notice {
   box-shadow: 0 0 0 4px rgba(0, 3, 75, 0.08);
   outline: none;
 }
-.eop-admin-license-shell .el-license-active-btn {
-  margin-top: 20px;
-  padding-top: 18px;
-  border-top: 1px solid #e4eaff;
-}
-.eop-admin-license-shell .el-license-active-btn .button-primary,
-.eop-admin-license-shell .el-license-active-btn .button {
-  min-height: 52px;
-  padding: 0 22px;
+.eop-admin-submitbar {
+  position: fixed;
+  right: 28px;
+  bottom: 24px;
+  z-index: 120;
+  width: min(360px, calc(100vw - 32px));
+  margin: 0;
+  padding: 10px;
+  border: 1px solid rgba(214, 222, 253, 0.9);
+  border-radius: 24px;
+  background: rgba(255, 255, 255, 0.94);
+  box-shadow: 0 20px 48px rgba(0, 3, 75, 0.18);
+  backdrop-filter: blur(16px);
+}
+.eop-admin-submitbar .button-primary,
+.eop-admin-submitbar .button,
+.eop-admin-submitbar input[type=submit].button-primary,
+.eop-admin-submitbar input[type=submit].button {
+  width: 100%;
+  min-height: 60px;
+  padding: 0 28px;
   border: 0;
-  border-radius: 999px;
+  border-radius: 18px;
   background: linear-gradient(135deg, var(--eop-primary, #00034b) 0%, #050a76 100%);
   color: #fff;
-  font-size: 15px;
+  font-size: 16px;
   font-weight: 800;
-  box-shadow: 0 18px 34px rgba(0, 3, 75, 0.2);
+  text-align: right;
+  box-shadow: 0 18px 34px rgba(0, 3, 75, 0.24);
+}
+.eop-admin-submitbar .button-primary:hover,
+.eop-admin-submitbar .button-primary:focus,
+.eop-admin-submitbar .button:hover,
+.eop-admin-submitbar .button:focus,
+.eop-admin-submitbar input[type=submit].button-primary:hover,
+.eop-admin-submitbar input[type=submit].button-primary:focus,
+.eop-admin-submitbar input[type=submit].button:hover,
+.eop-admin-submitbar input[type=submit].button:focus {
+  background: linear-gradient(135deg, var(--eop-primary, #00034b) 0%, #050a76 100%);
+  color: #fff;
+}
+.eop-admin-license-shell form {
+  padding-bottom: 128px;
+}
+.eop-admin-license-shell .el-license-active-btn {
+  margin-top: 20px;
+  padding-top: 18px;
+  border-top: 1px solid #e4eaff;
 }
 @media (max-width: 782px) {
+  .eop-admin-submitbar {
+    left: 12px;
+    right: 12px;
+    bottom: 12px;
+    width: auto;
+    padding: 8px;
+  }
+  .eop-admin-submitbar .button-primary,
+  .eop-admin-submitbar .button,
+  .eop-admin-submitbar input[type=submit].button-primary,
+  .eop-admin-submitbar input[type=submit].button {
+    min-height: 56px;
+    padding: 0 22px;
+    font-size: 15px;
+  }
   .eop-admin-license-shell .el-license-container {
     padding: 22px 20px;
   }
@@ -1017,7 +1192,7 @@ body.eop-admin-spa-screen #wpbody-content > .fs-notice {
   }
   .eop-admin-view-header,
   .eop-admin-panel-head {
-    padding: 16px;
+    padding: 20px;
     border-radius: 12px;
     align-items: stretch;
   }
diff --git a/assets/css/orders.css b/assets/css/orders.css
index a0d7d8a..0aecc2b 100644
--- a/assets/css/orders.css
+++ b/assets/css/orders.css
@@ -76,6 +76,114 @@
     text-decoration: none;
 }
 
+.eop-post-flow-card {
+    display: grid;
+    gap: 16px;
+}
+
+.eop-post-flow-card__head {
+    display: flex;
+    justify-content: space-between;
+    gap: 12px;
+    align-items: flex-start;
+}
+
+.eop-post-flow-card__head h2 {
+    margin: 0 0 4px;
+}
+
+.eop-post-flow-card__head p {
+    margin: 0;
+    color: #5f6b7a;
+}
+
+.eop-post-flow-badge {
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    padding: 6px 12px;
+    border-radius: 999px;
+    font-size: 12px;
+    font-weight: 700;
+    background: #eef2f7;
+    color: #324154;
+}
+
+.eop-post-flow-badge.is-active {
+    background: #e8f4eb;
+    color: #1f6a38;
+}
+
+.eop-post-flow-card__stats {
+    display: grid;
+    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
+    gap: 12px;
+}
+
+.eop-post-flow-stat {
+    padding: 12px 14px;
+    border: 1px solid #dde5ef;
+    border-radius: 14px;
+    background: #fafcff;
+}
+
+.eop-post-flow-stat span {
+    display: block;
+    margin-bottom: 6px;
+    color: #667487;
+    font-size: 11px;
+    font-weight: 700;
+    letter-spacing: .06em;
+    text-transform: uppercase;
+}
+
+.eop-post-flow-stat strong {
+    font-size: 16px;
+}
+
+.eop-post-flow-card__section {
+    padding-top: 14px;
+    border-top: 1px solid #edf1f5;
+}
+
+.eop-post-flow-card__section h3 {
+    margin: 0 0 8px;
+}
+
+.eop-post-flow-list {
+    display: grid;
+    gap: 8px;
+}
+
+.eop-post-flow-row {
+    display: grid;
+    gap: 2px;
+    padding: 10px 12px;
+    border: 1px solid #e6ebf2;
+    border-radius: 12px;
+    background: #fff;
+}
+
+.eop-post-flow-row strong {
+    font-size: 13px;
+}
+
+.eop-post-flow-row span {
+    color: #586577;
+}
+
+.eop-post-flow-card__actions {
+    display: flex;
+    gap: 8px;
+    flex-wrap: wrap;
+}
+
+@media (max-width: 782px) {
+    .eop-post-flow-card__head {
+        flex-direction: column;
+    }
+}
+
 /* Pagination */
 .eop-orders-wrap .tablenav-pages {
     margin-top: 12px;
diff --git a/assets/css/pdf-admin.css b/assets/css/pdf-admin.css
index 9542dc7..d4b98b9 100644
--- a/assets/css/pdf-admin.css
+++ b/assets/css/pdf-admin.css
@@ -39,8 +39,8 @@
 
 .eop-pdf-admin__shell {
     position: relative;
-    display: block;
-    min-height: 760px;
+    display: grid;
+    min-height: 0;
     margin-top: 0;
     border: 1px solid #dbe3f0;
     border-top: none;
@@ -54,17 +54,41 @@
     box-shadow: 0 24px 60px rgba(0, 3, 75, 0.08);
 }
 
+.eop-pdf-admin__shell-head {
+    display: flex;
+    align-items: flex-start;
+    justify-content: space-between;
+    gap: 16px;
+    padding: 20px 22px 18px;
+    border-bottom: 1px solid #e6edf8;
+    background: linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);
+}
+
+.eop-pdf-admin__shell-copy {
+    display: grid;
+    gap: 6px;
+    min-width: 0;
+}
+
+.eop-pdf-admin__shell-copy strong {
+    color: #162033;
+    font-size: 20px;
+    line-height: 1.1;
+}
+
+.eop-pdf-admin__shell-copy span {
+    color: #5b6474;
+    font-size: 13px;
+    line-height: 1.55;
+}
+
 .eop-pdf-admin__sidebar {
     min-width: 0;
-    padding: 22px 96px 22px 22px;
+    padding: 20px 22px 22px;
     background: #fcfdff;
 }
 
 .eop-pdf-admin__preview-toggle {
-    position: absolute;
-    top: 22px;
-    right: 22px;
-    z-index: 5;
     display: inline-flex;
     align-items: center;
     gap: 8px;
@@ -76,6 +100,7 @@
     color: #172033;
     box-shadow: 0 12px 28px rgba(0, 3, 75, 0.12);
     cursor: pointer;
+    flex-shrink: 0;
 }
 
 .eop-pdf-admin__preview-toggle:hover,
@@ -202,9 +227,66 @@
     padding: 22px;
 }
 
+.eop-admin-spa .eop-admin-view-main--pdf {
+    gap: 0;
+}
+
 .eop-pdf-admin__form {
     display: grid;
     gap: 16px;
+    padding-bottom: 128px;
+}
+
+.eop-pdf-admin__grid--spacing {
+    margin-top: 14px;
+}
+
+.eop-pdf-admin__style-groups {
+    display: grid;
+    gap: 14px;
+    margin-top: 18px;
+}
+
+.eop-pdf-admin__style-card {
+    border: 1px solid #dbe3f0;
+    border-radius: 18px;
+    background: #fff;
+    overflow: hidden;
+    box-shadow: 0 12px 30px rgba(0, 3, 75, 0.05);
+}
+
+.eop-pdf-admin__style-card-header {
+    display: flex;
+    align-items: baseline;
+    justify-content: space-between;
+    gap: 12px;
+    padding: 16px 18px;
+    border-bottom: 1px solid #edf2fa;
+    background: linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);
+}
+
+.eop-pdf-admin__style-card-header strong {
+    font-size: 14px;
+    color: #172033;
+}
+
+.eop-pdf-admin__style-card-header span {
+    font-size: 11px;
+    font-weight: 700;
+    letter-spacing: .08em;
+    text-transform: uppercase;
+    color: #7b8494;
+}
+
+.eop-pdf-admin__style-card-grid {
+    display: grid;
+    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
+    gap: 14px;
+    padding: 18px;
+}
+
+.eop-pdf-admin__style-card .eop-settings-field.is-full {
+    grid-column: 1 / -1;
 }
 
 .eop-pdf-admin__notice {
@@ -426,7 +508,7 @@
 }
 
 .eop-pdf-admin__actions {
-    padding-top: 8px;
+    padding-top: 0;
 }
 
 .eop-pdf-admin__status-grid {
@@ -820,6 +902,15 @@
         grid-template-columns: 1fr;
     }
 
+    .eop-pdf-admin__shell-head {
+        flex-direction: column;
+        align-items: stretch;
+    }
+
+    .eop-pdf-admin__preview-toggle {
+        justify-content: center;
+    }
+
     .eop-pdf-admin__sidebar {
         border-right: none;
         border-bottom: 1px solid #e7edf7;
@@ -856,15 +947,8 @@
         flex-direction: column;
     }
 
-    .eop-pdf-admin__sidebar {
-        padding-right: 22px;
-    }
-
     .eop-pdf-admin__preview-toggle {
-        position: sticky;
-        top: 16px;
-        right: auto;
-        margin: 16px 16px 0 auto;
+        width: 100%;
     }
 
     .eop-pdf-admin__preview-drawer {
diff --git a/assets/css/settings-admin.css b/assets/css/settings-admin.css
index 413c0e7..c410b96 100644
--- a/assets/css/settings-admin.css
+++ b/assets/css/settings-admin.css
@@ -63,6 +63,84 @@
   min-height: 40px;
 }
 
+.eop-settings-page .button.button-secondary {
+  min-height: 42px;
+  padding: 0 16px;
+  border: 1px solid #c9d6ff;
+  border-radius: 12px;
+  color: #17327c;
+  background: #fff;
+  box-shadow: none;
+}
+.eop-settings-page .button.button-secondary:hover,
+.eop-settings-page .button.button-secondary:focus {
+  border-color: #8eabff;
+  color: #10265f;
+  background: #f7faff;
+}
+
+.eop-signature-documents {
+  display: grid;
+  gap: 14px;
+}
+.eop-signature-documents__list {
+  display: grid;
+  gap: 14px;
+}
+.eop-signature-document {
+  padding: 18px;
+  border: 1px solid #dfe6ff;
+  border-radius: 18px;
+  background: linear-gradient(180deg, #ffffff 0%, #f8faff 100%);
+}
+.eop-signature-document__header {
+  display: flex;
+  justify-content: space-between;
+  gap: 12px;
+  align-items: center;
+  margin-bottom: 14px;
+}
+.eop-signature-document__header strong {
+  font-size: 14px;
+  color: #11162f;
+}
+.eop-signature-document__grid {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 14px 16px;
+}
+.eop-signature-document__panel.is-hidden {
+  display: none;
+}
+.eop-signature-document__attachment-shell {
+  display: grid;
+  gap: 10px;
+  padding: 14px;
+  border: 1px dashed #c8d4fb;
+  border-radius: 14px;
+  background: #fff;
+}
+.eop-signature-document__attachment-name {
+  color: #44506d;
+  font-weight: 600;
+}
+.eop-signature-document__attachment-actions {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 10px;
+}
+.eop-signature-documents__add {
+  justify-self: start;
+}
+.eop-settings-page .wp-editor-wrap {
+  border: 1px solid #d6defd;
+  border-radius: 12px;
+  overflow: hidden;
+}
+.eop-settings-page .wp-editor-container textarea.wp-editor-area {
+  border: 0;
+}
+
 body.eop-settings-screen .notice,
 body.eop-settings-screen div.notice,
 body.eop-settings-screen div.error,
@@ -154,6 +232,7 @@ body.eop-settings-screen .error {
   text-transform: uppercase;
 }
 .eop-settings-field input[type=text],
+.eop-settings-field input[type=email],
 .eop-settings-field input[type=url],
 .eop-settings-field input[type=number],
 .eop-settings-field select,
@@ -167,6 +246,7 @@ body.eop-settings-screen .error {
   box-shadow: none;
 }
 .eop-settings-field input[type=text]:focus,
+.eop-settings-field input[type=email]:focus,
 .eop-settings-field input[type=url]:focus,
 .eop-settings-field input[type=number]:focus,
 .eop-settings-field select:focus,
@@ -378,11 +458,74 @@ body.eop-settings-screen .error {
   color: #6a7695;
 }
 
+.eop-admin-submitbar {
+  position: fixed;
+  right: 28px;
+  bottom: 24px;
+  z-index: 120;
+  width: min(360px, calc(100vw - 32px));
+  margin: 0;
+  padding: 10px;
+  border: 1px solid rgba(214, 222, 253, 0.9);
+  border-radius: 24px;
+  background: rgba(255, 255, 255, 0.94);
+  box-shadow: 0 20px 48px rgba(0, 3, 75, 0.18);
+  backdrop-filter: blur(16px);
+}
+
+.eop-admin-submitbar .button-primary,
+.eop-admin-submitbar .button,
+.eop-admin-submitbar input[type=submit].button-primary,
+.eop-admin-submitbar input[type=submit].button {
+  width: 100%;
+  min-height: 60px;
+  padding: 0 28px;
+  border: 0;
+  border-radius: 18px;
+  background: linear-gradient(135deg, #00034b 0%, #050a76 100%);
+  color: #fff;
+  font-size: 16px;
+  font-weight: 800;
+  text-align: right;
+  box-shadow: 0 18px 34px rgba(0, 3, 75, 0.24);
+}
+
+.eop-admin-submitbar .button-primary:hover,
+.eop-admin-submitbar .button-primary:focus,
+.eop-admin-submitbar .button:hover,
+.eop-admin-submitbar .button:focus,
+.eop-admin-submitbar input[type=submit].button-primary:hover,
+.eop-admin-submitbar input[type=submit].button-primary:focus,
+.eop-admin-submitbar input[type=submit].button:hover,
+.eop-admin-submitbar input[type=submit].button:focus {
+  background: linear-gradient(135deg, #00034b 0%, #050a76 100%);
+  color: #fff;
+}
+
+.eop-settings-form {
+  padding-bottom: 128px;
+}
+
 .eop-settings-submit {
-  margin-top: 20px;
+  margin-top: 0;
 }
 
 @media (max-width: 960px) {
+  .eop-admin-submitbar {
+    left: 12px;
+    right: 12px;
+    bottom: 12px;
+    width: auto;
+    padding: 8px;
+  }
+  .eop-admin-submitbar .button-primary,
+  .eop-admin-submitbar .button,
+  .eop-admin-submitbar input[type=submit].button-primary,
+  .eop-admin-submitbar input[type=submit].button {
+    min-height: 56px;
+    padding: 0 22px;
+    font-size: 15px;
+  }
   .eop-settings-shell,
   .eop-settings-grid {
     grid-template-columns: 1fr;
diff --git a/assets/js/admin-menu-flyout.js b/assets/js/admin-menu-flyout.js
index ca2954b..3d5b0ad 100644
--- a/assets/js/admin-menu-flyout.js
+++ b/assets/js/admin-menu-flyout.js
@@ -180,7 +180,9 @@
             items.forEach(function (item) {
                 if (!item) { return; }
 
-                if (item.key) {
+                if (item.query && item.query.page) {
+                    keys.push(String(item.query.page));
+                } else if (item.key) {
                     keys.push(String(item.key));
                 }
 
diff --git a/assets/js/admin.js b/assets/js/admin.js
index bf62fb0..8aa59ec 100644
--- a/assets/js/admin.js
+++ b/assets/js/admin.js
@@ -14,6 +14,7 @@
     var ordersPage = 1;
     var currentEditingOrderId = 0;
     var editLoadToken = 0;
+    var postFlowRequestToken = 0;
 
     function getDefaultDiscountFieldConfig() {
         if (discountMode === 'percent') {
@@ -403,6 +404,8 @@
         if ($submit.length) {
             $submit.text(isEditing ? (i18n.edit_submit || 'Salvar alteracoes') : i18n.submit_label);
         }
+
+		renderPostConfirmationFlowPlaceholder(isEditing ? (i18n.post_flow_loading || 'Carregando dados complementares da proposta...') : (i18n.post_flow_unavailable_edit || 'O resumo complementar aparece quando um pedido existente entra em modo de edicao.'));
     }
 
     function getCurrentSubmitLabel() {
@@ -459,6 +462,14 @@
             views.push('settings');
         }
 
+        if ($('[data-eop-view="appearance"]').length) {
+            views.push('appearance');
+        }
+
+        if ($('[data-eop-view="post-confirmation"]').length) {
+            views.push('post-confirmation');
+        }
+
         if ($('[data-eop-view="license"]').length) {
             views.push('license');
         }
@@ -478,7 +489,8 @@
     }
 
     function buildViewUrl(viewName) {
-        var base = String(eop_vars.view_url_base || '');
+        var viewUrls = eop_vars.view_urls || {};
+        var base = String(viewUrls[normalizeView(viewName)] || eop_vars.view_url_base || '');
         var currentParams = new URLSearchParams(window.location.search);
         var normalizedView = normalizeView(viewName);
         var url;
@@ -623,6 +635,22 @@
             }
 
             html += '</div>';
+
+            if (order.post_confirmation_flow_summary && order.post_confirmation_flow_summary.active_for_order) {
+                html += '<div class="eop-order-card__flow">';
+                html += '<div class="eop-order-card__flow-head">';
+                html += '<span>' + escapeHtml(i18n.orders_flow_title || 'Fluxo complementar') + '</span>';
+                html += '<strong>' + escapeHtml(order.post_confirmation_flow_summary.stage_label || '') + '</strong>';
+                html += '</div>';
+                html += '<div class="eop-order-card__flow-list">';
+                html += '<span class="eop-order-card__flow-pill">' + escapeHtml((i18n.orders_flow_contract || 'Contrato') + ': ' + ((order.post_confirmation_flow_summary.contract && order.post_confirmation_flow_summary.contract.accepted) ? (i18n.post_flow_contract_done || 'Aceite registrado.') : (i18n.post_flow_pending || 'Pendente'))) + '</span>';
+                html += '<span class="eop-order-card__flow-pill">' + escapeHtml((i18n.orders_flow_fields || 'Campos') + ': ' + (order.post_confirmation_flow_summary.documents ? (order.post_confirmation_flow_summary.documents.completed + '/' + order.post_confirmation_flow_summary.documents.total) : '0/0')) + '</span>';
+                html += '<span class="eop-order-card__flow-pill">' + escapeHtml((i18n.orders_flow_attachment || 'Anexo') + ': ' + (order.post_confirmation_flow_summary.attachment ? (order.post_confirmation_flow_summary.attachment.uploaded ? (i18n.orders_flow_uploaded || 'Enviado') : (order.post_confirmation_flow_summary.attachment.required ? (i18n.post_flow_pending || 'Pendente') : (i18n.orders_flow_optional || 'Opcional'))) : '—')) + '</span>';
+                html += '<span class="eop-order-card__flow-pill">' + escapeHtml((i18n.orders_flow_products || 'Produtos') + ': ' + (order.post_confirmation_flow_summary.products ? (order.post_confirmation_flow_summary.products.completed + '/' + order.post_confirmation_flow_summary.products.editable) : '0/0')) + '</span>';
+                html += '</div>';
+                html += '</div>';
+            }
+
             html += '<div class="eop-order-card__actions">';
 
             if (order.public_url) {
@@ -645,6 +673,7 @@
         var requestedPage = parseInt(page, 10) || 1;
         var search = $('#eop-orders-search').val() || '';
         var status = $('#eop-orders-status-filter').val() || 'any';
+        var postConfirmationFlow = $('#eop-orders-flow-filter').val() || 'any';
 
         ordersPage = requestedPage;
         $('#eop-orders-list').html('<div class="eop-card eop-orders-empty-state">' + escapeHtml(i18n.orders_loading || 'Carregando pedidos...') + '</div>');
@@ -655,7 +684,8 @@
             nonce: eop_vars.nonce,
             paged: requestedPage,
             search: search,
-            status: status
+            status: status,
+            post_confirmation_flow: postConfirmationFlow
         }, function (res) {
             if (!res.success) {
                 $('#eop-orders-list').html('<div class="eop-card eop-orders-empty-state">' + escapeHtml((res.data && res.data.message) || i18n.orders_error || 'Nao foi possivel carregar os pedidos agora.') + '</div>');
@@ -741,6 +771,7 @@
                 setShippingSummary(i18n.shipping_summary_default || 'Clique para calcular com o endereco do cliente.');
             }
 
+			loadPostConfirmationFlow(orderId);
             recalcTotals();
             showNotice(i18n.edit_loaded || 'Pedido carregado no painel para edicao.', 'success');
         }).fail(function () {
@@ -902,8 +933,186 @@
         recalcTotals();
     }
 
+    function renderPostConfirmationFlowPlaceholder(message) {
+        var $card = $('#eop-post-flow-card');
+
+        if (!$card.length) {
+            return;
+        }
+
+        $('#eop-post-flow-badge').text(i18n.post_flow_stage_inactive || 'Inativo').attr('class', 'eop-post-flow-badge is-inactive');
+        $('#eop-post-flow-subtitle').text(message || i18n.post_flow_unavailable_edit || 'O resumo complementar aparece quando um pedido existente entra em modo de edicao.');
+        $('#eop-post-flow-stats').empty();
+        $('#eop-post-flow-contract').text(i18n.post_flow_not_available || 'Este pedido nao esta usando o fluxo complementar da proposta.');
+        $('#eop-post-flow-signature-documents').html('<p>' + escapeHtml(i18n.post_flow_signature_documents_empty || 'Nenhum documento para assinatura foi gerado ainda.') + '</p>');
+        $('#eop-post-flow-order-data').html('<p>' + escapeHtml(i18n.post_flow_documents_empty || 'Nenhum dado do pedido preenchido no WooCommerce ate agora.') + '</p>');
+        $('#eop-post-flow-attachment').html('<p>' + escapeHtml(i18n.post_flow_attachment_missing || 'Nenhum anexo registrado.') + '</p>');
+        $('#eop-post-flow-products').html('<p>' + escapeHtml(i18n.post_flow_products_empty || 'Nenhuma personalizacao registrada ate agora.') + '</p>');
+        $('#eop-post-flow-public-link').prop('hidden', true).attr('href', '#');
+        $('#eop-post-flow-pdf-link').prop('hidden', true).attr('href', '#');
+    }
+
+    function renderPostConfirmationFlow(flow) {
+        flow = flow || {};
+
+        if (!flow.active_for_order) {
+            renderPostConfirmationFlowPlaceholder(i18n.post_flow_not_available || 'Este pedido nao esta usando o fluxo complementar da proposta.');
+            return;
+        }
+
+        $('#eop-post-flow-badge').text((flow.status && flow.status.current_stage_label) || '').attr('class', 'eop-post-flow-badge is-active');
+        $('#eop-post-flow-subtitle').text((flow.status && flow.status.completed_at) ? ((i18n.post_flow_completed_at || 'Concluido em') + ' ' + flow.status.completed_at + '.') : (i18n.post_flow_summary_ready || 'Payload estruturado pronto para PDF, admin e integracoes futuras.'));
+        $('#eop-post-flow-stats').empty();
+        $('#eop-post-flow-signature-documents').empty();
+        $('#eop-post-flow-order-data').empty();
+        $('#eop-post-flow-attachment').empty();
+        $('#eop-post-flow-products').empty();
+        var orderData = flow.order_data || flow.documents || [];
+        var orderDataFilled = (flow.summary && typeof flow.summary.order_data_filled !== 'undefined') ? flow.summary.order_data_filled : (flow.summary ? flow.summary.documents_completed : 0);
+        var orderDataTotal = (flow.summary && typeof flow.summary.order_data_total !== 'undefined') ? flow.summary.order_data_total : (flow.summary ? flow.summary.documents_total : 0);
+
+        [
+            { label: i18n.post_flow_stat_stage || 'Etapa atual', value: (flow.status && flow.status.current_stage_label) || '—' },
+            { label: i18n.post_flow_stat_documents || 'Dados do pedido', value: orderDataFilled + '/' + orderDataTotal },
+            { label: i18n.post_flow_stat_attachment || 'Anexo', value: (flow.summary && flow.summary.attachment_uploaded) ? (i18n.post_flow_attachment_done || 'Anexo registrado com sucesso.') : ((flow.summary && flow.summary.attachment_required) ? (i18n.post_flow_pending || 'Pendente') : 'Opcional') },
+            { label: i18n.post_flow_stat_products || 'Produtos', value: (flow.summary ? (flow.summary.products_completed + '/' + flow.summary.products_editable) : '0/0') }
+        ].forEach(function (stat) {
+            $('#eop-post-flow-stats').append(
+                '<div class="eop-post-flow-stat">' +
+                    '<span>' + escapeHtml(stat.label) + '</span>' +
+                    '<strong>' + escapeHtml(stat.value) + '</strong>' +
+                '</div>'
+            );
+        });
+
+        if (flow.contract && flow.contract.accepted) {
+            $('#eop-post-flow-contract').text((i18n.post_flow_contract_done || 'Aceite registrado.') + ' ' + [flow.contract.accepted_name, flow.contract.accepted_at].filter(Boolean).join(' • '));
+        } else {
+            $('#eop-post-flow-contract').text(i18n.post_flow_contract_pending || 'Aceite contratual pendente.');
+        }
+
+        if (flow.signature_documents && flow.signature_documents.length) {
+            flow.signature_documents.forEach(function (documentRow) {
+                $('#eop-post-flow-signature-documents').append(
+                    '<div class="eop-post-flow-row">' +
+                        '<strong>' + escapeHtml(documentRow.title || '') + '</strong>' +
+                        '<span><a href="' + escapeHtml(documentRow.admin_view_url || documentRow.public_view_url || '#') + '" target="_blank" rel="noopener">' + escapeHtml(documentRow.filename || documentRow.title || 'PDF') + '</a></span>' +
+                    '</div>'
+                );
+            });
+        }
+
+        if (!$('#eop-post-flow-signature-documents').children().length) {
+            $('#eop-post-flow-signature-documents').html('<p>' + escapeHtml(i18n.post_flow_signature_documents_empty || 'Nenhum documento para assinatura foi gerado ainda.') + '</p>');
+        }
+
+        if (orderData.length) {
+            orderData.forEach(function (documentField) {
+                if (!documentField.filled) {
+                    return;
+                }
+
+                $('#eop-post-flow-order-data').append(
+                    '<div class="eop-post-flow-row">' +
+                        '<strong>' + escapeHtml(documentField.label || '') + '</strong>' +
+                        '<span>' + escapeHtml(documentField.value || '') + '</span>' +
+                    '</div>'
+                );
+            });
+        }
+
+        if (!$('#eop-post-flow-order-data').children().length) {
+            $('#eop-post-flow-order-data').html('<p>' + escapeHtml(i18n.post_flow_documents_empty || 'Nenhum dado do pedido preenchido no WooCommerce ate agora.') + '</p>');
+        }
+
+        if (flow.attachment && flow.attachment.id) {
+            $('#eop-post-flow-attachment').append(
+                '<div class="eop-post-flow-row">' +
+                    '<strong>' + escapeHtml(flow.attachment.filename || '') + '</strong>' +
+                    '<span>' + escapeHtml(flow.attachment.uploaded_at || '') + '</span>' +
+                '</div>'
+            );
+
+            if (flow.attachment.url) {
+                $('#eop-post-flow-attachment').append('<p><a href="' + escapeHtml(flow.attachment.url) + '" target="_blank" rel="noopener">' + escapeHtml(flow.attachment.filename || 'Arquivo') + '</a></p>');
+            }
+        } else {
+            $('#eop-post-flow-attachment').html('<p>' + escapeHtml(i18n.post_flow_attachment_missing || 'Nenhum anexo registrado.') + '</p>');
+        }
+
+        if (flow.products && flow.products.length) {
+            flow.products.forEach(function (productRow) {
+                var statusLabel = productRow.locked ? (i18n.post_flow_locked || 'Bloqueado') : (productRow.custom_name ? (i18n.post_flow_customized || 'Personalizado') : (i18n.post_flow_pending || 'Pendente'));
+                var valueLabel = productRow.custom_name || productRow.original_name || '';
+
+                $('#eop-post-flow-products').append(
+                    '<div class="eop-post-flow-row">' +
+                        '<strong>' + escapeHtml(productRow.original_name || '') + '</strong>' +
+                        '<span>' + escapeHtml(valueLabel + ' • ' + statusLabel) + '</span>' +
+                    '</div>'
+                );
+            });
+        } else {
+            $('#eop-post-flow-products').html('<p>' + escapeHtml(i18n.post_flow_products_empty || 'Nenhuma personalizacao registrada ate agora.') + '</p>');
+        }
+
+        if (flow.links && flow.links.public_url) {
+            $('#eop-post-flow-public-link').prop('hidden', false).attr('href', flow.links.public_url).text(i18n.post_flow_open_public || 'Abrir link publico');
+        } else {
+            $('#eop-post-flow-public-link').prop('hidden', true).attr('href', '#');
+        }
+
+        if (flow.links && flow.links.admin_pdf_url) {
+            $('#eop-post-flow-pdf-link').prop('hidden', false).attr('href', flow.links.admin_pdf_url).text(i18n.post_flow_download_pdf || 'Baixar PDF complementar');
+        } else {
+            $('#eop-post-flow-pdf-link').prop('hidden', true).attr('href', '#');
+        }
+    }
+
+    function loadPostConfirmationFlow(orderId) {
+        var requestToken = postFlowRequestToken + 1;
+        var baseUrl = String(eop_vars.rest_url || '');
+
+        postFlowRequestToken = requestToken;
+
+        if (!orderId || !baseUrl) {
+            renderPostConfirmationFlowPlaceholder(i18n.post_flow_not_available || 'Este pedido nao esta usando o fluxo complementar da proposta.');
+            return;
+        }
+
+        renderPostConfirmationFlowPlaceholder(i18n.post_flow_loading || 'Carregando dados complementares da proposta...');
+
+        window.fetch(baseUrl + 'orders/' + encodeURIComponent(orderId) + '/post-confirmation?context=internal', {
+            method: 'GET',
+            headers: {
+                'X-WP-Nonce': String(eop_vars.rest_nonce || ''),
+                'Accept': 'application/json'
+            },
+            credentials: 'same-origin'
+        }).then(function (response) {
+            if (!response.ok) {
+                throw new Error('rest_error');
+            }
+
+            return response.json();
+        }).then(function (data) {
+            if (requestToken !== postFlowRequestToken) {
+                return;
+            }
+
+            renderPostConfirmationFlow(data || {});
+        }).catch(function () {
+            if (requestToken !== postFlowRequestToken) {
+                return;
+            }
+
+            renderPostConfirmationFlowPlaceholder(i18n.post_flow_not_available || 'Este pedido nao esta usando o fluxo complementar da proposta.');
+        });
+    }
+
     function resetForm() {
         editLoadToken += 1;
+        postFlowRequestToken += 1;
         items = [];
         renderItems();
 
@@ -1387,8 +1596,14 @@
             if (currentEditingOrderId) {
                 showNotice(res.data && res.data.message ? res.data.message : (i18n.edit_submit || 'Salvar alteracoes'), 'success');
                 ordersLoaded = false;
-                loadOrdersList(ordersPage || 1);
                 exitEditMode();
+
+                if (eop_vars.orders_url) {
+                    window.location.href = String(eop_vars.orders_url);
+                    return;
+                }
+
+                loadOrdersList(ordersPage || 1);
                 setAppView('orders');
                 return;
             }
@@ -1425,14 +1640,21 @@
         showNotice(i18n.edit_cancel || 'Edicao cancelada.', 'success');
     });
 
-    $(document).on('click', '.eop-pdv-nav__item', function () {
+    $(document).on('click', '.eop-pdv-nav__item', function (e) {
         if ($(this).is('[data-eop-nav-toggle]')) {
             togglePdfNavGroup($(this));
+            if ($(this).data('eop-view-target') === 'pdf') {
+                setAppView('pdf');
+            }
             return;
         }
 
         var targetView = $(this).data('eop-view-target');
 
+        if ($(this).is('a[href]') && targetView) {
+            e.preventDefault();
+        }
+
         if (targetView === 'new-order') {
             resetForm();
             setAppView('new-order');
@@ -1503,7 +1725,21 @@
     syncPdfSettingsFormSnapshot($(document));
 
     $(document).on('click', '.eop-order-edit-spa', function () {
-        loadOrderForEditing($(this).data('order-id'));
+        var orderId = parseInt($(this).data('order-id'), 10) || 0;
+
+        if (!orderId) {
+            return;
+        }
+
+        if (eop_vars.operation_url) {
+            var targetUrl = new URL(String(eop_vars.operation_url), window.location.origin);
+            targetUrl.searchParams.set('action', 'edit');
+            targetUrl.searchParams.set('order_id', String(orderId));
+            window.location.href = targetUrl.toString();
+            return;
+        }
+
+        loadOrderForEditing(orderId);
     });
 
     $('#eop-orders-refresh').on('click', function () {
@@ -1521,6 +1757,10 @@
         loadOrdersList(1);
     });
 
+	$('#eop-orders-flow-filter').on('change', function () {
+		loadOrdersList(1);
+	});
+
     $(document).on('click', '.eop-orders-page-btn', function () {
         if ($(this).is(':disabled')) {
             return;
diff --git a/assets/js/orders.js b/assets/js/orders.js
index af2a856..f8e645b 100644
--- a/assets/js/orders.js
+++ b/assets/js/orders.js
@@ -299,6 +299,145 @@
         recalcTotals();
     }
 
+    function renderPostConfirmationFlow(flow) {
+        var $card = $('#eop-post-flow-card');
+        var $badge = $('#eop-post-flow-badge');
+        var $subtitle = $('#eop-post-flow-subtitle');
+        var $stats = $('#eop-post-flow-stats');
+        var $contract = $('#eop-post-flow-contract');
+        var $signatureDocuments = $('#eop-post-flow-signature-documents');
+        var $documents = $('#eop-post-flow-order-data');
+        var $attachment = $('#eop-post-flow-attachment');
+        var $products = $('#eop-post-flow-products');
+        var $publicLink = $('#eop-post-flow-public-link');
+        var $pdfLink = $('#eop-post-flow-pdf-link');
+
+        if (!$card.length) {
+            return;
+        }
+
+        flow = flow || {};
+        $stats.empty();
+        $signatureDocuments.empty();
+        $documents.empty();
+        $attachment.empty();
+        $products.empty();
+        var orderData = flow.order_data || flow.documents || [];
+        var orderDataFilled = (flow.summary && typeof flow.summary.order_data_filled !== 'undefined') ? flow.summary.order_data_filled : (flow.summary ? flow.summary.documents_completed : 0);
+        var orderDataTotal = (flow.summary && typeof flow.summary.order_data_total !== 'undefined') ? flow.summary.order_data_total : (flow.summary ? flow.summary.documents_total : 0);
+
+        if (!flow.active_for_order) {
+            $badge.text(i18n.post_flow_stage_inactive || 'Inativo').attr('class', 'eop-post-flow-badge is-inactive');
+            $subtitle.text(i18n.post_flow_not_available || 'Este pedido nao esta usando o fluxo complementar da proposta.');
+            $contract.text(i18n.post_flow_not_available || 'Este pedido nao esta usando o fluxo complementar da proposta.');
+            $signatureDocuments.append('<p>' + $('<span>').text(i18n.post_flow_signature_documents_empty || 'Nenhum documento para assinatura foi gerado ainda.').html() + '</p>');
+            $documents.append('<p>' + $('<span>').text(i18n.post_flow_documents_empty || 'Nenhum dado do pedido preenchido no WooCommerce ate agora.').html() + '</p>');
+            $attachment.append('<p>' + $('<span>').text(i18n.post_flow_attachment_missing || 'Nenhum anexo registrado.').html() + '</p>');
+            $products.append('<p>' + $('<span>').text(i18n.post_flow_products_empty || 'Nenhuma personalizacao registrada ate agora.').html() + '</p>');
+            $publicLink.prop('hidden', true).attr('href', '#');
+            $pdfLink.prop('hidden', true).attr('href', '#');
+            return;
+        }
+
+        $badge.text((flow.status && flow.status.current_stage_label) || '').attr('class', 'eop-post-flow-badge is-active');
+        $subtitle.text((flow.status && flow.status.completed_at) ? ('Concluido em ' + flow.status.completed_at + '.') : 'Payload estruturado pronto para PDF, admin e futura integracao.');
+
+        [
+            { label: i18n.post_flow_stat_stage || 'Etapa atual', value: (flow.status && flow.status.current_stage_label) || '—' },
+            { label: i18n.post_flow_stat_documents || 'Dados do pedido', value: orderDataFilled + '/' + orderDataTotal },
+            { label: i18n.post_flow_stat_attachment || 'Anexo', value: (flow.summary && flow.summary.attachment_uploaded) ? (i18n.post_flow_attachment_done || 'Anexo registrado com sucesso.') : ((flow.summary && flow.summary.attachment_required) ? 'Pendente' : 'Opcional') },
+            { label: i18n.post_flow_stat_products || 'Produtos', value: (flow.summary ? (flow.summary.products_completed + '/' + flow.summary.products_editable) : '0/0') }
+        ].forEach(function (stat) {
+            $stats.append(
+                '<div class="eop-post-flow-stat">' +
+                    '<span>' + $('<span>').text(stat.label).html() + '</span>' +
+                    '<strong>' + $('<span>').text(stat.value).html() + '</strong>' +
+                '</div>'
+            );
+        });
+
+        if (flow.contract && flow.contract.accepted) {
+            $contract.text((i18n.post_flow_contract_done || 'Aceite registrado.') + ' ' + [flow.contract.accepted_name, flow.contract.accepted_at].filter(Boolean).join(' • '));
+        } else {
+            $contract.text(i18n.post_flow_contract_pending || 'Aceite contratual pendente.');
+        }
+
+        if (flow.signature_documents && flow.signature_documents.length) {
+            flow.signature_documents.forEach(function (documentField) {
+                $signatureDocuments.append(
+                    '<div class="eop-post-flow-row">' +
+                        '<strong>' + $('<span>').text(documentField.title || '').html() + '</strong>' +
+                        '<span><a href="' + $('<span>').text(documentField.admin_view_url || documentField.public_view_url || '#').html() + '" target="_blank" rel="noopener">' + $('<span>').text(documentField.filename || documentField.title || 'PDF').html() + '</a></span>' +
+                    '</div>'
+                );
+            });
+        }
+
+        if (!$signatureDocuments.children().length) {
+            $signatureDocuments.append('<p>' + $('<span>').text(i18n.post_flow_signature_documents_empty || 'Nenhum documento para assinatura foi gerado ainda.').html() + '</p>');
+        }
+
+        if (orderData.length) {
+            orderData.forEach(function (documentField) {
+                if (!documentField.filled) {
+                    return;
+                }
+                $documents.append(
+                    '<div class="eop-post-flow-row">' +
+                        '<strong>' + $('<span>').text(documentField.label || '').html() + '</strong>' +
+                        '<span>' + $('<span>').text(documentField.value || '').html() + '</span>' +
+                    '</div>'
+                );
+            });
+        }
+
+        if (!$documents.children().length) {
+            $documents.append('<p>' + $('<span>').text(i18n.post_flow_documents_empty || 'Nenhum dado do pedido preenchido no WooCommerce ate agora.').html() + '</p>');
+        }
+
+        if (flow.attachment && flow.attachment.id) {
+            $attachment.append(
+                '<div class="eop-post-flow-row">' +
+                    '<strong>' + $('<span>').text(flow.attachment.filename || '').html() + '</strong>' +
+                    '<span>' + $('<span>').text(flow.attachment.uploaded_at || '').html() + '</span>' +
+                '</div>'
+            );
+
+            if (flow.attachment.url) {
+                $attachment.append('<p><a href="' + $('<span>').text(flow.attachment.url).html() + '" target="_blank" rel="noopener">' + $('<span>').text(flow.attachment.filename || 'Arquivo').html() + '</a></p>');
+            }
+        } else {
+            $attachment.append('<p>' + $('<span>').text(i18n.post_flow_attachment_missing || 'Nenhum anexo registrado.').html() + '</p>');
+        }
+
+        if (flow.products && flow.products.length) {
+            flow.products.forEach(function (productRow) {
+                var statusLabel = productRow.locked ? 'Bloqueado' : (productRow.custom_name ? 'Personalizado' : 'Pendente');
+                var valueLabel = productRow.custom_name || productRow.original_name || '';
+                $products.append(
+                    '<div class="eop-post-flow-row">' +
+                        '<strong>' + $('<span>').text(productRow.original_name || '').html() + '</strong>' +
+                        '<span>' + $('<span>').text(valueLabel + ' • ' + statusLabel).html() + '</span>' +
+                    '</div>'
+                );
+            });
+        } else {
+            $products.append('<p>' + $('<span>').text(i18n.post_flow_products_empty || 'Nenhuma personalizacao registrada ate agora.').html() + '</p>');
+        }
+
+        if (flow.links && flow.links.public_url) {
+            $publicLink.prop('hidden', false).attr('href', flow.links.public_url).text(i18n.post_flow_open_public || 'Abrir link publico');
+        } else {
+            $publicLink.prop('hidden', true).attr('href', '#');
+        }
+
+        if (flow.links && flow.links.admin_pdf_url) {
+            $pdfLink.prop('hidden', false).attr('href', flow.links.admin_pdf_url).text(i18n.post_flow_download_pdf || 'Baixar PDF complementar');
+        } else {
+            $pdfLink.prop('hidden', true).attr('href', '#');
+        }
+    }
+
     /* ========================================
      *  Shipping panel helpers
      * ======================================== */
@@ -493,6 +632,8 @@
             // Status
             $('#eop-status').val(d.status || 'pending');
 
+			renderPostConfirmationFlow(d.post_confirmation_flow || {});
+
             recalcTotals();
         }).fail(function () {
             showNotice('Erro ao carregar pedido.', 'error');
diff --git a/assets/js/settings-admin.js b/assets/js/settings-admin.js
index dfdc9d1..7c0deff 100644
--- a/assets/js/settings-admin.js
+++ b/assets/js/settings-admin.js
@@ -3,6 +3,7 @@
     'use strict';
 
     var mediaFrame = null;
+    var documentPdfFrame = null;
     var mediaUploaderBound = false;
     var colorisConfigured = false;
     var colorSwatches = ['#067bc2', '#84bcda', '#80e377', '#ecc30b', '#f37748', '#d56062'];
@@ -41,11 +42,149 @@
         $selectButton.text(hasUrl ? getSettingsVar('change_logo', 'Trocar logo') : getSettingsVar('select_logo', 'Selecionar logo'));
     }
 
+    function buildDocumentEditorSettings() {
+        return {
+            tinymce: {
+                wpautop: true,
+                toolbar1: 'formatselect,bold,italic,bullist,numlist,blockquote,alignleft,aligncenter,alignright,link,unlink,undo,redo',
+                toolbar2: 'removeformat,pastetext,fullscreen'
+            },
+            quicktags: true,
+            mediaButtons: false
+        };
+    }
+
+    function initSignatureEditors(scope) {
+        var $scope = scope && scope.jquery ? scope : $(scope || document);
+
+        if (typeof window.wp === 'undefined' || !window.wp.editor || typeof window.wp.editor.initialize !== 'function') {
+            return;
+        }
+
+        $scope.find('textarea[data-signature-document-editor]').each(function () {
+            var textarea = this;
+            var id = String(textarea.id || '');
+
+            if (!id || $(textarea).data('editorInitialized')) {
+                return;
+            }
+
+            window.wp.editor.initialize(id, buildDocumentEditorSettings());
+            $(textarea).data('editorInitialized', true);
+        });
+    }
+
+    function destroySignatureEditor($textarea) {
+        var id = String($textarea.attr('id') || '');
+
+        if (!id || typeof window.wp === 'undefined' || !window.wp.editor || typeof window.wp.editor.remove !== 'function') {
+            return;
+        }
+
+        window.wp.editor.remove(id);
+    }
+
+    function updateSignatureDocumentPanels($document) {
+        var source = String($document.find('[data-signature-document-source]').val() || 'editor');
+
+        $document.find('[data-signature-document-panel="editor"]').toggleClass('is-hidden', source !== 'editor');
+        $document.find('[data-signature-document-panel="attachment"]').toggleClass('is-hidden', source !== 'attachment');
+    }
+
     function bindMediaUploader() {
         if (mediaUploaderBound) {
             return;
         }
 
+        function bindSignatureDocumentMedia() {
+            $(document).on('click', '[data-signature-document-attachment-select]', function (event) {
+                var $button = $(this);
+                var $document = $button.closest('[data-signature-document]');
+                var $hidden = $document.find('[data-signature-document-attachment-id]').first();
+                var $name = $document.find('[data-signature-document-attachment-name]').first();
+                var $remove = $document.find('[data-signature-document-attachment-remove]').first();
+
+                event.preventDefault();
+
+                if (typeof wp === 'undefined' || !wp.media) {
+                    return;
+                }
+
+                documentPdfFrame = wp.media({
+                    title: getSettingsVar('document_media_title', 'Selecionar PDF do documento'),
+                    button: {
+                        text: getSettingsVar('document_media_button', 'Usar este PDF')
+                    },
+                    multiple: false,
+                    library: {
+                        type: 'application/pdf'
+                    }
+                });
+
+                documentPdfFrame.on('select', function () {
+                    var selection = documentPdfFrame.state().get('selection').first();
+                    var attachment = selection ? selection.toJSON() : null;
+
+                    $hidden.val(attachment && attachment.id ? attachment.id : '');
+                    $name.text(attachment && attachment.filename ? attachment.filename : getSettingsVar('document_pdf_empty', 'Nenhum PDF anexado ainda.'));
+                    $button.text(attachment && attachment.id ? 'Trocar PDF' : 'Selecionar PDF');
+                    $remove.toggleClass('is-hidden', !(attachment && attachment.id));
+                });
+
+                documentPdfFrame.open();
+            });
+
+            $(document).on('click', '[data-signature-document-attachment-remove]', function (event) {
+                var $button = $(this);
+                var $document = $button.closest('[data-signature-document]');
+
+                event.preventDefault();
+
+                $document.find('[data-signature-document-attachment-id]').val('');
+                $document.find('[data-signature-document-attachment-name]').text(getSettingsVar('document_pdf_empty', 'Nenhum PDF anexado ainda.'));
+                $document.find('[data-signature-document-attachment-select]').text('Selecionar PDF');
+                $button.addClass('is-hidden');
+            });
+        }
+
+        function bindSignatureDocuments() {
+            $(document).on('click', '[data-signature-document-add]', function (event) {
+                var $root = $(this).closest('[data-signature-documents]');
+                var $list = $root.find('[data-signature-documents-list]').first();
+                var $template = $('#eop-signature-document-template');
+                var nextIndex = parseInt($root.attr('data-next-index') || '0', 10);
+                var markup;
+                var $document;
+
+                event.preventDefault();
+
+                if (!$template.length) {
+                    return;
+                }
+
+                markup = String($template.html() || '').replace(/__INDEX__/g, String(nextIndex));
+                $document = $(markup);
+                $list.append($document);
+                $root.attr('data-next-index', String(nextIndex + 1));
+                updateSignatureDocumentPanels($document);
+                initSignatureEditors($document);
+            });
+
+            $(document).on('click', '[data-signature-document-remove]', function (event) {
+                var $document = $(this).closest('[data-signature-document]');
+
+                event.preventDefault();
+                $document.find('textarea[data-signature-document-editor]').each(function () {
+                    destroySignatureEditor($(this));
+                });
+                $document.remove();
+            });
+
+            $(document).on('change', '[data-signature-document-source]', function () {
+                updateSignatureDocumentPanels($(this).closest('[data-signature-document]'));
+            });
+        }
+
         mediaUploaderBound = true;
 
         $(document).on('click', '[data-media-select]', function (event) {
@@ -274,6 +413,59 @@
         });
     }
 
+    function initLockedProductsSelector(scope) {
+        var $scope = scope && scope.jquery ? scope : $(scope || document);
+
+        if (!$.fn.select2) {
+            return;
+        }
+
+        $scope.find('.eop-settings-product-selector').each(function () {
+            var $select = $(this);
+            var targetSelector = String($select.data('target-input') || '');
+            var $target = targetSelector ? $(targetSelector) : $();
+
+            if ($select.hasClass('select2-hidden-accessible')) {
+                return;
+            }
+
+            $select.select2({
+                width: '100%',
+                multiple: true,
+                allowClear: true,
+                placeholder: getSettingsVar('locked_placeholder', 'Busque produtos por nome ou SKU...'),
+                language: {
+                    noResults: function () {
+                        return getSettingsVar('locked_no_results', 'Nenhum produto encontrado.');
+                    }
+                },
+                ajax: {
+                    url: getSettingsVar('ajax_url', ''),
+                    dataType: 'json',
+                    delay: 250,
+                    data: function (params) {
+                        return {
+                            action: 'eop_search_products',
+                            nonce: getSettingsVar('nonce', ''),
+                            term: params.term || ''
+                        };
+                    },
+                    processResults: function (data) {
+                        return data || { results: [] };
+                    },
+                    cache: true
+                }
+            });
+
+            if ($target.length) {
+                $select.on('change', function () {
+                    var value = ($select.val() || []).filter(Boolean).join(',');
+                    $target.val(value);
+                }).trigger('change');
+            }
+        });
+    }
+
     function initSettingsUi(scope) {
         var $scope = scope && scope.jquery ? scope : $(scope || document);
 
@@ -281,6 +473,11 @@
         injectPdfHelpTooltips($scope);
         initColorFields($scope);
         mountColorDefaultButtons($scope);
+        initLockedProductsSelector($scope);
+        initSignatureEditors($scope);
+        $scope.find('[data-signature-document]').each(function () {
+            updateSignatureDocumentPanels($(this));
+        });
     }
 
     $(function () {
@@ -329,6 +526,8 @@
         }
 
         bindMediaUploader();
+        bindSignatureDocuments();
+        bindSignatureDocumentMedia();
 
         $(document).on('eop:settings-ui:init', function (event, scope) {
             initSettingsUi(scope || document);
diff --git a/assets/scss/admin/_spa-shell.scss b/assets/scss/admin/_spa-shell.scss
index 1c24860..e6b0b1f 100644
--- a/assets/scss/admin/_spa-shell.scss
+++ b/assets/scss/admin/_spa-shell.scss
@@ -360,6 +360,108 @@ body.eop-admin-spa-screen {
   line-height: 1.6;
 }
 
+.eop-post-flow-card {
+  display: grid;
+  gap: 16px;
+}
+
+.eop-post-flow-card__head {
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: 12px;
+
+  h2 {
+    margin: 0 0 4px;
+  }
+
+  p {
+    margin: 0;
+    color: #5f6b7a;
+  }
+}
+
+.eop-post-flow-badge {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  padding: 6px 12px;
+  border-radius: 999px;
+  font-size: 12px;
+  font-weight: 700;
+  background: #eef2f7;
+  color: #324154;
+
+  &.is-active {
+    background: #e8f4eb;
+    color: #1f6a38;
+  }
+}
+
+.eop-post-flow-card__stats {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
+  gap: 12px;
+}
+
+.eop-post-flow-stat {
+  padding: 12px 14px;
+  border: 1px solid #dde5ef;
+  border-radius: 14px;
+  background: #fafcff;
+
+  span {
+    display: block;
+    margin-bottom: 6px;
+    color: #667487;
+    font-size: 11px;
+    font-weight: 700;
+    letter-spacing: .06em;
+    text-transform: uppercase;
+  }
+
+  strong {
+    font-size: 16px;
+  }
+}
+
+.eop-post-flow-card__section {
+  padding-top: 14px;
+  border-top: 1px solid #edf1f5;
+
+  h3 {
+    margin: 0 0 8px;
+  }
+}
+
+.eop-post-flow-list {
+  display: grid;
+  gap: 8px;
+}
+
+.eop-post-flow-row {
+  display: grid;
+  gap: 2px;
+  padding: 10px 12px;
+  border: 1px solid #e6ebf2;
+  border-radius: 12px;
+  background: #fff;
+
+  strong {
+    font-size: 13px;
+  }
+
+  span {
+    color: #586577;
+  }
+}
+
+.eop-post-flow-card__actions {
+  display: flex;
+  gap: 8px;
+  flex-wrap: wrap;
+}
+
 .eop-admin-panel-head {
   margin-bottom: 0;
 
@@ -443,6 +545,54 @@ body.eop-admin-spa-screen {
   color: #55607f;
 }
 
+.eop-order-card__flow {
+  display: grid;
+  gap: 10px;
+  margin-top: 14px;
+  padding: 14px;
+  border: 1px solid #e3e9f6;
+  border-radius: 16px;
+  background: linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);
+}
+
+.eop-order-card__flow-head {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
+
+  span {
+    color: #607089;
+    font-size: 11px;
+    font-weight: 800;
+    letter-spacing: .08em;
+    text-transform: uppercase;
+  }
+
+  strong {
+    color: #12203a;
+    font-size: 13px;
+  }
+}
+
+.eop-order-card__flow-list {
+  display: flex;
+  gap: 8px;
+  flex-wrap: wrap;
+}
+
+.eop-order-card__flow-pill {
+  display: inline-flex;
+  align-items: center;
+  min-height: 32px;
+  padding: 0 12px;
+  border-radius: 999px;
+  background: #eef3fb;
+  color: #344764;
+  font-size: 12px;
+  font-weight: 600;
+}
+
 .eop-admin-license-shell {
   max-width: 980px;
 
diff --git a/includes/class-admin-page.php b/includes/class-admin-page.php
index 9372505..88b7791 100644
--- a/includes/class-admin-page.php
+++ b/includes/class-admin-page.php
@@ -81,25 +81,156 @@ class EOP_Admin_Page {
 
     use EOP_License_Guard;
 
+    const MENU_SLUG = 'eop-pedido-expresso';
+
     public static function init() {
         if ( ! self::_resolve_env_config() ) {
             return;
         }
 
+        add_action( 'admin_init', array( __CLASS__, 'maybe_redirect_legacy_request' ) );
         add_action( 'admin_menu', array( __CLASS__, 'register_page' ) );
         add_action( 'admin_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );
         add_action( 'admin_enqueue_scripts', array( __CLASS__, 'enqueue_menu_flyout_assets' ) );
         add_filter( 'admin_body_class', array( __CLASS__, 'filter_admin_body_class' ) );
     }
 
-    public static function filter_admin_body_class( $classes ) {
-        $page = isset( $_GET['page'] ) ? sanitize_key( wp_unslash( $_GET['page'] ) ) : '';
+    public static function get_page_definitions() {
+        return array(
+            'operation' => array(
+                'page_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'    => 'edit_shop_orders',
+                'view'          => 'new-order',
+                'asset_context' => 'operation',
+            ),
+            'orders' => array(
+                'page_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'    => 'edit_shop_orders',
+                'view'          => 'orders',
+                'asset_context' => 'orders',
+            ),
+            'pdf' => array(
+                'page_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'    => 'edit_shop_orders',
+                'view'          => 'pdf',
+                'asset_context' => 'pdf',
+            ),
+            'settings-general' => array(
+                'page_title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'       => 'manage_options',
+                'view'             => 'settings',
+                'asset_context'    => 'settings',
+                'settings_section' => 'general',
+            ),
+            'appearance' => array(
+                'page_title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'       => 'manage_options',
+                'view'             => 'appearance',
+                'asset_context'    => 'settings',
+                'settings_section' => 'appearance',
+            ),
+            'post-confirmation' => array(
+                'page_title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'       => 'manage_options',
+                'view'             => 'post-confirmation',
+                'asset_context'    => 'settings',
+                'settings_section' => 'post-confirmation',
+            ),
+            'license' => array(
+                'page_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'menu_title'    => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
+                'capability'    => 'manage_options',
+                'view'          => 'license',
+                'asset_context' => 'license',
+            ),
+        );
+    }
+
+    public static function get_page_config( $page_key = '' ) {
+        $pages = self::get_page_definitions();
+
+        if ( '' === $page_key ) {
+            $page_key = self::get_current_page_key();
+        }
+
+        return isset( $pages[ $page_key ] ) ? $pages[ $page_key ] : array();
+    }
+
+    public static function get_current_page_slug() {
+        return isset( $_GET['page'] ) ? sanitize_key( wp_unslash( $_GET['page'] ) ) : '';
+    }
+
+    public static function get_current_page_key() {
+        $page_slug = self::get_current_page_slug();
+        $view      = self::normalize_view( isset( $_GET['view'] ) ? wp_unslash( $_GET['view'] ) : '' );
+        $map       = array(
+            'new-order'         => 'operation',
+            'orders'            => 'orders',
+            'pdf'               => 'pdf',
+            'settings'          => 'settings-general',
+            'appearance'        => 'appearance',
+            'post-confirmation' => 'post-confirmation',
+            'license'           => 'license',
+        );
+
+        if ( self::MENU_SLUG !== $page_slug ) {
+            return '';
+        }
 
-        if ( 'eop-pedido-expresso' !== $page ) {
+        return isset( $map[ $view ] ) ? $map[ $view ] : 'operation';
+    }
+
+    public static function get_page_url( $page_key, $args = array() ) {
+        $page_config = self::get_page_config( $page_key );
+        $view        = ! empty( $page_config['view'] ) ? $page_config['view'] : 'new-order';
+
+        return add_query_arg(
+            array_merge(
+                array(
+                    'page' => self::MENU_SLUG,
+                    'view' => $view,
+                ),
+                $args
+            ),
+            admin_url( 'admin.php' )
+        );
+    }
+
+    public static function get_view_urls() {
+        $urls = array(
+            'new-order' => self::get_page_url( 'operation' ),
+            'orders'    => self::get_page_url( 'orders' ),
+            'settings'  => self::get_page_url( 'settings-general' ),
+            'appearance' => self::get_page_url( 'appearance' ),
+            'post-confirmation' => self::get_page_url( 'post-confirmation' ),
+            'license'   => self::get_page_url( 'license' ),
+        );
+
+        $urls['pdf'] = self::get_view_url( 'pdf' );
+
+        return $urls;
+    }
+
+    public static function filter_admin_body_class( $classes ) {
+        if ( self::MENU_SLUG !== self::get_current_page_slug() ) {
             return $classes;
         }
 
-        return trim( $classes . ' eop-admin-spa-screen' );
+        $page_key = self::get_current_page_key();
+
+        $classes .= ' eop-admin-spa-screen';
+
+        if ( in_array( $page_key, array( 'pdf', 'settings-general', 'appearance', 'post-confirmation' ), true ) ) {
+            $classes .= ' eop-settings-screen';
+        }
+
+        return trim( $classes );
     }
 
     public static function get_default_view() {
@@ -116,6 +247,8 @@ class EOP_Admin_Page {
             'orders'    => current_user_can( 'edit_shop_orders' ),
             'pdf'       => current_user_can( 'edit_shop_orders' ),
             'settings'  => current_user_can( 'manage_options' ),
+            'appearance' => current_user_can( 'manage_options' ),
+            'post-confirmation' => current_user_can( 'manage_options' ),
             'license'   => current_user_can( 'manage_options' ),
         );
 
@@ -134,17 +267,60 @@ class EOP_Admin_Page {
     }
 
     public static function get_view_url( $view = '', $args = array() ) {
-        $query = array( 'page' => 'eop-pedido-expresso' );
+        $view = self::normalize_view( $view );
+
+        if ( 'pdf' === $view ) {
+            $query = array_merge(
+                array(
+                    'page'   => self::MENU_SLUG,
+                    'view'   => 'pdf',
+                    'pdf_tab' => isset( $args['pdf_tab'] ) ? sanitize_key( (string) $args['pdf_tab'] ) : 'general',
+                ),
+                $args
+            );
 
-        if ( '' !== $view ) {
-            $query['view'] = self::normalize_view( $view );
+            return add_query_arg( $query, admin_url( 'admin.php' ) );
         }
 
-        if ( ! empty( $args ) ) {
-            $query = array_merge( $query, $args );
+        $view_to_page = array(
+            'new-order' => 'operation',
+            'orders'    => 'orders',
+            'settings'  => 'settings-general',
+            'appearance' => 'appearance',
+            'post-confirmation' => 'post-confirmation',
+            'license'   => 'license',
+        );
+
+        $page_key = isset( $view_to_page[ $view ] ) ? $view_to_page[ $view ] : 'operation';
+
+        return self::get_page_url( $page_key, $args );
+    }
+
+    public static function maybe_redirect_legacy_request() {
+        $page = self::get_current_page_slug();
+        $legacy_page_map = array(
+            'eop-pedido-expresso-pedidos'      => 'orders',
+            'eop-pedido-expresso-configuracoes' => 'settings',
+            'eop-pedido-expresso-aparencia'    => 'appearance',
+            'eop-pedido-expresso-fluxo'        => 'post-confirmation',
+            'eop-pedido-expresso-licenca'      => 'license',
+        );
+
+        if ( ! isset( $legacy_page_map[ $page ] ) ) {
+            return;
         }
 
-        return add_query_arg( $query, admin_url( 'admin.php' ) );
+        $view = $legacy_page_map[ $page ];
+        $args = array();
+
+        foreach ( array( 'action', 'order_id', 'document', 'preview_order', 'eop_pdf_action' ) as $arg_key ) {
+            if ( isset( $_GET[ $arg_key ] ) ) {
+                $args[ $arg_key ] = sanitize_text_field( wp_unslash( $_GET[ $arg_key ] ) );
+            }
+        }
+
+        wp_safe_redirect( self::get_view_url( $view, $args ) );
+        exit;
     }
 
     /**
@@ -158,7 +334,7 @@ class EOP_Admin_Page {
             __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
             __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
             'edit_shop_orders',
-            'eop-pedido-expresso',
+            self::MENU_SLUG,
             array( __CLASS__, 'render_page' )
         );
     }
@@ -169,11 +345,9 @@ class EOP_Admin_Page {
      * @param string $hook
      */
     public static function enqueue_assets( $hook ) {
-        if ( 'aireset_page_eop-pedido-expresso' !== $hook ) {
-            return;
-        }
+        $page_config = self::get_page_config();
 
-        if ( ! function_exists( 'WC' ) || ! WC() ) {
+        if ( empty( $page_config ) || empty( $page_config['asset_context'] ) ) {
             return;
         }
 
@@ -183,81 +357,36 @@ class EOP_Admin_Page {
             wp_enqueue_style( 'eop-admin-selected-font', $font_url, array(), null );
         }
 
-        // Select2 (shipped with WooCommerce).
-        wp_enqueue_style( 'select2', WC()->plugin_url() . '/assets/css/select2.css', array(), WC_VERSION );
-        wp_enqueue_script( 'select2', WC()->plugin_url() . '/assets/js/select2/select2.full.min.js', array( 'jquery' ), WC_VERSION, true );
-
-        wp_enqueue_style( 'eop-admin', EOP_PLUGIN_URL . 'assets/css/admin.css', array( 'select2' ), EOP_VERSION );
-        wp_enqueue_style( 'eop-frontend', EOP_PLUGIN_URL . 'assets/css/frontend.css', array(), EOP_VERSION );
-        wp_enqueue_style( 'eop-pdf-admin', EOP_PLUGIN_URL . 'assets/css/pdf-admin.css', array( 'eop-admin' ), EOP_VERSION );
-        wp_enqueue_script( 'eop-admin', EOP_PLUGIN_URL . 'assets/js/admin.js', array( 'jquery', 'select2' ), EOP_VERSION, true );
+        if ( ! function_exists( 'WC' ) || ! WC() ) {
+            return;
+        }
 
         $font_css_path = ABSPATH . 'wp-content/plugins/checkout-aireset-master/backend/assets/css/jquery.fontselect.css';
         $font_js_path  = ABSPATH . 'wp-content/plugins/checkout-aireset-master/backend/assets/js/jquery.fontselect.js';
 
-        wp_enqueue_media();
-        wp_enqueue_style( 'eop-coloris', EOP_PLUGIN_URL . 'assets/css/coloris.min.css', array(), EOP_VERSION );
-        wp_enqueue_style(
-            'eop-settings-admin',
-            EOP_PLUGIN_URL . 'assets/css/settings-admin.css',
-            array( 'eop-admin', 'eop-coloris' ),
-            EOP_VERSION
-        );
-
-        if ( file_exists( $font_css_path ) ) {
-            wp_enqueue_style(
-                'eop-fontselect',
-                content_url( 'plugins/checkout-aireset-master/backend/assets/css/jquery.fontselect.css' ),
-                array(),
-                EOP_VERSION
-            );
-        }
-
-        wp_enqueue_script( 'eop-coloris', EOP_PLUGIN_URL . 'assets/js/coloris.min.js', array(), EOP_VERSION, true );
-
-        if ( file_exists( $font_js_path ) ) {
-            wp_enqueue_script(
-                'eop-fontselect',
-                content_url( 'plugins/checkout-aireset-master/backend/assets/js/jquery.fontselect.js' ),
-                array( 'jquery' ),
-                EOP_VERSION,
-                true
-            );
-        }
-
-        wp_enqueue_script(
-            'eop-settings-admin',
-            EOP_PLUGIN_URL . 'assets/js/settings-admin.js',
-            array_filter( array( 'jquery', 'eop-coloris', 'media-editor', 'media-upload', file_exists( $font_js_path ) ? 'eop-fontselect' : '' ) ),
-            EOP_VERSION,
-            true
-        );
-
-        wp_localize_script(
-            'eop-settings-admin',
-            'eop_settings_vars',
-            array(
-                'has_fontselect'   => file_exists( $font_js_path ),
-                'font_placeholder' => __( 'Escolha uma fonte Google', EOP_TEXT_DOMAIN ),
-                'media_title'      => __( 'Selecionar logo', EOP_TEXT_DOMAIN ),
-                'media_button'     => __( 'Usar esta imagem', EOP_TEXT_DOMAIN ),
-                'remove_logo'      => __( 'Remover logo', EOP_TEXT_DOMAIN ),
-                'select_logo'      => __( 'Selecionar logo', EOP_TEXT_DOMAIN ),
-                'change_logo'      => __( 'Trocar logo', EOP_TEXT_DOMAIN ),
-                'no_logo'          => __( 'Nenhum logo selecionado ainda.', EOP_TEXT_DOMAIN ),
-                'color_default'    => __( 'Padrao', EOP_TEXT_DOMAIN ),
-                'color_clear'      => __( 'Limpar', EOP_TEXT_DOMAIN ),
-                'color_close'      => __( 'Fechar', EOP_TEXT_DOMAIN ),
-            )
-        );
-
-        wp_localize_script( 'eop-admin', 'eop_vars', array(
-            'ajax_url'      => admin_url( 'admin-ajax.php' ),
-            'nonce'         => wp_create_nonce( 'eop_nonce' ),
-            'discount_mode' => EOP_Settings::get( 'discount_mode', 'both' ),
-            'initial_view'  => self::normalize_view( isset( $_GET['view'] ) ? wp_unslash( $_GET['view'] ) : '' ),
-            'view_url_base' => self::get_view_url(),
-            'i18n'          => array(
+        $wc_version = defined( 'WC_VERSION' ) ? WC_VERSION : EOP_VERSION;
+
+        switch ( $page_config['asset_context'] ) {
+            case 'operation':
+            case 'orders':
+                wp_enqueue_style( 'select2', WC()->plugin_url() . '/assets/css/select2.css', array(), $wc_version );
+                wp_enqueue_script( 'select2', WC()->plugin_url() . '/assets/js/select2/select2.full.min.js', array( 'jquery' ), $wc_version, true );
+                wp_enqueue_style( 'eop-admin', EOP_PLUGIN_URL . 'assets/css/admin.css', array( 'select2' ), EOP_VERSION );
+                wp_enqueue_style( 'eop-frontend', EOP_PLUGIN_URL . 'assets/css/frontend.css', array(), EOP_VERSION );
+                wp_enqueue_script( 'eop-admin', EOP_PLUGIN_URL . 'assets/js/admin.js', array( 'jquery', 'select2' ), EOP_VERSION, true );
+
+                wp_localize_script( 'eop-admin', 'eop_vars', array(
+                    'ajax_url'      => admin_url( 'admin-ajax.php' ),
+                    'nonce'         => wp_create_nonce( 'eop_nonce' ),
+                    'rest_url'      => esc_url_raw( rest_url( trailingslashit( apply_filters( 'eop_post_confirmation_rest_namespace', 'aireset-expresso-order/v1' ) ) ) ),
+                    'rest_nonce'    => wp_create_nonce( 'wp_rest' ),
+                    'discount_mode' => EOP_Settings::get( 'discount_mode', 'both' ),
+                    'initial_view'  => $page_config['view'],
+                    'view_url_base' => self::get_view_url( 'new-order' ),
+                    'view_urls'     => self::get_view_urls(),
+                    'operation_url' => self::get_view_url( 'new-order' ),
+                    'orders_url'    => self::get_view_url( 'orders' ),
+                    'i18n'          => array(
                 'search_product'   => __( 'Buscar produto por nome ou SKU...', EOP_TEXT_DOMAIN ),
                 'no_items'         => __( 'Nenhum produto adicionado.', EOP_TEXT_DOMAIN ),
                 'label_price'      => __( 'Preco', EOP_TEXT_DOMAIN ),
@@ -291,16 +420,189 @@ class EOP_Admin_Page {
                 'shipping_postcode_error'   => __( 'Nao foi possivel buscar o CEP agora. Continue manualmente.', EOP_TEXT_DOMAIN ),
                 'shipping_rates_found'      => __( 'Opcoes encontradas. Escolha a melhor para o cliente.', EOP_TEXT_DOMAIN ),
                 'processing'       => __( 'Processando...', EOP_TEXT_DOMAIN ),
+                'loading'          => __( 'Carregando...', EOP_TEXT_DOMAIN ),
                 'error'            => __( 'Erro ao criar pedido. Tente novamente.', EOP_TEXT_DOMAIN ),
                 'success'          => __( 'Pedido criado com sucesso!', EOP_TEXT_DOMAIN ),
                 'submit_label'     => __( 'Finalizar e Gerar PDF', EOP_TEXT_DOMAIN ),
+                'edit_title'       => __( 'Editando pedido', EOP_TEXT_DOMAIN ),
+                'edit_submit'      => __( 'Salvar alteracoes', EOP_TEXT_DOMAIN ),
+                'edit_loaded'      => __( 'Pedido carregado no painel para edicao.', EOP_TEXT_DOMAIN ),
+                'edit_error'       => __( 'Nao foi possivel abrir este pedido para edicao.', EOP_TEXT_DOMAIN ),
                 'nav_new_order'    => __( 'Novo pedido', EOP_TEXT_DOMAIN ),
                 'nav_orders'       => __( 'Pedidos', EOP_TEXT_DOMAIN ),
                 'nav_pdf'          => __( 'PDF', EOP_TEXT_DOMAIN ),
                 'nav_settings'     => __( 'Configuracoes', EOP_TEXT_DOMAIN ),
                 'nav_license'      => __( 'Licenca', EOP_TEXT_DOMAIN ),
+                'orders_flow_title'         => __( 'Fluxo complementar', EOP_TEXT_DOMAIN ),
+                'orders_flow_contract'      => __( 'Contrato', EOP_TEXT_DOMAIN ),
+                'orders_flow_fields'        => __( 'Dados do pedido', EOP_TEXT_DOMAIN ),
+                'orders_flow_attachment'    => __( 'Anexo', EOP_TEXT_DOMAIN ),
+                'orders_flow_products'      => __( 'Produtos', EOP_TEXT_DOMAIN ),
+                'orders_flow_uploaded'      => __( 'Enviado', EOP_TEXT_DOMAIN ),
+                'orders_flow_optional'      => __( 'Opcional', EOP_TEXT_DOMAIN ),
+                'post_flow_loading'           => __( 'Carregando dados complementares da proposta...', EOP_TEXT_DOMAIN ),
+                'post_flow_not_available'     => __( 'Este pedido nao esta usando o fluxo complementar da proposta.', EOP_TEXT_DOMAIN ),
+                'post_flow_unavailable_edit'  => __( 'O resumo complementar aparece quando um pedido existente entra em modo de edicao.', EOP_TEXT_DOMAIN ),
+                'post_flow_stage_inactive'    => __( 'Inativo', EOP_TEXT_DOMAIN ),
+                'post_flow_contract_pending'  => __( 'Aceite contratual pendente.', EOP_TEXT_DOMAIN ),
+                'post_flow_contract_done'     => __( 'Aceite registrado.', EOP_TEXT_DOMAIN ),
+                'post_flow_documents_empty'   => __( 'Nenhum dado do pedido preenchido no WooCommerce ate agora.', EOP_TEXT_DOMAIN ),
+                'post_flow_signature_documents_empty' => __( 'Nenhum documento para assinatura foi gerado ainda.', EOP_TEXT_DOMAIN ),
+                'post_flow_attachment_missing' => __( 'Nenhum anexo registrado.', EOP_TEXT_DOMAIN ),
+                'post_flow_attachment_done'   => __( 'Anexo registrado com sucesso.', EOP_TEXT_DOMAIN ),
+                'post_flow_products_empty'    => __( 'Nenhuma personalizacao registrada ate agora.', EOP_TEXT_DOMAIN ),
+                'post_flow_open_public'       => __( 'Abrir link publico', EOP_TEXT_DOMAIN ),
+                'post_flow_download_pdf'      => __( 'Baixar PDF complementar', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_stage'        => __( 'Etapa atual', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_documents'    => __( 'Dados do pedido', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_attachment'   => __( 'Anexo', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_products'     => __( 'Produtos', EOP_TEXT_DOMAIN ),
+                'post_flow_summary_ready'     => __( 'Payload estruturado pronto para PDF, admin e integracoes futuras.', EOP_TEXT_DOMAIN ),
+                'post_flow_completed_at'      => __( 'Concluido em', EOP_TEXT_DOMAIN ),
+                'post_flow_locked'            => __( 'Bloqueado', EOP_TEXT_DOMAIN ),
+                'post_flow_customized'        => __( 'Personalizado', EOP_TEXT_DOMAIN ),
+                'post_flow_pending'           => __( 'Pendente', EOP_TEXT_DOMAIN ),
             ),
-        ) );
+                ) );
+                break;
+
+            case 'settings':
+                wp_enqueue_media();
+                wp_enqueue_editor();
+                wp_enqueue_style( 'select2', WC()->plugin_url() . '/assets/css/select2.css', array(), $wc_version );
+                wp_enqueue_script( 'select2', WC()->plugin_url() . '/assets/js/select2/select2.full.min.js', array( 'jquery' ), $wc_version, true );
+                wp_enqueue_style( 'eop-admin', EOP_PLUGIN_URL . 'assets/css/admin.css', array( 'select2' ), EOP_VERSION );
+                wp_enqueue_style( 'eop-coloris', EOP_PLUGIN_URL . 'assets/css/coloris.min.css', array(), EOP_VERSION );
+                wp_enqueue_style(
+                    'eop-settings-admin',
+                    EOP_PLUGIN_URL . 'assets/css/settings-admin.css',
+                    array( 'eop-admin', 'eop-coloris', 'select2' ),
+                    EOP_VERSION
+                );
+
+                if ( file_exists( $font_css_path ) ) {
+                    wp_enqueue_style(
+                        'eop-fontselect',
+                        content_url( 'plugins/checkout-aireset-master/backend/assets/css/jquery.fontselect.css' ),
+                        array(),
+                        EOP_VERSION
+                    );
+                }
+
+                wp_enqueue_script( 'eop-coloris', EOP_PLUGIN_URL . 'assets/js/coloris.min.js', array(), EOP_VERSION, true );
+
+                if ( file_exists( $font_js_path ) ) {
+                    wp_enqueue_script(
+                        'eop-fontselect',
+                        content_url( 'plugins/checkout-aireset-master/backend/assets/js/jquery.fontselect.js' ),
+                        array( 'jquery' ),
+                        EOP_VERSION,
+                        true
+                    );
+                }
+
+                wp_enqueue_script(
+                    'eop-settings-admin',
+                    EOP_PLUGIN_URL . 'assets/js/settings-admin.js',
+                    array_filter( array( 'jquery', 'select2', 'eop-coloris', 'media-editor', 'media-upload', 'wp-editor', file_exists( $font_js_path ) ? 'eop-fontselect' : '' ) ),
+                    EOP_VERSION,
+                    true
+                );
+
+                wp_localize_script(
+                    'eop-settings-admin',
+                    'eop_settings_vars',
+                    array(
+                        'has_fontselect'   => file_exists( $font_js_path ),
+                        'font_placeholder' => __( 'Escolha uma fonte Google', EOP_TEXT_DOMAIN ),
+                        'media_title'      => __( 'Selecionar logo', EOP_TEXT_DOMAIN ),
+                        'media_button'     => __( 'Usar esta imagem', EOP_TEXT_DOMAIN ),
+                        'remove_logo'      => __( 'Remover logo', EOP_TEXT_DOMAIN ),
+                        'select_logo'      => __( 'Selecionar logo', EOP_TEXT_DOMAIN ),
+                        'change_logo'      => __( 'Trocar logo', EOP_TEXT_DOMAIN ),
+                        'no_logo'          => __( 'Nenhum logo selecionado ainda.', EOP_TEXT_DOMAIN ),
+                        'color_default'    => __( 'Padrao', EOP_TEXT_DOMAIN ),
+                        'color_clear'      => __( 'Limpar', EOP_TEXT_DOMAIN ),
+                        'color_close'      => __( 'Fechar', EOP_TEXT_DOMAIN ),
+                        'ajax_url'         => admin_url( 'admin-ajax.php' ),
+                        'nonce'            => wp_create_nonce( 'eop_nonce' ),
+                        'locked_placeholder' => __( 'Busque produtos por nome ou SKU...', EOP_TEXT_DOMAIN ),
+                        'locked_no_results'  => __( 'Nenhum produto encontrado.', EOP_TEXT_DOMAIN ),
+                        'document_add'       => __( 'Adicionar documento', EOP_TEXT_DOMAIN ),
+                        'document_remove'    => __( 'Remover documento', EOP_TEXT_DOMAIN ),
+                        'document_media_title' => __( 'Selecionar PDF do documento', EOP_TEXT_DOMAIN ),
+                        'document_media_button' => __( 'Usar este PDF', EOP_TEXT_DOMAIN ),
+                        'document_pdf_empty' => __( 'Nenhum PDF anexado ainda.', EOP_TEXT_DOMAIN ),
+                        'document_editor_placeholder' => __( 'Escreva ou cole aqui o texto completo do documento. A formatacao sera preservada no PDF gerado.', EOP_TEXT_DOMAIN ),
+                        'document_type_editor' => __( 'Texto formatado', EOP_TEXT_DOMAIN ),
+                        'document_type_attachment' => __( 'PDF anexado', EOP_TEXT_DOMAIN ),
+                        'document_visual_label' => __( 'Visualizar no fluxo', EOP_TEXT_DOMAIN ),
+                        'document_download_label' => __( 'Botao de download', EOP_TEXT_DOMAIN ),
+                        'document_title_label' => __( 'Titulo do documento', EOP_TEXT_DOMAIN ),
+                        'document_description_label' => __( 'Descricao curta', EOP_TEXT_DOMAIN ),
+                        'document_source_label' => __( 'Origem do documento', EOP_TEXT_DOMAIN ),
+                        'document_body_label' => __( 'Conteudo do documento', EOP_TEXT_DOMAIN ),
+                        'document_attachment_label' => __( 'Arquivo PDF base', EOP_TEXT_DOMAIN ),
+                    )
+                );
+                break;
+
+            case 'pdf':
+                $document_type = isset( $_GET['document'] ) && 'proposal' === sanitize_key( wp_unslash( $_GET['document'] ) ) ? 'proposal' : 'order';
+                $font_urls     = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_font_stylesheet_urls( $document_type ) : array();
+
+                if ( empty( $font_urls ) && $font_url ) {
+                    $font_urls[] = $font_url;
+                }
+
+                foreach ( array_values( array_unique( $font_urls ) ) as $index => $selected_font_url ) {
+                    wp_enqueue_style( 'eop-pdf-selected-font-' . $index, $selected_font_url, array(), null );
+                }
+
+                wp_enqueue_media();
+                wp_enqueue_style( 'eop-admin', EOP_PLUGIN_URL . 'assets/css/admin.css', array(), EOP_VERSION );
+                wp_enqueue_style( 'eop-coloris', EOP_PLUGIN_URL . 'assets/css/coloris.min.css', array(), EOP_VERSION );
+                wp_enqueue_style( 'eop-orders', EOP_PLUGIN_URL . 'assets/css/orders.css', array( 'eop-admin' ), EOP_VERSION );
+                wp_enqueue_style( 'eop-settings-admin', EOP_PLUGIN_URL . 'assets/css/settings-admin.css', array( 'eop-admin', 'eop-coloris' ), EOP_VERSION );
+                wp_enqueue_style( 'eop-pdf-admin', EOP_PLUGIN_URL . 'assets/css/pdf-admin.css', array( 'eop-settings-admin' ), EOP_VERSION );
+                wp_enqueue_script( 'eop-coloris', EOP_PLUGIN_URL . 'assets/js/coloris.min.js', array(), EOP_VERSION, true );
+                wp_enqueue_script(
+                    'eop-settings-admin',
+                    EOP_PLUGIN_URL . 'assets/js/settings-admin.js',
+                    array( 'jquery', 'eop-coloris', 'media-editor', 'media-upload' ),
+                    EOP_VERSION,
+                    true
+                );
+
+                wp_localize_script(
+                    'eop-settings-admin',
+                    'eop_settings_vars',
+                    array(
+                        'has_fontselect'   => false,
+                        'font_placeholder' => __( 'Escolha uma fonte Google', EOP_TEXT_DOMAIN ),
+                        'media_title'      => __( 'Selecionar logo', EOP_TEXT_DOMAIN ),
+                        'media_button'     => __( 'Usar esta imagem', EOP_TEXT_DOMAIN ),
+                        'remove_logo'      => __( 'Remover logo', EOP_TEXT_DOMAIN ),
+                        'select_logo'      => __( 'Selecionar logo', EOP_TEXT_DOMAIN ),
+                        'change_logo'      => __( 'Trocar logo', EOP_TEXT_DOMAIN ),
+                        'no_logo'          => __( 'Nenhum logo selecionado ainda.', EOP_TEXT_DOMAIN ),
+                        'color_default'    => __( 'Padrao', EOP_TEXT_DOMAIN ),
+                        'color_clear'      => __( 'Limpar', EOP_TEXT_DOMAIN ),
+                        'color_close'      => __( 'Fechar', EOP_TEXT_DOMAIN ),
+                        'pdf_help_map'     => class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_admin_tooltip_map() : array(),
+                        'help_label'       => __( 'Ajuda da configuracao', EOP_TEXT_DOMAIN ),
+                        'help_statuses'    => array(
+                            'active'       => __( 'Ativo', EOP_TEXT_DOMAIN ),
+                            'experimental' => __( 'Experimental', EOP_TEXT_DOMAIN ),
+                        ),
+                    )
+                );
+                break;
+
+            case 'license':
+                wp_enqueue_style( 'eop-admin', EOP_PLUGIN_URL . 'assets/css/admin.css', array(), EOP_VERSION );
+                break;
+        }
     }
 
     public static function enqueue_menu_flyout_assets() {
@@ -308,6 +610,10 @@ class EOP_Admin_Page {
             return;
         }
 
+        if ( '' === self::get_current_page_key() && ! class_exists( 'EOP_PDF_Admin_Page' ) ) {
+            return;
+        }
+
         wp_enqueue_style(
             'aireset-admin-flyout',
             EOP_PLUGIN_URL . 'assets/css/admin-menu-flyout.css',
@@ -329,9 +635,9 @@ class EOP_Admin_Page {
                 'key'   => 'eop-view-pdf-general',
                 'label' => __( 'Geral', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-admin-home',
-                'url'   => class_exists( 'EOP_PDF_Admin_Page' ) ? EOP_PDF_Admin_Page::get_tab_url( 'general' ) : admin_url( 'admin.php?page=eop-pdf&tab=general' ),
+                'url'   => self::get_view_url( 'pdf', array( 'pdf_tab' => 'general' ) ),
                 'query' => array(
-                    'page'    => 'eop-pedido-expresso',
+                    'page'    => self::MENU_SLUG,
                     'view'    => 'pdf',
                     'pdf_tab' => 'general',
                 ),
@@ -343,9 +649,9 @@ class EOP_Admin_Page {
                 'key'   => 'eop-view-pdf-documents',
                 'label' => __( 'Documentos', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-media-text',
-                'url'   => class_exists( 'EOP_PDF_Admin_Page' ) ? EOP_PDF_Admin_Page::get_tab_url( 'documents' ) : admin_url( 'admin.php?page=eop-pdf&tab=documents' ),
+                'url'   => self::get_view_url( 'pdf', array( 'pdf_tab' => 'documents' ) ),
                 'query' => array(
-                    'page'    => 'eop-pedido-expresso',
+                    'page'    => self::MENU_SLUG,
                     'view'    => 'pdf',
                     'pdf_tab' => 'documents',
                 ),
@@ -354,9 +660,9 @@ class EOP_Admin_Page {
                 'key'   => 'eop-view-pdf-edocuments',
                 'label' => __( 'Documentos eletronicos', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-media-spreadsheet',
-                'url'   => class_exists( 'EOP_PDF_Admin_Page' ) ? EOP_PDF_Admin_Page::get_tab_url( 'edocuments' ) : admin_url( 'admin.php?page=eop-pdf&tab=edocuments' ),
+                'url'   => self::get_view_url( 'pdf', array( 'pdf_tab' => 'edocuments' ) ),
                 'query' => array(
-                    'page'    => 'eop-pedido-expresso',
+                    'page'    => self::MENU_SLUG,
                     'view'    => 'pdf',
                     'pdf_tab' => 'edocuments',
                 ),
@@ -365,9 +671,9 @@ class EOP_Admin_Page {
                 'key'   => 'eop-view-pdf-advanced',
                 'label' => __( 'Avancado', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-admin-tools',
-                'url'   => class_exists( 'EOP_PDF_Admin_Page' ) ? EOP_PDF_Admin_Page::get_tab_url( 'advanced' ) : admin_url( 'admin.php?page=eop-pdf&tab=advanced' ),
+                'url'   => self::get_view_url( 'pdf', array( 'pdf_tab' => 'advanced' ) ),
                 'query' => array(
-                    'page'    => 'eop-pedido-expresso',
+                    'page'    => self::MENU_SLUG,
                     'view'    => 'pdf',
                     'pdf_tab' => 'advanced',
                 ),
@@ -376,9 +682,9 @@ class EOP_Admin_Page {
                 'key'   => 'eop-view-pdf-documentation',
                 'label' => __( 'Documentacao', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-editor-help',
-                'url'   => class_exists( 'EOP_PDF_Admin_Page' ) ? EOP_PDF_Admin_Page::get_tab_url( 'documentation' ) : admin_url( 'admin.php?page=eop-pdf&tab=documentation' ),
+                'url'   => self::get_view_url( 'pdf', array( 'pdf_tab' => 'documentation' ) ),
                 'query' => array(
-                    'page'    => 'eop-pedido-expresso',
+                    'page'    => self::MENU_SLUG,
                     'view'    => 'pdf',
                     'pdf_tab' => 'documentation',
                 ),
@@ -386,13 +692,23 @@ class EOP_Admin_Page {
         }
 
         $items = array(
+            array(
+                'key'   => 'eop-view-operation',
+                'label' => __( 'Operacao', EOP_TEXT_DOMAIN ),
+                'icon'  => 'dashicons-cart',
+                'url'   => self::get_view_url( 'new-order' ),
+                'query' => array(
+                    'page' => self::MENU_SLUG,
+                    'view' => 'new-order',
+                ),
+            ),
             array(
                 'key'   => 'eop-view-orders',
                 'label' => __( 'Pedidos', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-list-view',
                 'url'   => self::get_view_url( 'orders' ),
                 'query' => array(
-                    'page' => 'eop-pedido-expresso',
+                    'page' => self::MENU_SLUG,
                     'view' => 'orders',
                 ),
             ),
@@ -400,11 +716,10 @@ class EOP_Admin_Page {
                 'key'   => 'eop-view-pdf',
                 'label' => __( 'PDF', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-media-document',
-                'url'   => self::get_view_url( 'pdf', array( 'pdf_tab' => 'general' ) ),
+                'url'   => self::get_view_url( 'pdf' ),
                 'query' => array(
-                    'page'    => 'eop-pedido-expresso',
-                    'view'    => 'pdf',
-                    'pdf_tab' => 'general',
+                    'page' => self::MENU_SLUG,
+                    'view' => 'pdf',
                 ),
                 'children' => $pdf_children,
             ),
@@ -415,23 +730,45 @@ class EOP_Admin_Page {
                 $items,
                 array(
                     'key'   => 'eop-view-settings',
-                    'label' => __( 'Configuracoes', EOP_TEXT_DOMAIN ),
+                    'label' => __( 'Configuracoes gerais', EOP_TEXT_DOMAIN ),
                     'icon'  => 'dashicons-admin-generic',
                     'url'   => self::get_view_url( 'settings' ),
                     'query' => array(
-                        'page' => 'eop-pedido-expresso',
+                        'page' => self::MENU_SLUG,
                         'view' => 'settings',
                     ),
                 )
             );
 
+            $items[] = array(
+                'key'   => 'eop-view-appearance',
+                'label' => __( 'Aparencia e painel', EOP_TEXT_DOMAIN ),
+                'icon'  => 'dashicons-art',
+                'url'   => self::get_view_url( 'appearance' ),
+                'query' => array(
+                    'page' => self::MENU_SLUG,
+                    'view' => 'appearance',
+                ),
+            );
+
+            $items[] = array(
+                'key'   => 'eop-view-post-confirmation',
+                'label' => __( 'Fluxo complementar', EOP_TEXT_DOMAIN ),
+                'icon'  => 'dashicons-controls-repeat',
+                'url'   => self::get_view_url( 'post-confirmation' ),
+                'query' => array(
+                    'page' => self::MENU_SLUG,
+                    'view' => 'post-confirmation',
+                ),
+            );
+
             $items[] = array(
                 'key'   => 'eop-view-license',
                 'label' => __( 'Licenca', EOP_TEXT_DOMAIN ),
                 'icon'  => 'dashicons-admin-network',
                 'url'   => self::get_view_url( 'license' ),
                 'query' => array(
-                    'page' => 'eop-pedido-expresso',
+                    'page' => self::MENU_SLUG,
                     'view' => 'license',
                 ),
             );
@@ -439,7 +776,7 @@ class EOP_Admin_Page {
 
         $config = array(
             'currentPage' => $current_page,
-            'anchorPage'  => 'eop-pedido-expresso',
+            'anchorPage'  => self::MENU_SLUG,
             'menuRoot'    => 'toplevel_page_aireset',
             'title'       => __( 'Pedido Expresso', EOP_TEXT_DOMAIN ),
             'items'       => $items,
@@ -457,7 +794,13 @@ class EOP_Admin_Page {
      * Render the admin page.
      */
     public static function render_page() {
-        if ( ! current_user_can( 'edit_shop_orders' ) ) {
+        $page_config = self::get_page_config();
+
+        if ( empty( $page_config ) ) {
+            wp_die( esc_html__( 'Pagina administrativa invalida.', EOP_TEXT_DOMAIN ) );
+        }
+
+        if ( ! current_user_can( $page_config['capability'] ) ) {
             wp_die( esc_html__( 'Acesso negado.', EOP_TEXT_DOMAIN ) );
         }
 
diff --git a/includes/class-document-manager.php b/includes/class-document-manager.php
index cd5fbb2..f57d161 100644
--- a/includes/class-document-manager.php
+++ b/includes/class-document-manager.php
@@ -19,6 +19,10 @@ class EOP_Document_Manager {
         if ( class_exists( 'EOP_PDF_Settings' ) ) {
             add_action( 'update_option_' . EOP_PDF_Settings::OPTION_KEY, array( __CLASS__, 'maybe_invalidate_cached_documents' ), 10, 2 );
         }
+
+        if ( class_exists( 'EOP_Settings' ) ) {
+            add_action( 'update_option_' . EOP_Settings::OPTION_KEY, array( __CLASS__, 'maybe_invalidate_cached_documents_from_global_settings' ), 10, 2 );
+        }
     }
 
     private static function maybe_bootstrap_pdf_output_buffer() {
@@ -75,6 +79,21 @@ class EOP_Document_Manager {
         );
     }
 
+    public static function get_pdf_preview_document_url( WC_Order $order, $document_type = '', $force_download = false ) {
+        $document_type = self::normalize_document_type( $document_type ? $document_type : self::detect_document_type( $order ) );
+
+        return add_query_arg(
+            array(
+                'action'           => 'eop_download_pdf',
+                'order_id'         => $order->get_id(),
+                'document'         => $document_type,
+                'download'         => $force_download ? '1' : '0',
+                'preview_document' => '1',
+            ),
+            admin_url( 'admin-post.php' )
+        );
+    }
+
     public static function is_expresso_order( $order ) {
         return $order instanceof WC_Order && 'aireset-expresso-order' === (string) $order->get_created_via();
     }
@@ -254,6 +273,13 @@ class EOP_Document_Manager {
                 'totals_font_size'        => '16',
                 'note_font_size'          => '13',
             );
+        $style_settings = class_exists( 'EOP_PDF_Settings' )
+            ? EOP_PDF_Settings::get_document_style_settings( $document_type )
+            : array(
+                'typography' => array(),
+                'blocks'     => array(),
+                'spacing'    => array(),
+            );
 
         return array(
             'header_background_color' => self::sanitize_pdf_hex_color( $settings[ $prefix . '_header_background_color' ] ?? $defaults['header_background_color'], $defaults['header_background_color'] ),
@@ -267,6 +293,9 @@ class EOP_Document_Manager {
             'table_body_font_size'    => self::sanitize_pdf_font_size( $settings[ $prefix . '_table_body_font_size' ] ?? $defaults['table_body_font_size'], $defaults['table_body_font_size'] ),
             'totals_font_size'        => self::sanitize_pdf_font_size( $settings[ $prefix . '_totals_font_size' ] ?? $defaults['totals_font_size'], $defaults['totals_font_size'] ),
             'note_font_size'          => self::sanitize_pdf_font_size( $settings[ $prefix . '_note_font_size' ] ?? $defaults['note_font_size'], $defaults['note_font_size'] ),
+			'styles'                  => is_array( $style_settings['typography'] ?? null ) ? $style_settings['typography'] : array(),
+			'blocks'                  => is_array( $style_settings['blocks'] ?? null ) ? $style_settings['blocks'] : array(),
+			'spacing'                 => is_array( $style_settings['spacing'] ?? null ) ? $style_settings['spacing'] : array(),
         );
     }
 
@@ -439,7 +468,7 @@ class EOP_Document_Manager {
             );
         }
 
-        return $rows;
+        return self::reconcile_total_row_display_values( $rows );
     }
 
     public static function get_cached_pdf_path( WC_Order $order, $document_type = '', $force_regenerate = false ) {
@@ -500,6 +529,20 @@ class EOP_Document_Manager {
         self::purge_cached_documents();
     }
 
+    public static function maybe_invalidate_cached_documents_from_global_settings( $old_value, $value ) {
+        $old_value = is_array( $old_value ) ? $old_value : array();
+        $value     = is_array( $value ) ? $value : array();
+        $defaults  = class_exists( 'EOP_Settings' ) ? EOP_Settings::get_defaults() : array( 'font_family' => 'Montserrat:400,700' );
+        $old_font  = sanitize_text_field( (string) ( $old_value['font_family'] ?? $defaults['font_family'] ?? '' ) );
+        $new_font  = sanitize_text_field( (string) ( $value['font_family'] ?? $defaults['font_family'] ?? '' ) );
+
+        if ( $old_font === $new_font ) {
+            return;
+        }
+
+        self::purge_cached_documents();
+    }
+
     private static function purge_cached_documents() {
         $upload_dir = wp_upload_dir();
 
@@ -643,6 +686,9 @@ class EOP_Document_Manager {
         $test_mode        = ! empty( $settings['test_mode'] ) && 'yes' === (string) $settings['test_mode'];
         $total_rows       = self::get_document_total_rows( $totals, $document_type );
         $billing_label    = self::get_billing_label( $order );
+		$style_groups     = is_array( $visual_settings['styles'] ?? null ) ? $visual_settings['styles'] : array();
+		$block_styles     = is_array( $visual_settings['blocks'] ?? null ) ? $visual_settings['blocks'] : array();
+		$spacing_settings = is_array( $visual_settings['spacing'] ?? null ) ? $visual_settings['spacing'] : array();
         $preview_style    = implode(
             '; ',
             array(
@@ -659,11 +705,92 @@ class EOP_Document_Manager {
                 '--eop-doc-note-size: ' . absint( $visual_settings['note_font_size'] ) . 'px',
             )
         );
+        $sheet_style = self::build_preview_inline_style(
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $block_styles['page_background_color'] ?? '#ffffff' ),
+            )
+        );
+        $brand_name_style = self::get_preview_style_from_profile( $style_groups['brand_name'] ?? array() );
+        $brand_meta_style = self::get_preview_style_from_profile( $style_groups['brand_meta'] ?? array(), array( 'margin-top: 6px' ) );
+        $title_style      = self::get_preview_style_from_profile( $style_groups['title'] ?? array(), array( 'margin: 0 0 10px' ) );
+        $meta_style       = self::get_preview_style_from_profile( $style_groups['meta'] ?? array() );
+        $summary_label_style = self::get_preview_style_from_profile( $style_groups['summary_label'] ?? array() );
+        $summary_value_style = self::get_preview_style_from_profile( $style_groups['summary_value'] ?? array() );
+        $summary_block_style = self::build_preview_inline_style(
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $block_styles['summary_background_color'] ?? '#ffffff' ),
+                'padding: ' . absint( $spacing_settings['summary_block_padding'] ?? 14 ) . 'px',
+                'border-radius: 12px',
+            )
+        );
+        $table_header_cell_style = self::get_preview_style_from_profile(
+            $style_groups['table_header'] ?? array(),
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $visual_settings['header_background_color'] ),
+                'padding-top: ' . absint( $spacing_settings['table_header_cell_padding_y'] ?? 10 ) . 'px',
+                'padding-bottom: ' . absint( $spacing_settings['table_header_cell_padding_y'] ?? 10 ) . 'px',
+                '-webkit-print-color-adjust: exact',
+                'print-color-adjust: exact',
+                'color-adjust: exact',
+            )
+        );
+        $table_body_cell_style = self::get_preview_style_from_profile(
+            $style_groups['table_body'] ?? array(),
+            array(
+                'padding-top: ' . absint( $spacing_settings['table_body_cell_padding_y'] ?? 10 ) . 'px',
+                'padding-bottom: ' . absint( $spacing_settings['table_body_cell_padding_y'] ?? 10 ) . 'px',
+            )
+        );
+        $table_meta_style = self::get_preview_style_from_profile( $style_groups['table_meta'] ?? array(), array( 'margin-top: 6px', 'display: block' ) );
+        $discount_meta_style = self::get_preview_style_from_profile( $style_groups['table_meta'] ?? array() );
+        $totals_row_style = self::get_preview_style_from_profile(
+            $style_groups['totals'] ?? array(),
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $block_styles['totals_background_color'] ?? '#ffffff' ),
+                'padding-top: ' . absint( $spacing_settings['totals_row_padding_y'] ?? 8 ) . 'px',
+                'padding-bottom: ' . absint( $spacing_settings['totals_row_padding_y'] ?? 8 ) . 'px',
+            )
+        );
+        $grand_total_style = self::get_preview_style_from_profile(
+            $style_groups['grand_total'] ?? array(),
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $block_styles['totals_background_color'] ?? '#ffffff' ),
+                'padding-top: ' . absint( $spacing_settings['totals_row_padding_y'] ?? 8 ) . 'px',
+                'padding-bottom: ' . absint( $spacing_settings['totals_row_padding_y'] ?? 8 ) . 'px',
+            )
+        );
+        $notes_wrapper_style = self::build_preview_inline_style(
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $block_styles['notes_background_color'] ?? '#ffffff' ),
+                'padding: ' . absint( $spacing_settings['notes_block_padding'] ?? 14 ) . 'px',
+                'border-radius: 12px',
+            )
+        );
+        $notes_label_style = self::get_preview_style_from_profile( $style_groups['notes_label'] ?? array() );
+        $notes_body_style  = self::get_preview_style_from_profile( $style_groups['notes_body'] ?? array(), array( 'margin: 8px 0 0' ) );
+        $footer_style      = self::get_preview_style_from_profile(
+            $style_groups['footer'] ?? array(),
+            array(
+                'background-color: ' . self::sanitize_pdf_hex_color( $block_styles['footer_background_color'] ?? '#ffffff' ),
+                'padding-top: ' . absint( $spacing_settings['footer_padding_top'] ?? 16 ) . 'px',
+            )
+        );
+        $header_cell_style = implode(
+            '; ',
+            array(
+                'background-color: ' . $visual_settings['header_background_color'],
+                'color: ' . $visual_settings['header_text_color'],
+                'font-size: ' . absint( $visual_settings['table_header_font_size'] ) . 'px',
+                '-webkit-print-color-adjust: exact',
+                'print-color-adjust: exact',
+                'color-adjust: exact',
+            )
+        );
 
         ob_start();
         ?>
         <div class="eop-pdf-preview eop-pdf-preview--<?php echo esc_attr( $template_name ); ?><?php echo $ink_saving ? ' is-ink-saving' : ''; ?>" style="<?php echo esc_attr( $preview_style ); ?>">
-            <div class="eop-pdf-preview__sheet eop-pdf-preview__sheet--<?php echo esc_attr( strtolower( $paper_size ) ); ?>">
+            <div class="eop-pdf-preview__sheet eop-pdf-preview__sheet--<?php echo esc_attr( strtolower( $paper_size ) ); ?>" style="<?php echo esc_attr( $sheet_style ); ?>">
                 <?php if ( $test_mode ) : ?>
                     <div class="eop-pdf-preview__watermark"><?php esc_html_e( 'MODO DE TESTE', EOP_TEXT_DOMAIN ); ?></div>
                 <?php endif; ?>
@@ -673,41 +800,41 @@ class EOP_Document_Manager {
                             <img class="eop-pdf-preview__logo" src="<?php echo 0 === strpos( $shop_logo_url, 'data:image/' ) ? esc_attr( $shop_logo_url ) : esc_url( $shop_logo_url ); ?>" style="max-height: <?php echo esc_attr( $shop_logo_height ); ?>;" alt="">
                         <?php endif; ?>
                         <div>
-                            <strong><?php echo esc_html( $shop_name ?: get_bloginfo( 'name' ) ); ?></strong>
+                            <strong style="<?php echo esc_attr( $brand_name_style ); ?>"><?php echo esc_html( $shop_name ?: get_bloginfo( 'name' ) ); ?></strong>
                             <?php foreach ( $shop_identity as $identity_line ) : ?>
-                                <span><?php echo esc_html( $identity_line ); ?></span>
+                                <span style="<?php echo esc_attr( $brand_meta_style ); ?>"><?php echo esc_html( $identity_line ); ?></span>
                             <?php endforeach; ?>
                         </div>
                     </div>
-                    <div class="eop-pdf-preview__meta">
-                        <h3><?php echo esc_html( 'proposal' === $document_type ? __( 'PROPOSTA', EOP_TEXT_DOMAIN ) : __( 'PEDIDO', EOP_TEXT_DOMAIN ) ); ?></h3>
-                        <div><?php esc_html_e( 'Numero do documento:', EOP_TEXT_DOMAIN ); ?> <strong><?php echo esc_html( $document_number ); ?></strong></div>
-                        <div><?php esc_html_e( 'Data:', EOP_TEXT_DOMAIN ); ?> <?php echo esc_html( $date ? $date->date_i18n( 'd/m/Y' ) : '—' ); ?></div>
-                        <div><?php esc_html_e( 'Pedido WooCommerce:', EOP_TEXT_DOMAIN ); ?> #<?php echo esc_html( $order->get_id() ); ?></div>
+                    <div class="eop-pdf-preview__meta" style="<?php echo esc_attr( $meta_style ); ?>">
+                        <h3 style="<?php echo esc_attr( $title_style ); ?>"><?php echo esc_html( 'proposal' === $document_type ? __( 'PROPOSTA', EOP_TEXT_DOMAIN ) : __( 'PEDIDO', EOP_TEXT_DOMAIN ) ); ?></h3>
+                        <div style="<?php echo esc_attr( $meta_style ); ?>"><?php esc_html_e( 'Numero do documento:', EOP_TEXT_DOMAIN ); ?> <strong><?php echo esc_html( $document_number ); ?></strong></div>
+                        <div style="<?php echo esc_attr( $meta_style ); ?>"><?php esc_html_e( 'Data:', EOP_TEXT_DOMAIN ); ?> <?php echo esc_html( $date ? $date->date_i18n( 'd/m/Y' ) : '—' ); ?></div>
+                        <div style="<?php echo esc_attr( $meta_style ); ?>"><?php esc_html_e( 'Pedido WooCommerce:', EOP_TEXT_DOMAIN ); ?> #<?php echo esc_html( $order->get_id() ); ?></div>
                     </div>
                 </div>
 
                 <div class="eop-pdf-preview__summary">
-                    <div>
-                        <span><?php esc_html_e( 'Cliente', EOP_TEXT_DOMAIN ); ?></span>
-                        <strong><?php echo esc_html( $customer_name ?: __( 'Nao informado', EOP_TEXT_DOMAIN ) ); ?></strong>
+                    <div style="<?php echo esc_attr( $summary_block_style ); ?>">
+                        <span style="<?php echo esc_attr( $summary_label_style ); ?>"><?php esc_html_e( 'Cliente', EOP_TEXT_DOMAIN ); ?></span>
+                        <strong style="<?php echo esc_attr( $summary_value_style ); ?>"><?php echo esc_html( $customer_name ?: __( 'Nao informado', EOP_TEXT_DOMAIN ) ); ?></strong>
                         <?php if ( $show_email && $order->get_billing_email() ) : ?>
-                            <small><?php echo esc_html( $order->get_billing_email() ); ?></small>
+                            <small style="<?php echo esc_attr( $meta_style ); ?>"><?php echo esc_html( $order->get_billing_email() ); ?></small>
                         <?php endif; ?>
                         <?php if ( $show_phone && $order->get_billing_phone() ) : ?>
-                            <small><?php echo esc_html( $order->get_billing_phone() ); ?></small>
+                            <small style="<?php echo esc_attr( $meta_style ); ?>"><?php echo esc_html( $order->get_billing_phone() ); ?></small>
                         <?php endif; ?>
                     </div>
                     <?php if ( $show_shipping ) : ?>
-                        <div>
-                            <span><?php esc_html_e( 'Entrega', EOP_TEXT_DOMAIN ); ?></span>
-                            <strong><?php echo esc_html( self::get_shipping_label( $order ) ); ?></strong>
+                        <div style="<?php echo esc_attr( $summary_block_style ); ?>">
+                            <span style="<?php echo esc_attr( $summary_label_style ); ?>"><?php esc_html_e( 'Entrega', EOP_TEXT_DOMAIN ); ?></span>
+                            <strong style="<?php echo esc_attr( $summary_value_style ); ?>"><?php echo esc_html( self::get_shipping_label( $order ) ); ?></strong>
                         </div>
                     <?php endif; ?>
                     <?php if ( $show_billing && '' !== $billing_label ) : ?>
-                        <div>
-                            <span><?php esc_html_e( 'Cobranca', EOP_TEXT_DOMAIN ); ?></span>
-                            <strong><?php echo esc_html( $billing_label ); ?></strong>
+                        <div style="<?php echo esc_attr( $summary_block_style ); ?>">
+                            <span style="<?php echo esc_attr( $summary_label_style ); ?>"><?php esc_html_e( 'Cobranca', EOP_TEXT_DOMAIN ); ?></span>
+                            <strong style="<?php echo esc_attr( $summary_value_style ); ?>"><?php echo esc_html( $billing_label ); ?></strong>
                         </div>
                     <?php endif; ?>
                 </div>
@@ -716,11 +843,11 @@ class EOP_Document_Manager {
                     <thead>
                         <tr>
                             <?php if ( $show_item_index ) : ?>
-                                <th class="eop-pdf-preview__cell eop-pdf-preview__cell--item-index"><?php echo esc_html( $column_labels['item_index'] ); ?></th>
+                                <th class="eop-pdf-preview__cell eop-pdf-preview__cell--item-index" style="<?php echo esc_attr( $table_header_cell_style ); ?>"><?php echo esc_html( $column_labels['item_index'] ); ?></th>
                             <?php endif; ?>
-                            <th><?php echo esc_html( $column_labels['product'] ); ?></th>
+                            <th style="<?php echo esc_attr( $table_header_cell_style ); ?>"><?php echo esc_html( $column_labels['product'] ); ?></th>
                             <?php foreach ( $visible_columns as $column ) : ?>
-                                <th class="eop-pdf-preview__cell eop-pdf-preview__cell--<?php echo esc_attr( $column['key'] ); ?>"><?php echo esc_html( $column['label'] ); ?></th>
+                                <th class="eop-pdf-preview__cell eop-pdf-preview__cell--<?php echo esc_attr( $column['key'] ); ?>" style="<?php echo esc_attr( $table_header_cell_style ); ?>"><?php echo esc_html( $column['label'] ); ?></th>
                             <?php endforeach; ?>
                         </tr>
                     </thead>
@@ -733,34 +860,34 @@ class EOP_Document_Manager {
                             ?>
                             <tr>
                                 <?php if ( $show_item_index ) : ?>
-                                    <td class="eop-pdf-preview__cell eop-pdf-preview__cell--item-index"><?php echo esc_html( $index + 1 ); ?></td>
+                                    <td class="eop-pdf-preview__cell eop-pdf-preview__cell--item-index" style="<?php echo esc_attr( $table_body_cell_style ); ?>"><?php echo esc_html( $index + 1 ); ?></td>
                                 <?php endif; ?>
-                                <td>
+                                <td style="<?php echo esc_attr( $table_body_cell_style ); ?>">
                                     <strong><?php echo esc_html( $item->get_name() ); ?></strong>
                                     <?php if ( $show_sku && $product && $product->get_sku() ) : ?>
-                                        <small><?php printf( esc_html__( 'SKU: %s', EOP_TEXT_DOMAIN ), esc_html( $product->get_sku() ) ); ?></small>
+                                        <small style="<?php echo esc_attr( $table_meta_style ); ?>"><?php printf( esc_html__( 'SKU: %s', EOP_TEXT_DOMAIN ), esc_html( $product->get_sku() ) ); ?></small>
                                     <?php endif; ?>
                                 </td>
                                 <?php foreach ( $visible_columns as $column ) : ?>
-                                    <td class="eop-pdf-preview__cell eop-pdf-preview__cell--<?php echo esc_attr( $column['key'] ); ?>">
+                                    <td class="eop-pdf-preview__cell eop-pdf-preview__cell--<?php echo esc_attr( $column['key'] ); ?>" style="<?php echo esc_attr( $table_body_cell_style ); ?>">
                                         <?php if ( 'quantity' === $column['key'] ) : ?>
                                             <?php echo esc_html( $line_item['quantity'] ); ?>
                                         <?php elseif ( 'unit_price' === $column['key'] ) : ?>
-                                            <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['unit_price'] ) ); ?></span>
+                                            <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['display_unit_price'] ?? $line_item['unit_price'] ) ); ?></span>
                                         <?php elseif ( 'discount' === $column['key'] ) : ?>
                                             <span class="eop-pdf-preview__discount-line">
-                                                <strong><?php echo esc_html( self::format_percentage( $line_item['discount_percent'] ) ); ?></strong>
-                                                <span class="eop-pdf-preview__discount-meta">
-                                                    <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['discount_per_unit'] ) ); ?></span>
+                                                <strong><?php echo esc_html( self::format_percentage( $line_item['display_discount_percent'] ?? $line_item['discount_percent'] ) ); ?></strong>
+                                                <span class="eop-pdf-preview__discount-meta" style="<?php echo esc_attr( $discount_meta_style ); ?>">
+                                                    <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['display_discount_per_unit'] ?? $line_item['discount_per_unit'] ) ); ?></span>
                                                     <?php if ( '' !== $discount_line ) : ?>
                                                         <?php echo ' ' . esc_html( $discount_line ); ?>
                                                     <?php endif; ?>
                                                 </span>
                                             </span>
                                         <?php elseif ( 'discounted_unit_price' === $column['key'] ) : ?>
-                                            <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['discounted_unit_price'] ) ); ?></span>
+                                            <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['display_discounted_unit_price'] ?? $line_item['discounted_unit_price'] ) ); ?></span>
                                         <?php elseif ( 'line_total' === $column['key'] ) : ?>
-                                            <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['line_total'] ) ); ?></span>
+                                            <span class="eop-pdf-preview__money"><?php echo wp_kses_post( wc_price( $line_item['display_line_total'] ?? $line_item['line_total'] ) ); ?></span>
                                         <?php endif; ?>
                                     </td>
                                 <?php endforeach; ?>
@@ -772,7 +899,7 @@ class EOP_Document_Manager {
                 <?php if ( ! empty( $total_rows ) ) : ?>
                     <div class="eop-pdf-preview__totals">
                         <?php foreach ( $total_rows as $row ) : ?>
-                            <div class="<?php echo esc_attr( $row['class'] ); ?>">
+                            <div class="<?php echo esc_attr( $row['class'] ); ?>" style="<?php echo esc_attr( 'is-grand' === $row['class'] ? $grand_total_style : $totals_row_style ); ?>">
                                 <span><?php echo esc_html( $row['label'] ); ?></span>
                                 <strong><?php echo wp_kses_post( $row['value'] ); ?></strong>
                             </div>
@@ -781,14 +908,14 @@ class EOP_Document_Manager {
                 <?php endif; ?>
 
                 <?php if ( $show_notes && $order->get_customer_note() ) : ?>
-                    <div class="eop-pdf-preview__notes">
-                        <span><?php esc_html_e( 'Observacoes', EOP_TEXT_DOMAIN ); ?></span>
-                        <p><?php echo esc_html( $order->get_customer_note() ); ?></p>
+                    <div class="eop-pdf-preview__notes" style="<?php echo esc_attr( $notes_wrapper_style ); ?>">
+                        <span style="<?php echo esc_attr( $notes_label_style ); ?>"><?php esc_html_e( 'Observacoes', EOP_TEXT_DOMAIN ); ?></span>
+                        <p style="<?php echo esc_attr( $notes_body_style ); ?>"><?php echo esc_html( $order->get_customer_note() ); ?></p>
                     </div>
                 <?php endif; ?>
 
                 <?php if ( ! empty( $settings['shop_footer'] ) ) : ?>
-                    <div class="eop-pdf-preview__footer"><?php echo esc_html( $settings['shop_footer'] ); ?></div>
+                    <div class="eop-pdf-preview__footer" style="<?php echo esc_attr( $footer_style ); ?>"><?php echo esc_html( $settings['shop_footer'] ); ?></div>
                 <?php endif; ?>
             </div>
         </div>
@@ -816,7 +943,11 @@ class EOP_Document_Manager {
             wp_die( esc_html__( 'Documento indisponivel para este pedido.', EOP_TEXT_DOMAIN ) );
         }
 
-        $document_type = self::resolve_document_type_for_order( $order, $document_type );
+        $is_preview_document = self::is_preview_document_request();
+
+        if ( ! $is_preview_document ) {
+            $document_type = self::resolve_document_type_for_order( $order, $document_type );
+        }
 
         $is_public = self::is_public_token_request( $order, $document_type, $token );
 
@@ -846,9 +977,11 @@ class EOP_Document_Manager {
             wp_die( esc_html__( 'Documento desativado para este pedido.', EOP_TEXT_DOMAIN ) );
         }
 
-        self::store_document_meta( $order, $document_type, $is_public );
-        self::maybe_mark_document_printed( $order, $document_type, $is_public );
-        self::get_document_number( $order, $document_type, true );
+        if ( ! $is_preview_document ) {
+            self::store_document_meta( $order, $document_type, $is_public );
+            self::maybe_mark_document_printed( $order, $document_type, $is_public );
+            self::get_document_number( $order, $document_type, true );
+        }
 
         $binary = self::maybe_build_browser_pdf_document( $order, $document_type );
 
@@ -860,7 +993,7 @@ class EOP_Document_Manager {
         $disposition = $force_download ? 'attachment' : self::get_content_disposition();
         $discarded_output = self::discard_buffered_output();
 
-        if ( '' !== trim( wp_strip_all_tags( $discarded_output ) ) ) {
+        if ( ! $is_preview_document && '' !== trim( wp_strip_all_tags( $discarded_output ) ) ) {
             self::log_document_event(
                 $order,
                 $document_type,
@@ -872,16 +1005,18 @@ class EOP_Document_Manager {
             );
         }
 
-        self::log_document_event(
-            $order,
-            $document_type,
-            sprintf(
-                /* translators: 1: document type, 2: access mode */
-                __( 'Documento %1$s gerado com acesso %2$s.', EOP_TEXT_DOMAIN ),
+        if ( ! $is_preview_document ) {
+            self::log_document_event(
+                $order,
                 $document_type,
-                $is_public ? 'token' : 'restrito'
-            )
-        );
+                sprintf(
+                    /* translators: 1: document type, 2: access mode */
+                    __( 'Documento %1$s gerado com acesso %2$s.', EOP_TEXT_DOMAIN ),
+                    $document_type,
+                    $is_public ? 'token' : 'restrito'
+                )
+            );
+        }
 
         nocache_headers();
         header( 'Content-Type: application/pdf' );
@@ -998,31 +1133,85 @@ class EOP_Document_Manager {
         $show_notes      = 'yes' === $document_config['show_notes'];
         $billing_label   = self::get_billing_label( $order );
         $test_mode       = ! empty( $settings['test_mode'] ) && 'yes' === (string) $settings['test_mode'];
+		$style_groups    = is_array( $visual_settings['styles'] ?? null ) ? $visual_settings['styles'] : array();
+		$block_styles    = is_array( $visual_settings['blocks'] ?? null ) ? $visual_settings['blocks'] : array();
+		$spacing_settings = is_array( $visual_settings['spacing'] ?? null ) ? $visual_settings['spacing'] : array();
         $title_font_size = self::scale_pdf_font_size( $visual_settings['title_font_size'], 18 );
         $meta_font_size  = self::scale_pdf_font_size( $visual_settings['meta_font_size'], 9 );
         $table_header_font_size = self::scale_pdf_font_size( $visual_settings['table_header_font_size'], 8 );
         $table_body_font_size   = self::scale_pdf_font_size( $visual_settings['table_body_font_size'], 9 );
         $totals_font_size       = self::scale_pdf_font_size( $visual_settings['totals_font_size'], 11 );
         $note_font_size         = self::scale_pdf_font_size( $visual_settings['note_font_size'], 9 );
-        $company_font_size      = max( 12, $meta_font_size + 5 );
+        $company_font_size      = max( 10, $meta_font_size + 2 );
         $summary_label_size     = max( 8, $meta_font_size - 1 );
-        $summary_value_size     = max( 10, $meta_font_size + 1 );
+        $summary_value_size     = max( 10, $meta_font_size + 2 );
         $discount_secondary_size = max( 8, $table_body_font_size - 1 );
         $header_background_color = $visual_settings['header_background_color'];
         $header_text_color       = $visual_settings['header_text_color'];
         $body_text_color         = $visual_settings['body_text_color'];
         $muted_text_color        = $visual_settings['muted_text_color'];
         $border_color            = $visual_settings['border_color'];
+        $brand_name_metrics   = self::get_pdf_profile_metrics( $style_groups['brand_name'] ?? array(), $company_font_size, $body_text_color );
+        $brand_meta_metrics   = self::get_pdf_profile_metrics( $style_groups['brand_meta'] ?? array(), $meta_font_size, $muted_text_color );
+        $title_metrics        = self::get_pdf_profile_metrics( $style_groups['title'] ?? array(), $title_font_size, $body_text_color );
+        $meta_metrics         = self::get_pdf_profile_metrics( $style_groups['meta'] ?? array(), $meta_font_size, $muted_text_color );
+        $summary_label_metrics = self::get_pdf_profile_metrics( $style_groups['summary_label'] ?? array(), $summary_label_size, $muted_text_color );
+        $summary_value_metrics = self::get_pdf_profile_metrics( $style_groups['summary_value'] ?? array(), $summary_value_size, $body_text_color );
+        $table_header_metrics = self::get_pdf_profile_metrics( $style_groups['table_header'] ?? array(), $table_header_font_size, $header_text_color );
+        $table_body_metrics   = self::get_pdf_profile_metrics( $style_groups['table_body'] ?? array(), $table_body_font_size, $body_text_color );
+        $table_meta_metrics   = self::get_pdf_profile_metrics( $style_groups['table_meta'] ?? array(), $discount_secondary_size, $muted_text_color );
+        $totals_metrics       = self::get_pdf_profile_metrics( $style_groups['totals'] ?? array(), $totals_font_size, $body_text_color );
+        $grand_total_metrics  = self::get_pdf_profile_metrics( $style_groups['grand_total'] ?? array(), $totals_font_size + 2, $body_text_color );
+        $notes_label_metrics  = self::get_pdf_profile_metrics( $style_groups['notes_label'] ?? array(), max( 10, $note_font_size + 1 ), $body_text_color );
+        $notes_body_metrics   = self::get_pdf_profile_metrics( $style_groups['notes_body'] ?? array(), $note_font_size, $muted_text_color );
+        $footer_metrics       = self::get_pdf_profile_metrics( $style_groups['footer'] ?? array(), $note_font_size, $muted_text_color );
+        $page_background_color = self::sanitize_pdf_hex_color( $block_styles['page_background_color'] ?? '#ffffff', '#ffffff' );
+        $summary_background_color = self::sanitize_pdf_hex_color( $block_styles['summary_background_color'] ?? '#ffffff', '#ffffff' );
+        $totals_background_color  = self::sanitize_pdf_hex_color( $block_styles['totals_background_color'] ?? '#ffffff', '#ffffff' );
+        $notes_background_color   = self::sanitize_pdf_hex_color( $block_styles['notes_background_color'] ?? '#ffffff', '#ffffff' );
+        $footer_background_color  = self::sanitize_pdf_hex_color( $block_styles['footer_background_color'] ?? '#ffffff', '#ffffff' );
+        $summary_block_padding    = absint( $spacing_settings['summary_block_padding'] ?? 14 );
+        $table_header_padding_y   = absint( $spacing_settings['table_header_cell_padding_y'] ?? 10 );
+        $table_body_padding_y     = absint( $spacing_settings['table_body_cell_padding_y'] ?? 10 );
+        $totals_row_padding_y     = absint( $spacing_settings['totals_row_padding_y'] ?? 8 );
+        $notes_block_padding      = absint( $spacing_settings['notes_block_padding'] ?? 14 );
+        $footer_padding_top       = absint( $spacing_settings['footer_padding_top'] ?? 16 );
+        $brand_name_line_gap      = max( 12, (int) round( $brand_name_metrics['font_size'] * $brand_name_metrics['line_height'] ) );
+        $brand_meta_line_gap      = max( 11, (int) round( $brand_meta_metrics['font_size'] * $brand_meta_metrics['line_height'] ) );
+        $title_line_gap           = max( 18, (int) round( $title_metrics['font_size'] * $title_metrics['line_height'] ) );
+        $meta_line_gap            = max( 12, (int) round( $meta_metrics['font_size'] * $meta_metrics['line_height'] ) );
+        $summary_label_line_gap   = max( 12, (int) round( $summary_label_metrics['font_size'] * $summary_label_metrics['line_height'] ) );
+        $summary_value_line_gap   = max( 14, (int) round( $summary_value_metrics['font_size'] * $summary_value_metrics['line_height'] ) );
+        $table_header_line_gap    = max( 11, (int) round( $table_header_metrics['font_size'] * $table_header_metrics['line_height'] ) );
+        $table_body_line_gap      = max( 12, (int) round( $table_body_metrics['font_size'] * $table_body_metrics['line_height'] ) );
+        $table_meta_line_gap      = max( 11, (int) round( $table_meta_metrics['font_size'] * $table_meta_metrics['line_height'] ) );
+        $totals_line_gap          = max( 14, (int) round( $totals_metrics['font_size'] * $totals_metrics['line_height'] ) );
+        $grand_total_line_gap     = max( 16, (int) round( $grand_total_metrics['font_size'] * $grand_total_metrics['line_height'] ) );
+        $notes_label_line_gap     = max( 14, (int) round( $notes_label_metrics['font_size'] * $notes_label_metrics['line_height'] ) );
+        $notes_body_line_gap      = max( 12, (int) round( $notes_body_metrics['font_size'] * $notes_body_metrics['line_height'] ) );
+        $footer_line_gap          = max( 10, (int) round( $footer_metrics['font_size'] * $footer_metrics['line_height'] ) );
+        $page_width               = isset( $settings['paper_size'] ) && 'letter' === $settings['paper_size'] ? 612 : 595;
+        $page_height              = isset( $settings['paper_size'] ) && 'letter' === $settings['paper_size'] ? 792 : 842;
         $product_wrap_length     = $show_item_index ? 20 : 24;
         $item_index_center       = $page_left + 16;
         $product_x               = $show_item_index ? $page_left + 36 : $page_left + 12;
 
-        $start_page = function () use ( &$pages, &$content, &$y ) {
+        $start_page = function () use ( &$pages, &$content, &$y, $page_background_color, $page_width, $page_height ) {
             if ( '' !== $content ) {
                 $pages[] = $content;
             }
 
             $content = '';
+
+            if ( '#ffffff' !== strtolower( $page_background_color ) ) {
+                $content .= sprintf(
+                    "q %s 0 0 %s %s re f Q\n",
+                    self::get_pdf_fill_color_operator( $page_background_color ),
+                    self::pdf_number( $page_width ),
+                    self::pdf_number( $page_height )
+                );
+            }
+
             $y       = 794;
         };
 
@@ -1032,9 +1221,9 @@ class EOP_Document_Manager {
             }
         };
 
-        $estimate_text_width = function ( $text, $size, $font = 'F1' ) {
+		$estimate_text_width = function ( $text, $size, $font = 'F1' ) {
             $text   = wp_strip_all_tags( (string) $text );
-            $factor = 'F2' === $font ? 0.56 : 0.5;
+			$factor = in_array( $font, array( 'F2', 'F4', 'F6', 'F8', 'F10', 'F12' ), true ) ? 0.56 : 0.5;
 
             return strlen( self::escape_pdf_text( $text ) ) * $size * $factor;
         };
@@ -1122,21 +1311,21 @@ class EOP_Document_Manager {
 
         $ensure_space( 220 );
 
-        $add_text_at( $company_name ?: get_bloginfo( 'name' ), $page_left, $y, 'F2', $company_font_size, 'left', $body_text_color );
+        $add_text_at( $company_name ?: get_bloginfo( 'name' ), $page_left, $y, $brand_name_metrics['font'], $brand_name_metrics['font_size'], 'left', $brand_name_metrics['text_color'] );
 
         foreach ( $company_lines as $line_index => $company_line ) {
-            $add_text_at( $company_line, $page_left, $y - 24 - ( $line_index * 14 ), 'F1', $meta_font_size, 'left', $muted_text_color );
+            $add_text_at( $company_line, $page_left, $y - $brand_name_line_gap - ( $line_index * $brand_meta_line_gap ), $brand_meta_metrics['font'], $brand_meta_metrics['font_size'], 'left', $brand_meta_metrics['text_color'] );
         }
 
-        $add_text_at( $document_heading, $page_right, $y + 2, 'F2', $title_font_size, 'right', $body_text_color );
+        $add_text_at( $document_heading, $page_right, $y + 2, $title_metrics['font'], $title_metrics['font_size'], 'right', $title_metrics['text_color'] );
         if ( $test_mode ) {
-            $add_text_at( __( 'MODO DE TESTE', EOP_TEXT_DOMAIN ), $page_right, $y - 16, 'F2', max( 10, $meta_font_size ), 'right', '#c93535' );
+            $add_text_at( __( 'MODO DE TESTE', EOP_TEXT_DOMAIN ), $page_right, $y - $meta_line_gap, $title_metrics['font'], max( 10, $meta_metrics['font_size'] ), 'right', '#c93535' );
         }
-        $add_text_at( sprintf( __( 'Numero do documento: %s', EOP_TEXT_DOMAIN ), $document_number ), $page_right, $y - 24, 'F1', $meta_font_size, 'right', $muted_text_color );
-        $add_text_at( sprintf( __( 'Data: %s', EOP_TEXT_DOMAIN ), $date ? $date->date_i18n( 'd/m/Y' ) : '—' ), $page_right, $y - 40, 'F1', $meta_font_size, 'right', $muted_text_color );
-        $add_text_at( sprintf( __( 'Pedido WooCommerce: #%s', EOP_TEXT_DOMAIN ), $order->get_id() ), $page_right, $y - 56, 'F1', $meta_font_size, 'right', $muted_text_color );
+        $add_text_at( sprintf( __( 'Numero do documento: %s', EOP_TEXT_DOMAIN ), $document_number ), $page_right, $y - $title_line_gap, $meta_metrics['font'], $meta_metrics['font_size'], 'right', $meta_metrics['text_color'] );
+        $add_text_at( sprintf( __( 'Data: %s', EOP_TEXT_DOMAIN ), $date ? $date->date_i18n( 'd/m/Y' ) : '—' ), $page_right, $y - $title_line_gap - $meta_line_gap, $meta_metrics['font'], $meta_metrics['font_size'], 'right', $meta_metrics['text_color'] );
+        $add_text_at( sprintf( __( 'Pedido WooCommerce: #%s', EOP_TEXT_DOMAIN ), $order->get_id() ), $page_right, $y - $title_line_gap - ( 2 * $meta_line_gap ), $meta_metrics['font'], $meta_metrics['font_size'], 'right', $meta_metrics['text_color'] );
 
-        $y -= max( 98, 70 + ( count( $company_lines ) * 14 ) );
+        $y -= max( 98, max( 70 + ( count( $company_lines ) * $brand_meta_line_gap ), $title_line_gap + ( 3 * $meta_line_gap ) + 24 ) );
 
         if ( ! empty( $summary_blocks ) ) {
             $block_count = count( $summary_blocks );
@@ -1146,42 +1335,46 @@ class EOP_Document_Manager {
 
             foreach ( $summary_blocks as $index => $block ) {
                 $block_x    = $page_left + ( $index * ( $block_width + $block_gap ) );
-                $line_y     = $y;
+                $line_y     = $y - $summary_block_padding;
                 $block_rows = 1 + count( $block['extra'] );
+				$block_height = ( 2 * $summary_block_padding ) + $summary_label_line_gap + $summary_value_line_gap + ( count( $block['extra'] ) * $meta_line_gap );
 
-                $add_text_at( strtoupper( $block['label'] ), $block_x, $line_y, 'F2', $summary_label_size, 'left', $muted_text_color );
-                $line_y -= 22;
-                $add_text_at( $block['value'], $block_x, $line_y, 'F2', $summary_value_size, 'left', $body_text_color );
-                $line_y -= 16;
+				$fill_rect( $block_x, $y + $summary_block_padding + $summary_label_metrics['font_size'], $block_width, $block_height, $summary_background_color );
+
+                $add_text_at( strtoupper( $block['label'] ), $block_x + $summary_block_padding, $line_y, $summary_label_metrics['font'], $summary_label_metrics['font_size'], 'left', $summary_label_metrics['text_color'] );
+                $line_y -= $summary_label_line_gap;
+                $add_text_at( $block['value'], $block_x + $summary_block_padding, $line_y, $summary_value_metrics['font'], $summary_value_metrics['font_size'], 'left', $summary_value_metrics['text_color'] );
+                $line_y -= $summary_value_line_gap;
 
                 foreach ( $block['extra'] as $extra_line ) {
-                    $add_text_at( $extra_line, $block_x, $line_y, 'F1', $meta_font_size, 'left', $muted_text_color );
-                    $line_y -= 14;
+                    $add_text_at( $extra_line, $block_x + $summary_block_padding, $line_y, $meta_metrics['font'], $meta_metrics['font_size'], 'left', $meta_metrics['text_color'] );
+                    $line_y -= $meta_line_gap;
                 }
 
-                $max_height = max( $max_height, 22 + ( $block_rows * 16 ) );
+				$max_height = max( $max_height, $block_height );
             }
 
             $y -= $max_height + 12;
         }
 
         $header_top    = $y;
-        $header_height = max( 40, 32 + ( $table_header_font_size * 1.4 ) );
+        $header_height = max( 40, ( 2 * $table_header_padding_y ) + ( $table_header_line_gap * 2 ) );
         $fill_rect( $page_left, $header_top, $content_width, $header_height, $header_background_color );
+		$header_baseline = $header_top - $table_header_padding_y - $table_header_metrics['font_size'];
 
         if ( $show_item_index ) {
-            $add_text_at( $column_labels['item_index'], $item_index_center, $header_top - 21, 'F2', $table_header_font_size, 'center', $header_text_color );
+            $add_text_at( $column_labels['item_index'], $item_index_center, $header_baseline, $table_header_metrics['font'], $table_header_metrics['font_size'], 'center', $table_header_metrics['text_color'] );
         }
 
-        $add_text_at( $column_labels['product'], $product_x, $header_top - 21, 'F2', $table_header_font_size, 'left', $header_text_color );
+        $add_text_at( $column_labels['product'], $product_x, $header_baseline, $table_header_metrics['font'], $table_header_metrics['font_size'], 'left', $table_header_metrics['text_color'] );
 
-        $render_column_header = function ( $label, $x ) use ( $header_top, $add_text_at, $table_header_font_size, $header_text_color ) {
+        $render_column_header = function ( $label, $x ) use ( $header_baseline, $add_text_at, $table_header_metrics, $table_header_line_gap ) {
             $lines      = array_slice( self::wrap_text( $label, 16 ), 0, 3 );
             $line_count = count( $lines );
-            $start_gap  = 21 - max( 0, $line_count - 1 ) * 5;
+            $start_y    = $header_baseline + ( ( $line_count - 1 ) * ( $table_header_line_gap / 2 ) );
 
             foreach ( $lines as $line_index => $line ) {
-                $add_text_at( $line, $x, $header_top - $start_gap - ( $line_index * 11 ), 'F2', max( 7, $table_header_font_size - 1 ), 'center', $header_text_color );
+                $add_text_at( $line, $x, $start_y - ( $line_index * $table_header_line_gap ), $table_header_metrics['font'], max( 7, $table_header_metrics['font_size'] - 1 ), 'center', $table_header_metrics['text_color'] );
             }
         };
 
@@ -1204,13 +1397,13 @@ class EOP_Document_Manager {
             }
 
             if ( isset( $column_lookup['unit_price'] ) ) {
-                $meta_lines['unit_price'] = self::format_money( $line_item['unit_price'] );
+                $meta_lines['unit_price'] = self::format_money( $line_item['display_unit_price'] ?? $line_item['unit_price'] );
             }
 
             if ( isset( $column_lookup['discount'] ) ) {
                 $discount_parts = array(
-                    self::format_percentage( $line_item['discount_percent'] ),
-                    trim( self::format_money( $line_item['discount_per_unit'] ) . ( '' !== $discount_suffix ? ' ' . $discount_suffix : '' ) ),
+                    self::format_percentage( $line_item['display_discount_percent'] ?? $line_item['discount_percent'] ),
+                    trim( self::format_money( $line_item['display_discount_per_unit'] ?? $line_item['discount_per_unit'] ) . ( '' !== $discount_suffix ? ' ' . $discount_suffix : '' ) ),
                 );
                 $discount_parts = array_values( array_filter( $discount_parts, 'strlen' ) );
 
@@ -1220,32 +1413,32 @@ class EOP_Document_Manager {
             }
 
             if ( isset( $column_lookup['discounted_unit_price'] ) ) {
-                $meta_lines['discounted_unit_price'] = self::format_money( $line_item['discounted_unit_price'] );
+                $meta_lines['discounted_unit_price'] = self::format_money( $line_item['display_discounted_unit_price'] ?? $line_item['discounted_unit_price'] );
             }
 
             if ( isset( $column_lookup['line_total'] ) ) {
-                $meta_lines['line_total'] = self::format_money( $line_item['line_total'] );
+                $meta_lines['line_total'] = self::format_money( $line_item['display_line_total'] ?? $line_item['line_total'] );
             }
 
             $sku_line   = $show_sku && $product && $product->get_sku() ? sprintf( __( 'SKU: %s', EOP_TEXT_DOMAIN ), $product->get_sku() ) : '';
-            $row_height = max( 26, count( $name_lines ) * ( $table_body_font_size + 3 ) + ( '' !== $sku_line ? $meta_font_size + 4 : 0 ), $table_body_font_size ) + 14;
+            $row_height = max( 26, count( $name_lines ) * $table_body_line_gap + ( '' !== $sku_line ? $table_meta_line_gap : 0 ), $table_body_metrics['font_size'] ) + ( 2 * $table_body_padding_y );
 
             $ensure_space( $row_height + 18 );
 
             $row_top = $y;
-            $line_y  = $row_top;
+            $line_y  = $row_top - $table_body_padding_y;
 
             foreach ( $name_lines as $name_line ) {
-                $add_text_at( $name_line, $product_x, $line_y, 'F2', $table_body_font_size, 'left', $body_text_color );
-                $line_y -= $table_body_font_size + 3;
+                $add_text_at( $name_line, $product_x, $line_y, $table_body_metrics['font'], $table_body_metrics['font_size'], 'left', $table_body_metrics['text_color'] );
+                $line_y -= $table_body_line_gap;
             }
 
             if ( '' !== $sku_line ) {
-                $add_text_at( $sku_line, $product_x, $line_y - 1, 'F1', $meta_font_size, 'left', $muted_text_color );
+                $add_text_at( $sku_line, $product_x, $line_y - 1, $table_meta_metrics['font'], $table_meta_metrics['font_size'], 'left', $table_meta_metrics['text_color'] );
             }
 
             if ( $show_item_index ) {
-                $add_text_at( (string) ( $index + 1 ), $item_index_center, $row_top, 'F2', $table_body_font_size, 'center', $body_text_color );
+                $add_text_at( (string) ( $index + 1 ), $item_index_center, $row_top - $table_body_padding_y, $table_body_metrics['font'], $table_body_metrics['font_size'], 'center', $table_body_metrics['text_color'] );
             }
 
             foreach ( $columns as $column ) {
@@ -1257,28 +1450,28 @@ class EOP_Document_Manager {
                 }
 
                 if ( 'quantity' === $column_key && isset( $meta_lines['quantity'] ) ) {
-                    $add_text_at( $meta_lines['quantity'], $column_x, $row_top, 'F1', $table_body_font_size, 'center', $body_text_color );
+                    $add_text_at( $meta_lines['quantity'], $column_x, $row_top - $table_body_padding_y, $table_body_metrics['font'], $table_body_metrics['font_size'], 'center', $table_body_metrics['text_color'] );
                     continue;
                 }
 
                 if ( 'unit_price' === $column_key && isset( $meta_lines['unit_price'] ) ) {
-                    $add_text_at( $meta_lines['unit_price'], $column_x, $row_top, 'F1', $table_body_font_size, 'center', $body_text_color );
+                    $add_text_at( $meta_lines['unit_price'], $column_x, $row_top - $table_body_padding_y, $table_body_metrics['font'], $table_body_metrics['font_size'], 'center', $table_body_metrics['text_color'] );
                     continue;
                 }
 
                 if ( 'discount' === $column_key && isset( $meta_lines['discount'] ) ) {
-                    $add_text_at( $meta_lines['discount'], $column_x, $row_top, 'F2', $discount_secondary_size, 'center', $body_text_color );
+                    $add_text_at( $meta_lines['discount'], $column_x, $row_top - $table_body_padding_y, $table_meta_metrics['font'], $table_meta_metrics['font_size'], 'center', $table_meta_metrics['text_color'] );
 
                     continue;
                 }
 
                 if ( 'discounted_unit_price' === $column_key && isset( $meta_lines['discounted_unit_price'] ) ) {
-                    $add_text_at( $meta_lines['discounted_unit_price'], $column_x, $row_top, 'F1', $table_body_font_size, 'center', $body_text_color );
+                    $add_text_at( $meta_lines['discounted_unit_price'], $column_x, $row_top - $table_body_padding_y, $table_body_metrics['font'], $table_body_metrics['font_size'], 'center', $table_body_metrics['text_color'] );
                     continue;
                 }
 
                 if ( 'line_total' === $column_key && isset( $meta_lines['line_total'] ) ) {
-                    $add_text_at( $meta_lines['line_total'], $column_x, $row_top, 'F2', $table_body_font_size, 'center', $body_text_color );
+                    $add_text_at( $meta_lines['line_total'], $column_x, $row_top - $table_body_padding_y, $table_body_metrics['font'], $table_body_metrics['font_size'], 'center', $table_body_metrics['text_color'] );
                 }
             }
 
@@ -1289,6 +1482,13 @@ class EOP_Document_Manager {
 
         if ( ! empty( $total_rows ) ) {
             $totals_y = $y - 2;
+			$totals_height = 0;
+
+			foreach ( $total_rows as $row ) {
+				$totals_height += ( 'is-grand' === $row['class'] ? $grand_total_line_gap : $totals_line_gap ) + ( 2 * $totals_row_padding_y );
+			}
+
+			$fill_rect( 350, $totals_y + $totals_row_padding_y + $totals_metrics['font_size'], $page_right - 350, $totals_height, $totals_background_color );
 
             foreach ( $total_rows as $row ) {
                 $is_grand = 'is-grand' === $row['class'];
@@ -1297,9 +1497,12 @@ class EOP_Document_Manager {
                     $draw_line( 360, $page_right, $totals_y + 10, $body_text_color, 1.2 );
                 }
 
-                $add_text_at( $is_grand ? __( 'Total', EOP_TEXT_DOMAIN ) : $row['label'], 360, $totals_y, $is_grand ? 'F2' : 'F1', $is_grand ? $totals_font_size + 3 : $totals_font_size, 'left', $body_text_color );
-                $add_text_at( self::format_money( $row['raw'] ?? 0 ), $page_right, $totals_y, 'F2', $is_grand ? $totals_font_size + 3 : $totals_font_size, 'right', $body_text_color );
-                $totals_y -= $is_grand ? ( $totals_font_size + 9 ) : ( $totals_font_size + 5 );
+                $metrics = $is_grand ? $grand_total_metrics : $totals_metrics;
+				$line_gap = $is_grand ? $grand_total_line_gap : $totals_line_gap;
+
+				$add_text_at( $is_grand ? __( 'Total', EOP_TEXT_DOMAIN ) : $row['label'], 360, $totals_y, $metrics['font'], $metrics['font_size'], 'left', $metrics['text_color'] );
+				$add_text_at( self::format_money( $row['raw'] ?? 0 ), $page_right, $totals_y, $metrics['font'], $metrics['font_size'], 'right', $metrics['text_color'] );
+				$totals_y -= $line_gap + ( 2 * $totals_row_padding_y );
             }
 
             $y = $totals_y - 8;
@@ -1307,27 +1510,35 @@ class EOP_Document_Manager {
 
         if ( 'proposal' === $document_type ) {
             $confirmation = 'yes' === $order->get_meta( '_eop_proposal_confirmed', true ) ? __( 'Situacao da proposta: Confirmada', EOP_TEXT_DOMAIN ) : __( 'Situacao da proposta: Aguardando confirmacao', EOP_TEXT_DOMAIN );
-            $add_text_at( $confirmation, $page_left, $y, 'F1', $note_font_size, 'left', $muted_text_color );
-            $y -= 18;
+            $add_text_at( $confirmation, $page_left, $y, $notes_body_metrics['font'], $notes_body_metrics['font_size'], 'left', $notes_body_metrics['text_color'] );
+            $y -= $notes_body_line_gap;
         }
 
         $notes = trim( (string) $order->get_customer_note() );
         if ( $show_notes && '' !== $notes ) {
-            $add_text_at( __( 'Observacoes', EOP_TEXT_DOMAIN ), $page_left, $y, 'F2', max( 10, $note_font_size + 1 ), 'left', $body_text_color );
-            $y -= 16;
-
-            foreach ( self::wrap_text( $notes, 78 ) as $note_line ) {
-                $add_text_at( $note_line, $page_left, $y, 'F1', $note_font_size, 'left', $muted_text_color );
-                $y -= 14;
+			$note_lines    = self::wrap_text( $notes, 78 );
+			$notes_height  = ( 2 * $notes_block_padding ) + $notes_label_line_gap + ( count( $note_lines ) * $notes_body_line_gap );
+			$fill_rect( $page_left, $y + $notes_block_padding + $notes_label_metrics['font_size'], $content_width, $notes_height, $notes_background_color );
+			$add_text_at( __( 'Observacoes', EOP_TEXT_DOMAIN ), $page_left + $notes_block_padding, $y - $notes_block_padding, $notes_label_metrics['font'], $notes_label_metrics['font_size'], 'left', $notes_label_metrics['text_color'] );
+			$y -= $notes_block_padding + $notes_label_line_gap;
+
+            foreach ( $note_lines as $note_line ) {
+                $add_text_at( $note_line, $page_left + $notes_block_padding, $y, $notes_body_metrics['font'], $notes_body_metrics['font_size'], 'left', $notes_body_metrics['text_color'] );
+                $y -= $notes_body_line_gap;
             }
+
+			$y -= $notes_block_padding;
         }
 
         if ( '' !== $footer_note ) {
-            $y -= 4;
-
-            foreach ( self::wrap_text( $footer_note, 82 ) as $footer_line ) {
-                $add_text_at( $footer_line, $page_left, $y, 'F1', $note_font_size, 'left', $muted_text_color );
-                $y -= 12;
+			$footer_lines   = self::wrap_text( $footer_note, 82 );
+			$footer_height  = $footer_padding_top + ( count( $footer_lines ) * $footer_line_gap ) + 8;
+			$fill_rect( $page_left, $y + $footer_padding_top + $footer_metrics['font_size'], $content_width, $footer_height, $footer_background_color );
+			$y -= $footer_padding_top;
+
+            foreach ( $footer_lines as $footer_line ) {
+                $add_text_at( $footer_line, $page_left, $y, $footer_metrics['font'], $footer_metrics['font_size'], 'left', $footer_metrics['text_color'] );
+                $y -= $footer_line_gap;
             }
         }
 
@@ -1487,27 +1698,33 @@ class EOP_Document_Manager {
 
     private static function get_preview_print_document_html( WC_Order $order, $document_type ) {
         $settings   = self::get_pdf_settings();
-        $font_url   = method_exists( 'EOP_Settings', 'get_font_stylesheet_url' ) ? EOP_Settings::get_font_stylesheet_url( $settings['font_family'] ?? '' ) : '';
-        $font_css   = method_exists( 'EOP_Settings', 'get_font_css_family' ) ? EOP_Settings::get_font_css_family( $settings['font_family'] ?? '' ) : "'Segoe UI', sans-serif";
+        $font_css   = method_exists( 'EOP_Settings', 'get_font_css_family' ) ? EOP_Settings::get_font_css_family() : "'Segoe UI', sans-serif";
         $paper_size = isset( $settings['paper_size'] ) && 'letter' === $settings['paper_size'] ? 'Letter' : 'A4';
         $css_path   = EOP_PLUGIN_DIR . 'assets/css/pdf-admin.css';
         $css        = file_exists( $css_path ) ? (string) file_get_contents( $css_path ) : '';
         $preview    = self::get_preview_html( $order, $document_type, true );
+        $font_urls  = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_font_stylesheet_urls( $document_type ) : array();
+        $font_links = '';
 
         if ( '' === $preview ) {
             return '';
         }
 
+        foreach ( $font_urls as $font_url ) {
+            $font_links .= '<link rel="stylesheet" href="' . esc_url( $font_url ) . '">';
+        }
+
         $css .= "\n@page { size: {$paper_size}; margin: 12mm; }\n";
         $css .= "html, body { margin: 0; padding: 0; background: #ffffff; font-family: {$font_css}; }\n";
         $css .= "body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }\n";
         $css .= ".eop-pdf-preview { padding: 0 !important; }\n";
+        $css .= ".eop-pdf-preview__table thead, .eop-pdf-preview__table thead tr, .eop-pdf-preview__table thead th { background-color: var(--eop-doc-header-bg) !important; color: var(--eop-doc-header-text) !important; -webkit-print-color-adjust: exact; print-color-adjust: exact; color-adjust: exact; }\n";
         $css .= ".eop-pdf-preview__sheet { max-width: none !important; width: auto !important; margin: 0 auto !important; box-shadow: none !important; }\n";
         $css .= ".eop-pdf-preview__sheet--a4 { max-width: none !important; }\n";
         $css .= ".eop-pdf-preview__sheet--letter { max-width: none !important; }\n";
 
         return '<!doctype html><html><head><meta charset="utf-8">'
-            . ( $font_url ? '<link rel="stylesheet" href="' . esc_url( $font_url ) . '">' : '' )
+            . $font_links
             . '<style>' . $css . '</style></head><body>' . $preview . '</body></html>';
     }
 
@@ -1648,28 +1865,108 @@ class EOP_Document_Manager {
             $remaining_fee_discount = max( 0, round( $remaining_fee_discount - $allocated_fee_amount, $decimals ) );
             $remaining_line_total   = max( 0, round( $remaining_line_total - $line_total, $decimals ) );
 
-            $discount_total     = round( $item_discount_total + $allocated_fee_amount, $decimals );
-            $effective_total    = max( 0, round( $line_subtotal - $discount_total, $decimals ) );
+            $raw_discount_total = max( 0, $item_discount_total + $allocated_fee_amount );
+            $raw_line_total     = max( 0, $line_total - $allocated_fee_amount );
             $unit_price         = $quantity > 0 ? $line_subtotal / $quantity : $line_subtotal;
-            $discount_per_unit  = $quantity > 0 ? $discount_total / $quantity : $discount_total;
-            $discounted_unit_price = $quantity > 0 ? $effective_total / $quantity : $effective_total;
-            $discount_percent   = $line_subtotal > 0 ? ( $discount_total / $line_subtotal ) * 100 : 0;
 
             $rows[] = array(
                 'item'              => $item,
                 'product'           => $item->get_product(),
                 'quantity'          => $quantity,
                 'unit_price'        => round( $unit_price, $decimals ),
-                'discount_percent'  => round( $discount_percent, 2 ),
-                'discount_per_unit' => round( $discount_per_unit, $decimals ),
-                'discounted_unit_price' => round( $discounted_unit_price, $decimals ),
-                'discount_total'    => $discount_total,
-                'line_total'        => $effective_total,
+                'discount_percent'  => round( $line_subtotal > 0 ? ( $raw_discount_total / $line_subtotal ) * 100 : 0, 2 ),
+                'discount_per_unit' => round( $quantity > 0 ? $raw_discount_total / $quantity : $raw_discount_total, $decimals ),
+                'discounted_unit_price' => round( $quantity > 0 ? $raw_line_total / $quantity : $raw_line_total, $decimals ),
+                'discount_total'    => round( $raw_discount_total, $decimals ),
+                'line_total'        => $raw_line_total,
+                'line_subtotal'     => $line_subtotal,
             );
 
             $line_index++;
         }
 
+        return self::reconcile_line_item_display_values( $rows, $decimals );
+    }
+
+    private static function reconcile_total_row_display_values( $rows ) {
+        $decimals         = wc_get_price_decimals();
+        $display_balance  = 0.0;
+        $grand_total      = null;
+        $adjustable_index = null;
+        $adjustable_keys  = array( 'discount', 'tax', 'shipping', 'subtotal' );
+
+        foreach ( $rows as $index => $row ) {
+            $display_raw            = round( (float) ( $row['raw'] ?? 0 ), $decimals );
+            $rows[ $index ]['raw']  = $display_raw;
+            $rows[ $index ]['value'] = wc_price( $display_raw );
+
+            if ( 'is-grand' === ( $row['class'] ?? '' ) || 'total' === ( $row['key'] ?? '' ) ) {
+                $grand_total = $display_raw;
+                continue;
+            }
+
+            $display_balance += $display_raw;
+
+            if ( in_array( $row['key'] ?? '', $adjustable_keys, true ) ) {
+                $adjustable_index = $index;
+            }
+        }
+
+        if ( null === $grand_total || null === $adjustable_index ) {
+            return $rows;
+        }
+
+        $difference = round( $grand_total - round( $display_balance, $decimals ), $decimals );
+
+        if ( abs( $difference ) < 0.01 ) {
+            return $rows;
+        }
+
+        $rows[ $adjustable_index ]['raw']   = round( (float) $rows[ $adjustable_index ]['raw'] + $difference, $decimals );
+        $rows[ $adjustable_index ]['value'] = wc_price( (float) $rows[ $adjustable_index ]['raw'] );
+
+        return $rows;
+    }
+
+    private static function reconcile_line_item_display_values( $rows, $decimals ) {
+        $raw_total_sum     = 0.0;
+        $display_total_sum = 0.0;
+        $last_row_index    = null;
+
+        foreach ( $rows as $index => $row ) {
+            $display_line_total = round( (float) ( $row['line_total'] ?? 0 ), $decimals );
+
+            $rows[ $index ]['display_line_total'] = $display_line_total;
+            $raw_total_sum                       += (float) ( $row['line_total'] ?? 0 );
+            $display_total_sum                   += $display_line_total;
+            $last_row_index                       = $index;
+        }
+
+        if ( null !== $last_row_index ) {
+            $difference = round( round( $raw_total_sum, $decimals ) - round( $display_total_sum, $decimals ), $decimals );
+
+            if ( abs( $difference ) >= 0.01 ) {
+                $rows[ $last_row_index ]['display_line_total'] = max( 0, round( (float) $rows[ $last_row_index ]['display_line_total'] + $difference, $decimals ) );
+            }
+        }
+
+        foreach ( $rows as $index => $row ) {
+            $quantity                 = max( 1, (int) ( $row['quantity'] ?? 1 ) );
+            $line_subtotal            = round( (float) ( $row['line_subtotal'] ?? 0 ), $decimals );
+            $display_line_total       = round( (float) ( $row['display_line_total'] ?? 0 ), $decimals );
+            $display_discount_total   = max( 0, round( $line_subtotal - $display_line_total, $decimals ) );
+            $display_discount_per_unit = round( $display_discount_total / $quantity, $decimals );
+            $display_unit_price       = round( $line_subtotal / $quantity, $decimals );
+            $display_discounted_price = round( $display_line_total / $quantity, $decimals );
+            $display_discount_percent = $line_subtotal > 0 ? round( ( $display_discount_total / $line_subtotal ) * 100, 2 ) : 0;
+
+            $rows[ $index ]['display_unit_price']            = $display_unit_price;
+            $rows[ $index ]['display_discount_total']        = $display_discount_total;
+            $rows[ $index ]['display_discount_per_unit']     = $display_discount_per_unit;
+            $rows[ $index ]['display_discounted_unit_price'] = $display_discounted_price;
+            $rows[ $index ]['display_discount_percent']      = $display_discount_percent;
+        }
+
         return $rows;
     }
 
@@ -1679,17 +1976,27 @@ class EOP_Document_Manager {
             2 => '',
             3 => '<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica /Encoding /WinAnsiEncoding >>',
             4 => '<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Bold /Encoding /WinAnsiEncoding >>',
+            5 => '<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Oblique /Encoding /WinAnsiEncoding >>',
+            6 => '<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-BoldOblique /Encoding /WinAnsiEncoding >>',
+            7 => '<< /Type /Font /Subtype /Type1 /BaseFont /Times-Roman /Encoding /WinAnsiEncoding >>',
+            8 => '<< /Type /Font /Subtype /Type1 /BaseFont /Times-Bold /Encoding /WinAnsiEncoding >>',
+            9 => '<< /Type /Font /Subtype /Type1 /BaseFont /Times-Italic /Encoding /WinAnsiEncoding >>',
+            10 => '<< /Type /Font /Subtype /Type1 /BaseFont /Times-BoldItalic /Encoding /WinAnsiEncoding >>',
+            11 => '<< /Type /Font /Subtype /Type1 /BaseFont /Courier /Encoding /WinAnsiEncoding >>',
+            12 => '<< /Type /Font /Subtype /Type1 /BaseFont /Courier-Bold /Encoding /WinAnsiEncoding >>',
+            13 => '<< /Type /Font /Subtype /Type1 /BaseFont /Courier-Oblique /Encoding /WinAnsiEncoding >>',
+            14 => '<< /Type /Font /Subtype /Type1 /BaseFont /Courier-BoldOblique /Encoding /WinAnsiEncoding >>',
         );
 
         $kids          = array();
-        $object_number = 5;
+		$object_number = 15;
 
         foreach ( $pages as $page_content ) {
             $page_object    = $object_number++;
             $content_object = $object_number++;
             $kids[]         = $page_object . ' 0 R';
 
-            $objects[ $page_object ] = '<< /Type /Page /Parent 2 0 R /MediaBox [' . self::get_media_box() . '] /Resources << /Font << /F1 3 0 R /F2 4 0 R >> >> /Contents ' . $content_object . ' 0 R >>';
+			$objects[ $page_object ] = '<< /Type /Page /Parent 2 0 R /MediaBox [' . self::get_media_box() . '] /Resources << /Font << /F1 3 0 R /F2 4 0 R /F3 5 0 R /F4 6 0 R /F5 7 0 R /F6 8 0 R /F7 9 0 R /F8 10 0 R /F9 11 0 R /F10 12 0 R /F11 13 0 R /F12 14 0 R >> >> /Contents ' . $content_object . ' 0 R >>';
             $objects[ $content_object ] = '<< /Length ' . strlen( $page_content ) . " >>\nstream\n" . $page_content . "\nendstream";
         }
 
@@ -1735,6 +2042,78 @@ class EOP_Document_Manager {
         return explode( "\n", $wrapped );
     }
 
+    private static function build_preview_inline_style( $rules ) {
+        $rules = is_array( $rules ) ? array_filter( $rules, 'strlen' ) : array();
+
+        return implode( '; ', $rules );
+    }
+
+    private static function get_preview_style_from_profile( $profile, $extra_rules = array() ) {
+        $profile = is_array( $profile ) ? $profile : array();
+        $font_css = method_exists( 'EOP_Settings', 'get_font_css_family' ) ? EOP_Settings::get_font_css_family( $profile['font_family'] ?? '' ) : "'Segoe UI', sans-serif";
+
+        $rules = array(
+            'font-family: ' . $font_css,
+            'font-size: ' . absint( $profile['font_size'] ?? 12 ) . 'px',
+            'line-height: ' . esc_attr( (string) ( $profile['line_height'] ?? '1.2' ) ),
+            'font-style: ' . esc_attr( (string) ( $profile['font_style'] ?? 'normal' ) ),
+            'font-weight: ' . esc_attr( (string) ( $profile['font_weight'] ?? '400' ) ),
+            'color: ' . self::sanitize_pdf_hex_color( $profile['text_color'] ?? '#172033' ),
+        );
+
+        return self::build_preview_inline_style( array_merge( $rules, is_array( $extra_rules ) ? $extra_rules : array() ) );
+    }
+
+    private static function get_pdf_profile_metrics( $profile, $fallback_size = 11, $fallback_color = '#172033' ) {
+        $profile = is_array( $profile ) ? $profile : array();
+
+        return array(
+            'font'        => self::get_pdf_font_alias_for_profile( $profile ),
+            'font_size'   => self::scale_pdf_font_size( $profile['font_size'] ?? $fallback_size, $fallback_size ),
+            'line_height' => max( 1, (float) ( $profile['line_height'] ?? '1.2' ) ),
+            'text_color'  => self::sanitize_pdf_hex_color( $profile['text_color'] ?? $fallback_color, $fallback_color ),
+        );
+    }
+
+    private static function get_pdf_font_alias_for_profile( $profile ) {
+        $profile    = is_array( $profile ) ? $profile : array();
+        $font_value = strtolower( (string) ( $profile['font_family'] ?? '' ) );
+        $is_bold    = absint( $profile['font_weight'] ?? 400 ) >= 600;
+        $is_italic  = 'italic' === (string) ( $profile['font_style'] ?? 'normal' );
+        $variant    = $is_bold && $is_italic ? 'bolditalic' : ( $is_bold ? 'bold' : ( $is_italic ? 'italic' : 'normal' ) );
+
+        if ( false !== strpos( $font_value, 'times' ) || false !== strpos( $font_value, 'serif' ) || false !== strpos( $font_value, 'merriweather' ) || false !== strpos( $font_value, 'playfair' ) ) {
+            $family = 'times';
+        } elseif ( false !== strpos( $font_value, 'courier' ) || false !== strpos( $font_value, 'mono' ) || false !== strpos( $font_value, 'code' ) ) {
+            $family = 'courier';
+        } else {
+            $family = 'helvetica';
+        }
+
+        $aliases = array(
+            'helvetica' => array(
+                'normal'     => 'F1',
+                'bold'       => 'F2',
+                'italic'     => 'F3',
+                'bolditalic' => 'F4',
+            ),
+            'times' => array(
+                'normal'     => 'F5',
+                'bold'       => 'F6',
+                'italic'     => 'F7',
+                'bolditalic' => 'F8',
+            ),
+            'courier' => array(
+                'normal'     => 'F9',
+                'bold'       => 'F10',
+                'italic'     => 'F11',
+                'bolditalic' => 'F12',
+            ),
+        );
+
+        return $aliases[ $family ][ $variant ] ?? 'F1';
+    }
+
     private static function format_money( $value ) {
         $formatted = wc_price( (float) $value );
         $formatted = html_entity_decode( wp_strip_all_tags( (string) $formatted ), ENT_QUOTES, 'UTF-8' );
@@ -1761,9 +2140,7 @@ class EOP_Document_Manager {
     }
 
     private static function scale_pdf_font_size( $value, $minimum = 8 ) {
-        $size = self::sanitize_pdf_font_size( $value, $minimum );
-
-        return max( absint( $minimum ), (int) round( $size * 0.78 ) );
+        return self::sanitize_pdf_font_size( $value, $minimum );
     }
 
     private static function sanitize_pdf_hex_color( $value, $default = '#111111' ) {
@@ -1855,6 +2232,12 @@ class EOP_Document_Manager {
         return 'private_nonce' === self::get_access_policy();
     }
 
+    private static function is_preview_document_request() {
+        return current_user_can( 'edit_shop_orders' )
+            && isset( $_REQUEST['preview_document'] )
+            && '1' === sanitize_text_field( wp_unslash( $_REQUEST['preview_document'] ) );
+    }
+
     private static function get_private_download_args( WC_Order $order, $document_type, $force_download ) {
         $args = array(
             'order_id' => $order->get_id(),
@@ -1950,7 +2333,7 @@ class EOP_Document_Manager {
             error_log( $prefix . wp_strip_all_tags( (string) $message ) );
         }
 
-        if ( self::should_log_order_notes() ) {
+        if ( self::should_log_order_notes() && ! self::is_preview_document_request() ) {
             $order->add_order_note( wp_strip_all_tags( (string) $message ), false, true );
         }
     }
diff --git a/includes/class-eop-license-manager.php b/includes/class-eop-license-manager.php
index beb65fc..01e9e02 100644
--- a/includes/class-eop-license-manager.php
+++ b/includes/class-eop-license-manager.php
@@ -248,9 +248,9 @@ class EOP_License_Manager {
 						</span>
 					</div></li>
 				</ul>
-				<div class="el-license-active-btn">
+				<div class="el-license-active-btn eop-admin-submitbar">
 					<?php wp_nonce_field( 'el-license' ); ?>
-					<?php submit_button( __( 'Desativar Licenca', 'aireset-expresso-order' ) ); ?>
+					<?php submit_button( __( 'Desativar Licenca', 'aireset-expresso-order' ), 'primary', 'submit', false ); ?>
 				</div>
 			</div>
 		</form>
@@ -292,9 +292,9 @@ class EOP_License_Manager {
 						<label for="el_license_email"><?php esc_html_e( 'E-mail', 'aireset-expresso-order' ); ?></label>
 						<input type="email" class="regular-text" name="el_license_email" id="el_license_email" value="<?php echo esc_attr( get_option( self::OPT_PREFIX . '_lic_email', get_bloginfo( 'admin_email' ) ) ); ?>" placeholder="<?php esc_attr_e( 'seu@email.com', 'aireset-expresso-order' ); ?>">
 					</div>
-					<div class="el-license-active-btn">
+					<div class="el-license-active-btn eop-admin-submitbar">
 						<?php wp_nonce_field( 'el-license' ); ?>
-						<?php submit_button( __( 'Ativar Licenca', 'aireset-expresso-order' ) ); ?>
+						<?php submit_button( __( 'Ativar Licenca', 'aireset-expresso-order' ), 'primary', 'submit', false ); ?>
 					</div>
 				</div>
 			</div>
diff --git a/includes/class-orders-page.php b/includes/class-orders-page.php
index c3c54cb..fc30579 100644
--- a/includes/class-orders-page.php
+++ b/includes/class-orders-page.php
@@ -38,8 +38,10 @@ class EOP_Orders_Page {
             wp_enqueue_style( 'eop-orders-selected-font', $font_url, array(), null );
         }
 
-        wp_enqueue_style( 'select2', WC()->plugin_url() . '/assets/css/select2.css', array(), WC_VERSION );
-        wp_enqueue_script( 'select2', WC()->plugin_url() . '/assets/js/select2/select2.full.min.js', array( 'jquery' ), WC_VERSION, true );
+        $wc_version = defined( 'WC_VERSION' ) ? WC_VERSION : EOP_VERSION;
+
+        wp_enqueue_style( 'select2', WC()->plugin_url() . '/assets/css/select2.css', array(), $wc_version );
+        wp_enqueue_script( 'select2', WC()->plugin_url() . '/assets/js/select2/select2.full.min.js', array( 'jquery' ), $wc_version, true );
 
         wp_enqueue_style( 'eop-admin', EOP_PLUGIN_URL . 'assets/css/admin.css', array( 'select2' ), EOP_VERSION );
         wp_enqueue_style( 'eop-orders', EOP_PLUGIN_URL . 'assets/css/orders.css', array( 'eop-admin' ), EOP_VERSION );
@@ -84,6 +86,21 @@ class EOP_Orders_Page {
                 'shipping_postcode_invalid'    => __( 'Digite um CEP valido com 8 numeros.', EOP_TEXT_DOMAIN ),
                 'shipping_postcode_error'      => __( 'Nao foi possivel buscar o CEP agora. Continue manualmente.', EOP_TEXT_DOMAIN ),
                 'shipping_rates_found'         => __( 'Opcoes encontradas. Escolha a melhor para o cliente.', EOP_TEXT_DOMAIN ),
+                'post_flow_not_available'      => __( 'Este pedido nao esta usando o fluxo complementar da proposta.', EOP_TEXT_DOMAIN ),
+                'post_flow_stage_inactive'     => __( 'Inativo', EOP_TEXT_DOMAIN ),
+                'post_flow_contract_pending'   => __( 'Aceite contratual pendente.', EOP_TEXT_DOMAIN ),
+                'post_flow_contract_done'      => __( 'Aceite registrado.', EOP_TEXT_DOMAIN ),
+                'post_flow_documents_empty'    => __( 'Nenhum dado do pedido preenchido no WooCommerce ate agora.', EOP_TEXT_DOMAIN ),
+                'post_flow_signature_documents_empty' => __( 'Nenhum documento para assinatura foi gerado ainda.', EOP_TEXT_DOMAIN ),
+                'post_flow_attachment_missing' => __( 'Nenhum anexo registrado.', EOP_TEXT_DOMAIN ),
+                'post_flow_attachment_done'    => __( 'Anexo registrado com sucesso.', EOP_TEXT_DOMAIN ),
+                'post_flow_products_empty'     => __( 'Nenhuma personalizacao registrada ate agora.', EOP_TEXT_DOMAIN ),
+                'post_flow_open_public'        => __( 'Abrir link publico', EOP_TEXT_DOMAIN ),
+                'post_flow_download_pdf'       => __( 'Baixar PDF complementar', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_stage'         => __( 'Etapa atual', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_documents'     => __( 'Dados do pedido', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_attachment'    => __( 'Anexo', EOP_TEXT_DOMAIN ),
+                'post_flow_stat_products'      => __( 'Produtos', EOP_TEXT_DOMAIN ),
             ),
         ) );
     }
@@ -128,6 +145,7 @@ class EOP_Orders_Page {
             'paged'      => 1,
             'status'     => 'any',
             'search'     => '',
+            'post_confirmation_flow' => 'any',
             'orderby'    => 'date',
             'order'      => 'DESC',
         );
@@ -154,7 +172,9 @@ class EOP_Orders_Page {
             }
         }
 
-        if ( ! EOP_Role::is_vendedor() ) {
+		$flow_filter = self::normalize_post_confirmation_flow_filter( $args['post_confirmation_flow'] ?? 'any' );
+
+        if ( ! EOP_Role::is_vendedor() && 'any' === $flow_filter ) {
             return wc_get_orders( $query_args );
         }
 
@@ -166,6 +186,7 @@ class EOP_Orders_Page {
 
         $orders = wc_get_orders( $query_args );
         $orders = array_values( array_filter( $orders, array( __CLASS__, 'current_user_can_access_order' ) ) );
+		$orders = self::filter_orders_by_post_confirmation_flow( $orders, $flow_filter );
 
         $total       = count( $orders );
         $max_pages   = max( 1, (int) ceil( $total / $limit ) );
@@ -189,12 +210,14 @@ class EOP_Orders_Page {
         $paged  = max( 1, absint( $_POST['paged'] ?? 1 ) );
         $search = isset( $_POST['search'] ) ? sanitize_text_field( wp_unslash( $_POST['search'] ) ) : '';
         $status = isset( $_POST['status'] ) ? sanitize_key( wp_unslash( $_POST['status'] ) ) : 'any';
+		$post_confirmation_flow = isset( $_POST['post_confirmation_flow'] ) ? self::normalize_post_confirmation_flow_filter( wp_unslash( $_POST['post_confirmation_flow'] ) ) : 'any';
 
         $result = self::get_orders( array(
             'limit'  => 12,
             'paged'  => $paged,
             'status' => $status,
             'search' => $search,
+			'post_confirmation_flow' => $post_confirmation_flow,
         ) );
 
         $orders = array_map( array( __CLASS__, 'prepare_order_list_item' ), $result->orders );
@@ -236,6 +259,11 @@ class EOP_Orders_Page {
         $items         = array();
         $discount_mode = EOP_Settings::get( 'discount_mode', 'both' );
         foreach ( $order->get_items() as $item ) {
+            if ( ! $item instanceof WC_Order_Item_Product ) {
+                continue;
+            }
+
+            /** @var WC_Order_Item_Product $item */
             $product = $item->get_product();
             $subtotal = (float) $item->get_subtotal();
             $total    = (float) $item->get_total();
@@ -322,6 +350,7 @@ class EOP_Orders_Page {
             'discount'      => $general_discount_data['value'],
             'discount_type' => $general_discount_data['type'],
             'notes'         => $order->get_customer_note(),
+			'post_confirmation_flow' => class_exists( 'EOP_Post_Confirmation_Flow' ) ? EOP_Post_Confirmation_Flow::get_export_data( $order, 'admin' ) : array(),
         ) );
     }
 
@@ -442,7 +471,7 @@ class EOP_Orders_Page {
 
                 if ( $disc_amount > 0 ) {
                     $line_item = $order->get_item( $line_item_id );
-                    if ( $line_item ) {
+                    if ( $line_item instanceof WC_Order_Item_Product ) {
                         $line_item->set_subtotal( $line_total );
                         $line_item->set_total( $line_total - $disc_amount );
                         $line_item->update_meta_data( '_eop_discount_type', $item_disc_type );
@@ -543,6 +572,13 @@ class EOP_Orders_Page {
         return ( $post && (int) $post->post_author === $user_id );
     }
 
+    public static function normalize_post_confirmation_flow_filter( $value ) {
+        $value = sanitize_key( (string) $value );
+        $allowed = array( 'any', 'active', 'pending', 'completed' );
+
+        return in_array( $value, $allowed, true ) ? $value : 'any';
+    }
+
     private static function prepare_order_list_item( $order ) {
         $customer_name = trim( $order->get_billing_first_name() . ' ' . $order->get_billing_last_name() );
         $public_url    = EOP_Public_Proposal::get_public_link( $order );
@@ -562,10 +598,54 @@ class EOP_Orders_Page {
             'pdf_url'         => EOP_Order_Creator::get_pdf_document_url( $order ),
             'public_url'      => $public_url,
             'is_proposal'     => ! empty( $public_url ),
+            'post_confirmation_flow_summary' => class_exists( 'EOP_Post_Confirmation_Flow' ) ? EOP_Post_Confirmation_Flow::get_list_summary( $order ) : array(),
             'created_by_name' => (string) $order->get_meta( '_eop_created_by_name' ),
         );
     }
 
+    private static function filter_orders_by_post_confirmation_flow( $orders, $flow_filter ) {
+        $flow_filter = self::normalize_post_confirmation_flow_filter( $flow_filter );
+
+        if ( 'any' === $flow_filter || empty( $orders ) ) {
+            return $orders;
+        }
+
+        if ( ! class_exists( 'EOP_Post_Confirmation_Flow' ) ) {
+            return array();
+        }
+
+        return array_values(
+            array_filter(
+                $orders,
+                static function ( $order ) use ( $flow_filter ) {
+                    if ( ! $order instanceof WC_Order ) {
+                        return false;
+                    }
+
+                    $summary = EOP_Post_Confirmation_Flow::get_list_summary( $order );
+
+                    if ( empty( $summary['active_for_order'] ) ) {
+                        return false;
+                    }
+
+                    if ( 'active' === $flow_filter ) {
+                        return true;
+                    }
+
+                    if ( 'completed' === $flow_filter ) {
+                        return ! empty( $summary['completed'] );
+                    }
+
+                    if ( 'pending' === $flow_filter ) {
+                        return empty( $summary['completed'] );
+                    }
+
+                    return true;
+                }
+            )
+        );
+    }
+
     private static function resolve_loaded_discount_data( $discount_amount, $base_amount, $discount_mode, $stored_type = '', $stored_value = '' ) {
         $discount_amount = max( 0, (float) $discount_amount );
         $base_amount     = max( 0, (float) $base_amount );
diff --git a/includes/class-pdf-admin-page.php b/includes/class-pdf-admin-page.php
index 83f2428..a4d9a43 100644
--- a/includes/class-pdf-admin-page.php
+++ b/includes/class-pdf-admin-page.php
@@ -15,7 +15,7 @@ class EOP_PDF_Admin_Page {
             return;
         }
 
-        add_action( 'admin_menu', array( __CLASS__, 'register_submenu' ) );
+        add_action( 'admin_init', array( __CLASS__, 'maybe_redirect_legacy_page_request' ) );
         add_action( 'admin_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );
         add_action( 'admin_post_eop_pdf_purge_cache', array( __CLASS__, 'handle_purge_cache' ) );
         add_action( 'admin_post_eop_pdf_reset_counters', array( __CLASS__, 'handle_reset_counters' ) );
@@ -25,61 +25,29 @@ class EOP_PDF_Admin_Page {
     public static function register_submenu() {
         ensure_aireset_parent_menu();
 
-        // self::$page_hook = add_submenu_page(
-        //     'aireset',
-        //     __( 'PDF', EOP_TEXT_DOMAIN ),
-        //     __( 'PDF', EOP_TEXT_DOMAIN ),
-        //     'edit_shop_orders',
-        //     'eop-pdf',
-        //     array( __CLASS__, 'render_documents_page' )
-        // );
-
-        // self::$settings_hook = add_submenu_page(
-        //     'aireset',
-        //     __( 'Configuracoes do PDF', EOP_TEXT_DOMAIN ),
-        //     __( 'Configuracoes do PDF', EOP_TEXT_DOMAIN ),
-        //     'manage_options',
-        //     'eop-pdf-configuracoes',
-        //     array( __CLASS__, 'render_settings_page' )
-        // );
-
-        // foreach ( self::get_tabs() as $tab => $label ) {
-        //     $slug = self::get_tab_page_slug( $tab );
-
-        //     if ( 'eop-pdf' === $slug ) {
-        //         continue;
-        //     }
-
-        //     self::$tab_hooks[ $tab ] = add_submenu_page(
-        //         'aireset',
-        //         $label,
-        //         $label,
-        //         self::get_tab_capability( $tab ),
-        //         $slug,
-        //         function () use ( $tab ) {
-        //             self::render_tab_page( $tab );
-        //         }
-        //     );
-        // }
-
-        // if ( ! self::$hidden_submenu_hooked ) {
-        //     self::$hidden_submenu_hooked = true;
-
-        //     add_action(
-        //         'admin_menu',
-        //         function () {
-        //             remove_submenu_page( 'aireset', 'eop-pdf-configuracoes' );
-        //             foreach ( self::get_tabs() as $tab => $label ) {
-        //                 $slug = self::get_tab_page_slug( $tab );
-
-        //                 if ( 'eop-pdf' !== $slug ) {
-        //                     remove_submenu_page( 'aireset', $slug );
-        //                 }
-        //             }
-        //         },
-        //         999
-        //     );
-        // }
+        self::$tab_hooks = array();
+
+        foreach ( self::get_tabs() as $tab => $label ) {
+            $slug       = self::get_tab_page_slug( $tab );
+            $menu_label = 'general' === $tab ? __( 'PDF', EOP_TEXT_DOMAIN ) : sprintf( __( 'PDF: %s', EOP_TEXT_DOMAIN ), $label );
+            $page_title = sprintf( __( 'PDF - %s', EOP_TEXT_DOMAIN ), $label );
+            $hook       = add_submenu_page(
+                'aireset',
+                $page_title,
+                $menu_label,
+                self::get_tab_capability( $tab ),
+                $slug,
+                function () use ( $tab ) {
+                    self::render_tab_page( $tab );
+                }
+            );
+
+            self::$tab_hooks[ $tab ] = $hook;
+
+            if ( 'general' === $tab ) {
+                self::$page_hook = $hook;
+            }
+        }
     }
 
     public static function enqueue_assets( $hook ) {
@@ -87,9 +55,19 @@ class EOP_PDF_Admin_Page {
             return;
         }
 
-        $font_url = EOP_Settings::get_font_stylesheet_url();
-        if ( $font_url ) {
-            wp_enqueue_style( 'eop-pdf-selected-font', $font_url, array(), null );
+        $document_type = isset( $_GET['document'] ) && 'proposal' === sanitize_key( wp_unslash( $_GET['document'] ) ) ? 'proposal' : 'order';
+        $font_urls     = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_font_stylesheet_urls( $document_type ) : array();
+
+        if ( empty( $font_urls ) ) {
+            $fallback_font_url = EOP_Settings::get_font_stylesheet_url();
+
+            if ( $fallback_font_url ) {
+                $font_urls[] = $fallback_font_url;
+            }
+        }
+
+        foreach ( array_values( array_unique( $font_urls ) ) as $index => $font_url ) {
+            wp_enqueue_style( 'eop-pdf-selected-font-' . $index, $font_url, array(), null );
         }
 
         wp_enqueue_media();
@@ -248,11 +226,6 @@ class EOP_PDF_Admin_Page {
             wp_die( esc_html__( 'Acesso negado.', EOP_TEXT_DOMAIN ) );
         }
 
-        if ( class_exists( 'EOP_Admin_Page' ) && ! self::is_spa_request() ) {
-            wp_safe_redirect( self::get_tab_url( $tab ) );
-            exit;
-        }
-
         self::render_page( $tab );
     }
 
@@ -280,6 +253,29 @@ class EOP_PDF_Admin_Page {
         include EOP_PLUGIN_DIR . 'templates/pdf-admin-page.php';
     }
 
+    public static function maybe_redirect_legacy_page_request() {
+        if ( ! class_exists( 'EOP_Admin_Page' ) ) {
+            return;
+        }
+
+        $page = isset( $_GET['page'] ) ? sanitize_key( wp_unslash( $_GET['page'] ) ) : '';
+
+        if ( 'eop-pdf' !== $page && 0 !== strpos( $page, 'eop-pdf-' ) ) {
+            return;
+        }
+
+        $args = array();
+
+        foreach ( array( 'document', 'preview_order', 'eop_pdf_action' ) as $arg_key ) {
+            if ( isset( $_GET[ $arg_key ] ) ) {
+                $args[ $arg_key ] = sanitize_text_field( wp_unslash( $_GET[ $arg_key ] ) );
+            }
+        }
+
+        wp_safe_redirect( self::get_tab_url( self::get_current_tab(), $args ) );
+        exit;
+    }
+
     public static function handle_purge_cache() {
         self::assert_admin_action_permissions( 'eop_pdf_purge_cache' );
 
diff --git a/includes/class-pdf-settings.php b/includes/class-pdf-settings.php
index 2f06830..c3a9b67 100644
--- a/includes/class-pdf-settings.php
+++ b/includes/class-pdf-settings.php
@@ -23,8 +23,10 @@ class EOP_PDF_Settings {
 
     public static function get_all() {
         return self::apply_document_label_fallbacks(
-            self::apply_legacy_pdf_fallbacks(
-                self::apply_woocommerce_store_defaults( wp_parse_args( get_option( self::OPTION_KEY, array() ), self::get_defaults() ) )
+            self::apply_shared_store_defaults(
+                self::apply_legacy_pdf_fallbacks(
+                    wp_parse_args( get_option( self::OPTION_KEY, array() ), self::get_defaults() )
+                )
             )
         );
     }
@@ -86,6 +88,239 @@ class EOP_PDF_Settings {
         );
     }
 
+    public static function get_document_style_group_definitions() {
+        return array(
+            'brand_name' => array(
+                'label'              => __( 'Nome da loja', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '15',
+                'default_line_height'=> '1.2',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'brand_meta' => array(
+                'label'              => __( 'Dados auxiliares da loja', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '13',
+                'default_line_height'=> '1.35',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+            'title' => array(
+                'label'              => __( 'Titulo do documento', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '34',
+                'default_line_height'=> '1.05',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'meta' => array(
+                'label'              => __( 'Metadados do documento', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '13',
+                'default_line_height'=> '1.35',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+            'summary_label' => array(
+                'label'              => __( 'Rotulos do resumo', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '12',
+                'default_line_height'=> '1.15',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'summary_value' => array(
+                'label'              => __( 'Valores do resumo', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '15',
+                'default_line_height'=> '1.2',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'table_header' => array(
+                'label'              => __( 'Cabecalho da tabela', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '12',
+                'default_line_height'=> '1.15',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'table_body' => array(
+                'label'              => __( 'Corpo da tabela', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '14',
+                'default_line_height'=> '1.25',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+            'table_meta' => array(
+                'label'              => __( 'Textos auxiliares da tabela', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '13',
+                'default_line_height'=> '1.2',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+            'totals' => array(
+                'label'              => __( 'Linhas de totais', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '16',
+                'default_line_height'=> '1.2',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+            'grand_total' => array(
+                'label'              => __( 'Total final em destaque', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '18',
+                'default_line_height'=> '1.1',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'notes_label' => array(
+                'label'              => __( 'Rotulo das observacoes', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '14',
+                'default_line_height'=> '1.15',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '700',
+            ),
+            'notes_body' => array(
+                'label'              => __( 'Corpo das observacoes', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '13',
+                'default_line_height'=> '1.45',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+            'footer' => array(
+                'label'              => __( 'Rodape', EOP_TEXT_DOMAIN ),
+                'default_font_size'  => '12',
+                'default_line_height'=> '1.35',
+                'default_font_style' => 'normal',
+                'default_font_weight'=> '400',
+            ),
+        );
+    }
+
+    public static function get_document_block_definitions() {
+        return array(
+            'page_background_color' => array(
+                'label'   => __( 'Fundo da pagina', EOP_TEXT_DOMAIN ),
+                'default' => '#ffffff',
+            ),
+            'summary_background_color' => array(
+                'label'   => __( 'Fundo do resumo', EOP_TEXT_DOMAIN ),
+                'default' => '#ffffff',
+            ),
+            'totals_background_color' => array(
+                'label'   => __( 'Fundo dos totais', EOP_TEXT_DOMAIN ),
+                'default' => '#ffffff',
+            ),
+            'notes_background_color' => array(
+                'label'   => __( 'Fundo das observacoes', EOP_TEXT_DOMAIN ),
+                'default' => '#ffffff',
+            ),
+            'footer_background_color' => array(
+                'label'   => __( 'Fundo do rodape', EOP_TEXT_DOMAIN ),
+                'default' => '#ffffff',
+            ),
+        );
+    }
+
+    public static function get_document_spacing_definitions() {
+        return array(
+            'summary_block_padding' => array(
+                'label'   => __( 'Espacamento interno do resumo', EOP_TEXT_DOMAIN ),
+                'default' => '14',
+            ),
+            'table_header_cell_padding_y' => array(
+                'label'   => __( 'Altura interna do cabecalho da tabela', EOP_TEXT_DOMAIN ),
+                'default' => '10',
+            ),
+            'table_body_cell_padding_y' => array(
+                'label'   => __( 'Altura interna do corpo da tabela', EOP_TEXT_DOMAIN ),
+                'default' => '10',
+            ),
+            'totals_row_padding_y' => array(
+                'label'   => __( 'Altura interna das linhas de totais', EOP_TEXT_DOMAIN ),
+                'default' => '8',
+            ),
+            'notes_block_padding' => array(
+                'label'   => __( 'Espacamento interno das observacoes', EOP_TEXT_DOMAIN ),
+                'default' => '14',
+            ),
+            'footer_padding_top' => array(
+                'label'   => __( 'Espaco antes do rodape', EOP_TEXT_DOMAIN ),
+                'default' => '16',
+            ),
+        );
+    }
+
+    public static function get_document_style_settings( $document_type = 'order' ) {
+        $document_type = 'proposal' === sanitize_key( (string) $document_type ) ? 'proposal' : 'order';
+        $settings      = self::get_all();
+		$typography    = array();
+		$blocks        = array();
+		$spacing       = array();
+
+        foreach ( self::get_document_style_group_definitions() as $group_key => $definition ) {
+            $prefix = $document_type . '_' . $group_key . '_';
+
+			$typography[ $group_key ] = array(
+                'label'       => (string) $definition['label'],
+                'font_family' => self::sanitize_font_family_value( $settings[ $prefix . 'font_family' ] ?? self::get_default_pdf_font_family(), self::get_default_pdf_font_family() ),
+                'font_size'   => self::sanitize_font_size_value( $settings[ $prefix . 'font_size' ] ?? $definition['default_font_size'], $definition['default_font_size'] ),
+                'line_height' => self::sanitize_line_height_value( $settings[ $prefix . 'line_height' ] ?? $definition['default_line_height'], $definition['default_line_height'] ),
+                'font_style'  => self::sanitize_font_style_value( $settings[ $prefix . 'font_style' ] ?? $definition['default_font_style'], $definition['default_font_style'] ),
+                'font_weight' => self::sanitize_font_weight_value( $settings[ $prefix . 'font_weight' ] ?? $definition['default_font_weight'], $definition['default_font_weight'] ),
+                'text_color'  => self::sanitize_hex_color_value( $settings[ $prefix . 'text_color' ] ?? self::get_default_group_text_color( $document_type, $group_key ), self::get_default_group_text_color( $document_type, $group_key ) ),
+            );
+        }
+
+        foreach ( self::get_document_block_definitions() as $key => $definition ) {
+            $setting_key = $document_type . '_' . $key;
+            $blocks[ $key ] = self::sanitize_hex_color_value( $settings[ $setting_key ] ?? $definition['default'], $definition['default'] );
+        }
+
+        foreach ( self::get_document_spacing_definitions() as $key => $definition ) {
+            $setting_key = $document_type . '_' . $key;
+            $spacing[ $key ] = self::sanitize_spacing_value( $settings[ $setting_key ] ?? $definition['default'], $definition['default'] );
+        }
+
+        return array(
+            'typography' => $typography,
+            'blocks'     => $blocks,
+            'spacing'    => $spacing,
+        );
+    }
+
+    public static function get_document_font_families( $document_type = '' ) {
+        $settings       = self::get_all();
+        $document_types = array( 'order', 'proposal' );
+        $font_families  = array();
+        $fallback_font  = self::get_default_pdf_font_family();
+
+        if ( '' !== $document_type ) {
+            $document_types = array( 'proposal' === sanitize_key( (string) $document_type ) ? 'proposal' : 'order' );
+        }
+
+        if ( '' !== $fallback_font ) {
+            $font_families[] = $fallback_font;
+        }
+
+        foreach ( $document_types as $type ) {
+            foreach ( self::get_document_style_group_definitions() as $group_key => $definition ) {
+                $setting_key      = $type . '_' . $group_key . '_font_family';
+                $font_families[] = self::sanitize_font_family_value( $settings[ $setting_key ] ?? $fallback_font, $fallback_font );
+            }
+        }
+
+        return array_values( array_unique( array_filter( $font_families, 'strlen' ) ) );
+    }
+
+    public static function get_document_font_stylesheet_urls( $document_type = '' ) {
+        $font_families = self::get_document_font_families( $document_type );
+
+        if ( empty( $font_families ) ) {
+            return array();
+        }
+
+        if ( class_exists( 'EOP_Settings' ) && method_exists( 'EOP_Settings', 'get_font_stylesheet_urls' ) ) {
+            return EOP_Settings::get_font_stylesheet_urls( $font_families );
+        }
+
+        return array();
+    }
+
     public static function get_legacy_document_item_label_defaults( $document_type = 'order' ) {
         $document_type = 'proposal' === sanitize_key( (string) $document_type ) ? 'proposal' : 'order';
 
@@ -119,8 +354,10 @@ class EOP_PDF_Settings {
 
     public static function get_output_signature( $settings = null ) {
         $settings = self::apply_document_label_fallbacks(
-            self::apply_legacy_pdf_fallbacks(
-                self::apply_woocommerce_store_defaults( wp_parse_args( is_array( $settings ) ? $settings : array(), self::get_defaults() ) )
+            self::apply_shared_store_defaults(
+                self::apply_legacy_pdf_fallbacks(
+                    wp_parse_args( is_array( $settings ) ? $settings : array(), self::get_defaults() )
+                )
             )
         );
 
@@ -201,6 +438,10 @@ class EOP_PDF_Settings {
             ) {
                 $signature_settings[ $document_type . '_' . $key ] = (string) $settings[ $document_type . '_' . $key ];
             }
+
+			foreach ( self::get_document_style_setting_keys( $document_type ) as $style_key ) {
+				$signature_settings[ $style_key ] = (string) ( $settings[ $style_key ] ?? '' );
+			}
         }
 
         return md5( (string) wp_json_encode( $signature_settings ) );
@@ -791,9 +1032,10 @@ class EOP_PDF_Settings {
     }
 
     public static function get_defaults() {
-        $woo_store = self::get_woocommerce_store_defaults();
+        $shared_store = self::get_shared_store_defaults();
 
-        return array(
+        return array_merge(
+			array(
             'display_mode'              => 'new_tab',
             'paper_size'                => 'a4',
             'template_name'             => 'simple',
@@ -803,16 +1045,16 @@ class EOP_PDF_Settings {
             'extended_currency_symbol'  => 'no',
             'shop_logo_url'             => '',
             'shop_logo_height'          => '3cm',
-            'shop_name'                 => get_bloginfo( 'name' ),
-            'shop_address_line_1'       => $woo_store['shop_address_line_1'],
-            'shop_address_line_2'       => $woo_store['shop_address_line_2'],
-            'shop_city'                 => $woo_store['shop_city'],
-            'shop_state'                => $woo_store['shop_state'],
-            'shop_postcode'             => $woo_store['shop_postcode'],
-            'shop_country'              => $woo_store['shop_country'],
-            'shop_phone'                => '',
-            'shop_email'                => get_bloginfo( 'admin_email' ),
-            'shop_vat_number'           => '',
+            'shop_name'                 => $shared_store['shop_name'],
+            'shop_address_line_1'       => $shared_store['shop_address_line_1'],
+            'shop_address_line_2'       => $shared_store['shop_address_line_2'],
+            'shop_city'                 => $shared_store['shop_city'],
+            'shop_state'                => $shared_store['shop_state'],
+            'shop_postcode'             => $shared_store['shop_postcode'],
+            'shop_country'              => $shared_store['shop_country'],
+            'shop_phone'                => $shared_store['shop_phone'],
+            'shop_email'                => $shared_store['shop_email'],
+            'shop_vat_number'           => $shared_store['shop_vat_number'],
             'shop_chamber_of_commerce'  => '',
             'shop_extra_1'              => '',
             'shop_extra_2'              => '',
@@ -934,15 +1176,18 @@ class EOP_PDF_Settings {
             'advanced_order_note_logs'  => 'no',
             'advanced_auto_cleanup'     => 'yes',
             'advanced_danger_zone'      => 'no',
-        );
+			),
+			self::get_document_style_defaults()
+		);
     }
 
     public static function sanitize_settings( $input ) {
         $defaults = self::get_defaults();
         $saved    = get_option( self::OPTION_KEY, array() );
-        $input    = wp_parse_args( is_array( $input ) ? $input : array(), is_array( $saved ) ? $saved : array() );
+        $raw_input = is_array( $input ) ? $input : array();
+        $input     = wp_parse_args( $raw_input, is_array( $saved ) ? $saved : array() );
 
-        return self::apply_woocommerce_store_defaults( array(
+        $sanitized = array_merge( array(
             'display_mode'              => in_array( $input['display_mode'] ?? '', array( 'new_tab', 'download' ), true ) ? $input['display_mode'] : $defaults['display_mode'],
             'paper_size'                => in_array( $input['paper_size'] ?? '', array( 'a4', 'letter' ), true ) ? $input['paper_size'] : $defaults['paper_size'],
             'template_name'             => in_array( $input['template_name'] ?? '', array( 'simple', 'compact', 'minimal' ), true ) ? $input['template_name'] : $defaults['template_name'],
@@ -1083,7 +1328,125 @@ class EOP_PDF_Settings {
             'advanced_order_note_logs'  => self::sanitize_toggle( $input['advanced_order_note_logs'] ?? $defaults['advanced_order_note_logs'] ),
             'advanced_auto_cleanup'     => self::sanitize_toggle( $input['advanced_auto_cleanup'] ?? $defaults['advanced_auto_cleanup'] ),
             'advanced_danger_zone'      => self::sanitize_toggle( $input['advanced_danger_zone'] ?? $defaults['advanced_danger_zone'] ),
-        ) );
+        ), self::sanitize_document_style_settings( $input, $defaults ) );
+
+        if ( self::has_shared_store_input( $raw_input ) ) {
+            self::sync_shared_store_settings( $sanitized );
+        }
+
+        return self::apply_shared_store_defaults( $sanitized );
+    }
+
+    private static function get_document_style_defaults() {
+        $defaults = array();
+
+        foreach ( array( 'order', 'proposal' ) as $document_type ) {
+            foreach ( self::get_document_style_group_definitions() as $group_key => $definition ) {
+                $prefix = $document_type . '_' . $group_key . '_';
+                $defaults[ $prefix . 'font_family' ] = self::get_default_pdf_font_family();
+                $defaults[ $prefix . 'font_size' ]   = (string) $definition['default_font_size'];
+                $defaults[ $prefix . 'line_height' ] = (string) $definition['default_line_height'];
+                $defaults[ $prefix . 'font_style' ]  = (string) $definition['default_font_style'];
+                $defaults[ $prefix . 'font_weight' ] = (string) $definition['default_font_weight'];
+                $defaults[ $prefix . 'text_color' ]  = self::get_default_group_text_color( $document_type, $group_key );
+            }
+
+            foreach ( self::get_document_block_definitions() as $key => $definition ) {
+                $defaults[ $document_type . '_' . $key ] = (string) $definition['default'];
+            }
+
+            foreach ( self::get_document_spacing_definitions() as $key => $definition ) {
+                $defaults[ $document_type . '_' . $key ] = (string) $definition['default'];
+            }
+        }
+
+        return $defaults;
+    }
+
+    private static function sanitize_document_style_settings( $input, $defaults ) {
+        $sanitized = array();
+
+        foreach ( array( 'order', 'proposal' ) as $document_type ) {
+            foreach ( self::get_document_style_group_definitions() as $group_key => $definition ) {
+                $prefix = $document_type . '_' . $group_key . '_';
+                $sanitized[ $prefix . 'font_family' ] = self::sanitize_font_family_value( $input[ $prefix . 'font_family' ] ?? $defaults[ $prefix . 'font_family' ], $defaults[ $prefix . 'font_family' ] );
+                $sanitized[ $prefix . 'font_size' ]   = self::sanitize_font_size_value( $input[ $prefix . 'font_size' ] ?? $defaults[ $prefix . 'font_size' ], $defaults[ $prefix . 'font_size' ] );
+                $sanitized[ $prefix . 'line_height' ] = self::sanitize_line_height_value( $input[ $prefix . 'line_height' ] ?? $defaults[ $prefix . 'line_height' ], $defaults[ $prefix . 'line_height' ] );
+                $sanitized[ $prefix . 'font_style' ]  = self::sanitize_font_style_value( $input[ $prefix . 'font_style' ] ?? $defaults[ $prefix . 'font_style' ], $defaults[ $prefix . 'font_style' ] );
+                $sanitized[ $prefix . 'font_weight' ] = self::sanitize_font_weight_value( $input[ $prefix . 'font_weight' ] ?? $defaults[ $prefix . 'font_weight' ], $defaults[ $prefix . 'font_weight' ] );
+                $sanitized[ $prefix . 'text_color' ]  = self::sanitize_hex_color_value( $input[ $prefix . 'text_color' ] ?? $defaults[ $prefix . 'text_color' ], $defaults[ $prefix . 'text_color' ] );
+            }
+
+            foreach ( self::get_document_block_definitions() as $key => $definition ) {
+                $setting_key = $document_type . '_' . $key;
+                $sanitized[ $setting_key ] = self::sanitize_hex_color_value( $input[ $setting_key ] ?? $defaults[ $setting_key ], $defaults[ $setting_key ] );
+            }
+
+            foreach ( self::get_document_spacing_definitions() as $key => $definition ) {
+                $setting_key = $document_type . '_' . $key;
+                $sanitized[ $setting_key ] = self::sanitize_spacing_value( $input[ $setting_key ] ?? $defaults[ $setting_key ], $defaults[ $setting_key ] );
+            }
+        }
+
+        return $sanitized;
+    }
+
+    private static function get_document_style_setting_keys( $document_type ) {
+        $document_type = 'proposal' === sanitize_key( (string) $document_type ) ? 'proposal' : 'order';
+        $keys = array();
+
+        foreach ( self::get_document_style_group_definitions() as $group_key => $definition ) {
+            $prefix = $document_type . '_' . $group_key . '_';
+            $keys[] = $prefix . 'font_family';
+            $keys[] = $prefix . 'font_size';
+            $keys[] = $prefix . 'line_height';
+            $keys[] = $prefix . 'font_style';
+            $keys[] = $prefix . 'font_weight';
+            $keys[] = $prefix . 'text_color';
+        }
+
+        foreach ( self::get_document_block_definitions() as $key => $definition ) {
+            $keys[] = $document_type . '_' . $key;
+        }
+
+        foreach ( self::get_document_spacing_definitions() as $key => $definition ) {
+            $keys[] = $document_type . '_' . $key;
+        }
+
+        return $keys;
+    }
+
+    private static function get_default_group_text_color( $document_type, $group_key ) {
+        $body_color   = '#172033';
+        $muted_color  = '#5b6474';
+        $header_color = '#ffffff';
+
+        if ( in_array( $group_key, array( 'brand_meta', 'meta', 'summary_label', 'table_meta', 'notes_body', 'footer' ), true ) ) {
+            return $muted_color;
+        }
+
+        if ( 'table_header' === $group_key ) {
+            return $header_color;
+        }
+
+        return $body_color;
+    }
+
+    private static function get_default_pdf_font_family() {
+        if ( class_exists( 'EOP_Settings' ) ) {
+            $configured_font = EOP_Settings::get( 'font_family', '' );
+
+            if ( ! empty( $configured_font ) ) {
+                return (string) $configured_font;
+            }
+
+            $defaults = EOP_Settings::get_defaults();
+            if ( ! empty( $defaults['font_family'] ) ) {
+                return (string) $defaults['font_family'];
+            }
+        }
+
+        return 'Montserrat:400,700';
     }
 
     private static function get_woocommerce_store_defaults() {
@@ -1112,18 +1475,97 @@ class EOP_PDF_Settings {
         );
     }
 
-    private static function apply_woocommerce_store_defaults( $settings ) {
+    private static function get_shared_store_defaults() {
+        $defaults = self::get_woocommerce_store_defaults();
+
+        $defaults['shop_name']       = sanitize_text_field( (string) get_option( 'blogname', get_bloginfo( 'name' ) ) );
+        $defaults['shop_phone']      = sanitize_text_field( (string) get_option( 'telefone', '' ) );
+        $defaults['shop_email']      = sanitize_email( (string) get_option( 'email_contato', get_option( 'admin_email', get_bloginfo( 'admin_email' ) ) ) );
+        $defaults['shop_vat_number'] = sanitize_text_field( (string) get_option( 'cnpj', '' ) );
+
+        return $defaults;
+    }
+
+    private static function apply_shared_store_defaults( $settings ) {
         $settings = is_array( $settings ) ? $settings : array();
 
-        foreach ( self::get_woocommerce_store_defaults() as $key => $value ) {
-            if ( ! isset( $settings[ $key ] ) || '' === trim( (string) $settings[ $key ] ) ) {
-                $settings[ $key ] = $value;
-            }
+        foreach ( self::get_shared_store_defaults() as $key => $value ) {
+            $settings[ $key ] = $value;
         }
 
         return $settings;
     }
 
+    private static function has_shared_store_input( $input ) {
+        if ( ! is_array( $input ) ) {
+            return false;
+        }
+
+        $shared_keys = array(
+            'shop_name',
+            'shop_address_line_1',
+            'shop_address_line_2',
+            'shop_city',
+            'shop_state',
+            'shop_postcode',
+            'shop_country',
+            'shop_phone',
+            'shop_email',
+            'shop_vat_number',
+        );
+
+        foreach ( $shared_keys as $shared_key ) {
+            if ( array_key_exists( $shared_key, $input ) ) {
+                return true;
+            }
+        }
+
+        return false;
+    }
+
+    private static function sync_shared_store_settings( $settings ) {
+        $settings = is_array( $settings ) ? $settings : array();
+
+        update_option( 'blogname', sanitize_text_field( (string) ( $settings['shop_name'] ?? get_option( 'blogname', '' ) ) ) );
+        update_option( 'telefone', sanitize_text_field( (string) ( $settings['shop_phone'] ?? '' ) ) );
+        update_option( 'email_contato', sanitize_email( (string) ( $settings['shop_email'] ?? '' ) ) );
+        update_option( 'cnpj', sanitize_text_field( (string) ( $settings['shop_vat_number'] ?? '' ) ) );
+
+        self::sync_store_address_settings( $settings );
+    }
+
+    private static function sync_store_address_settings( $settings ) {
+        $address_1 = sanitize_text_field( (string) ( $settings['shop_address_line_1'] ?? '' ) );
+        $address_2 = sanitize_text_field( (string) ( $settings['shop_address_line_2'] ?? '' ) );
+        $city      = sanitize_text_field( (string) ( $settings['shop_city'] ?? '' ) );
+        $postcode  = sanitize_text_field( (string) ( $settings['shop_postcode'] ?? '' ) );
+        $country   = strtoupper( sanitize_text_field( (string) ( $settings['shop_country'] ?? 'BR' ) ) );
+        $state     = strtoupper( sanitize_text_field( (string) ( $settings['shop_state'] ?? '' ) ) );
+
+        $country = preg_replace( '/[^A-Z]/', '', $country );
+        if ( strlen( $country ) !== 2 ) {
+            $country = 'BR';
+        }
+
+        $state = preg_replace( '/[^A-Z]/', '', $state );
+        if ( 'BR' === $country ) {
+            $state = substr( $state, 0, 2 );
+        }
+
+        update_option( 'endereco', $address_1 );
+        update_option( 'endereco_linha_2', $address_2 );
+        update_option( 'cidade', $city );
+        update_option( 'cep', $postcode );
+        update_option( 'pais', $country );
+        update_option( 'estado', $state );
+
+        update_option( 'woocommerce_store_address', $address_1 );
+        update_option( 'woocommerce_store_address_2', $address_2 );
+        update_option( 'woocommerce_store_city', $city );
+        update_option( 'woocommerce_store_postcode', $postcode );
+        update_option( 'woocommerce_default_country', '' === $state ? $country : $country . ':' . $state );
+    }
+
     private static function apply_legacy_pdf_fallbacks( $settings ) {
         $settings = is_array( $settings ) ? $settings : array();
         $legacy   = self::get_legacy_pdf_settings();
@@ -1291,6 +1733,52 @@ class EOP_PDF_Settings {
         return (string) $size;
     }
 
+    private static function sanitize_line_height_value( $value, $default ) {
+        $raw      = is_numeric( $value ) ? (float) $value : (float) $default;
+        $fallback = is_numeric( $default ) ? (float) $default : 1.2;
+
+        if ( $raw < 0.8 || $raw > 3 ) {
+            $raw = $fallback;
+        }
+
+        return number_format( $raw, 2, '.', '' );
+    }
+
+    private static function sanitize_font_family_value( $value, $default ) {
+        $value = sanitize_text_field( (string) $value );
+        $value = preg_replace( '/[^A-Za-z0-9,\:\+\-\s]/', '', $value );
+        $value = trim( preg_replace( '/\s+/', ' ', $value ) );
+
+        return '' !== $value ? $value : (string) $default;
+    }
+
+    private static function sanitize_font_style_value( $value, $default ) {
+        $value   = sanitize_key( (string) $value );
+        $default = sanitize_key( (string) $default );
+        $allowed = array( 'normal', 'italic' );
+
+        return in_array( $value, $allowed, true ) ? $value : ( in_array( $default, $allowed, true ) ? $default : 'normal' );
+    }
+
+    private static function sanitize_font_weight_value( $value, $default ) {
+        $allowed = array( '300', '400', '500', '600', '700', '800', '900' );
+        $value   = (string) absint( $value );
+        $default = (string) absint( $default );
+
+        return in_array( $value, $allowed, true ) ? $value : ( in_array( $default, $allowed, true ) ? $default : '400' );
+    }
+
+    private static function sanitize_spacing_value( $value, $default ) {
+        $size     = absint( $value );
+        $fallback = max( 0, min( 80, absint( $default ) ) );
+
+        if ( $size < 0 || $size > 80 ) {
+            $size = $fallback;
+        }
+
+        return (string) $size;
+    }
+
     private static function sanitize_column_position_value( $value, $default ) {
         $position = absint( $value );
         $fallback = max( 1, min( 99, absint( $default ) ) );
diff --git a/includes/class-public-proposal.php b/includes/class-public-proposal.php
index e43c0db..9017f3c 100644
--- a/includes/class-public-proposal.php
+++ b/includes/class-public-proposal.php
@@ -74,13 +74,25 @@ class EOP_Public_Proposal {
         $order->add_order_note( __( 'Proposta confirmada pelo cliente.', EOP_TEXT_DOMAIN ) );
         $order->save();
 
+        if ( class_exists( 'EOP_Post_Confirmation_Flow' ) && method_exists( 'EOP_Post_Confirmation_Flow', 'ensure_signature_documents_generated' ) ) {
+            EOP_Post_Confirmation_Flow::ensure_signature_documents_generated( $order, true );
+        }
+
         $redirect = add_query_arg( 'eop_confirmed', '1', self::get_public_link( $order ) );
 
+        if ( 'yes' === EOP_Settings::get( 'enable_checkout_confirmation', 'no' ) && method_exists( $order, 'needs_payment' ) && $order->needs_payment() ) {
+            $payment_url = $order->get_checkout_payment_url();
+
+            if ( '' !== $payment_url ) {
+                $redirect = $payment_url;
+            }
+        }
+
         wp_safe_redirect( $redirect );
         exit;
     }
 
-    private static function get_order_by_token( $token ) {
+    public static function get_order_by_token( $token ) {
         $orders = wc_get_orders(
             array(
                 'limit'      => 1,
@@ -132,6 +144,9 @@ class EOP_Public_Proposal {
                 return isset( $column['key'] ) && 'line_total' === $column['key'];
             }
         );
+        $flow_enabled       = class_exists( 'EOP_Post_Confirmation_Flow' ) && EOP_Post_Confirmation_Flow::is_enabled_for_order( $order );
+        $flow_stage         = $flow_enabled ? EOP_Post_Confirmation_Flow::get_current_stage( $order ) : 'inactive';
+        $show_isolated_flow = $confirmed && $flow_enabled;
 
         ob_start();
         ?>
@@ -139,40 +154,112 @@ class EOP_Public_Proposal {
             <link rel="stylesheet" href="<?php echo esc_url( $font_url ); ?>">
         <?php endif; ?>
         <style>
-            body{background:<?php echo esc_attr( $settings['proposal_background_color'] ); ?>}
-            .eop-proposal-wrap{max-width:<?php echo esc_attr( $max_width ); ?>px;margin:32px auto;padding:0 16px;font-family:<?php echo esc_attr( $font_css ); ?>;font-size:<?php echo esc_attr( $base_font_size ); ?>px;color:<?php echo esc_attr( $settings['proposal_text_color'] ); ?>}
-            .eop-proposal-card{background:<?php echo esc_attr( $settings['proposal_card_color'] ); ?>;border:1px solid <?php echo esc_attr( $settings['border_color'] ); ?>;border-radius:<?php echo esc_attr( $settings['border_radius'] ); ?>px;padding:32px;box-shadow:0 16px 42px rgba(23,32,51,.08)}
-            .eop-proposal-header{display:flex;align-items:flex-start;justify-content:space-between;gap:28px;margin-bottom:28px}
+            body{background:radial-gradient(circle at top left,rgba(255,255,255,.82) 0,rgba(255,255,255,0) 24%),linear-gradient(180deg,#eef2ff 0%,<?php echo esc_attr( $settings['proposal_background_color'] ); ?> 34%,#f6f7fb 100%)}
+            .eop-proposal-wrap{max-width:<?php echo esc_attr( $max_width ); ?>px;margin:40px auto;padding:0 18px 56px;font-family:<?php echo esc_attr( $font_css ); ?>;font-size:<?php echo esc_attr( $base_font_size ); ?>px;color:<?php echo esc_attr( $settings['proposal_text_color'] ); ?>}
+            .eop-proposal-card{position:relative;overflow:hidden;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(248,250,255,.98));border:1px solid rgba(255,255,255,.72);border-radius:<?php echo esc_attr( max( 24, (int) $settings['border_radius'] + 8 ) ); ?>px;padding:36px;box-shadow:0 28px 80px rgba(23,32,51,.14)}
+            .eop-proposal-card::before{content:"";position:absolute;inset:0 auto auto 0;width:100%;height:184px;background:radial-gradient(circle at top right,rgba(255,255,255,.18) 0,rgba(255,255,255,0) 34%),linear-gradient(135deg,#000126 0%,#00034b 42%,#1d3fa5 100%)}
+            .eop-proposal-card > *{position:relative;z-index:1}
+            .eop-proposal-header{display:flex;align-items:flex-start;justify-content:space-between;gap:32px;margin-bottom:30px;padding:6px 0 24px}
             .eop-proposal-header.has-no-logo{justify-content:flex-start}
-            .eop-proposal-brand{flex:0 0 auto}
+            .eop-proposal-brand{flex:0 0 auto;padding:18px 20px;border-radius:26px;background:rgba(255,255,255,.92);box-shadow:0 16px 40px rgba(0,3,75,.16)}
             .eop-proposal-header__content{max-width:640px}
             .eop-proposal-logo{max-height:64px;max-width:220px}
-            .eop-proposal-title{margin:0 0 8px;font-size:<?php echo esc_attr( $title_font_size ); ?>px;line-height:1.08}
-            .eop-proposal-text{margin:0;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>}
-            .eop-proposal-status{display:inline-block;margin-top:16px;padding:10px 14px;border-radius:999px;background:#eef4ff;color:<?php echo esc_attr( $settings['primary_color'] ); ?>;font-weight:700}
-            .eop-proposal-meta{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:14px;margin-bottom:22px}
-            .eop-proposal-meta p{margin:0;padding:14px 16px;border:1px solid <?php echo esc_attr( $settings['border_color'] ); ?>;border-radius:<?php echo esc_attr( max( 14, (int) $settings['border_radius'] - 2 ) ); ?>px;background:rgba(255,255,255,.58)}
+            .eop-proposal-title{margin:0 0 10px;font-size:<?php echo esc_attr( $title_font_size ); ?>px;line-height:1;letter-spacing:-.05em;color:#fff;text-shadow:0 14px 40px rgba(0,0,0,.16)}
+            .eop-proposal-text{margin:0;max-width:54ch;color:rgba(255,255,255,.76);font-size:16px;line-height:1.6}
+            .eop-proposal-status{display:inline-flex;align-items:center;min-height:42px;margin-top:18px;padding:0 16px;border-radius:999px;background:rgba(255,255,255,.14);border:1px solid rgba(255,255,255,.14);backdrop-filter:blur(16px);color:#fff;font-weight:800;letter-spacing:.04em}
+            .eop-proposal-meta{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px;margin-bottom:24px}
+            .eop-proposal-meta p{margin:0;padding:16px 18px;border:1px solid rgba(219,226,255,.72);border-radius:22px;background:linear-gradient(180deg,rgba(255,255,255,.94),rgba(243,246,255,.9));box-shadow:0 12px 28px rgba(23,32,51,.06)}
             .eop-proposal-items{display:grid;gap:16px;margin-top:24px}
-            .eop-proposal-item{display:grid;grid-template-columns:96px minmax(0,1fr) auto;gap:18px;align-items:center;padding:18px;border:1px solid <?php echo esc_attr( $settings['border_color'] ); ?>;border-radius:<?php echo esc_attr( max( 18, (int) $settings['border_radius'] ) ); ?>px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(245,247,255,.72))}
-            .eop-proposal-item__media{width:96px;height:96px;border-radius:22px;overflow:hidden;background:#fff;border:1px solid <?php echo esc_attr( $settings['border_color'] ); ?>;display:flex;align-items:center;justify-content:center}
+            .eop-proposal-item{display:grid;grid-template-columns:96px minmax(0,1fr) auto;gap:20px;align-items:center;padding:20px;border:1px solid rgba(219,226,255,.76);border-radius:26px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(242,246,255,.9));box-shadow:0 14px 34px rgba(23,32,51,.07)}
+            .eop-proposal-item__media{width:96px;height:96px;border-radius:24px;overflow:hidden;background:#fff;border:1px solid rgba(219,226,255,.8);display:flex;align-items:center;justify-content:center;box-shadow:0 10px 24px rgba(23,32,51,.08)}
             .eop-proposal-item__media img{display:block;width:100%;height:100%;object-fit:cover}
             .eop-proposal-item__body{min-width:0}
-            .eop-proposal-item__name{margin:0 0 8px;font-size:22px;line-height:1.2}
+            .eop-proposal-item__name{margin:0 0 8px;font-size:22px;line-height:1.15;letter-spacing:-.03em}
             .eop-proposal-item__meta{display:flex;flex-wrap:wrap;gap:10px 16px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:14px}
-            .eop-proposal-item__pill{display:inline-flex;align-items:center;min-height:32px;padding:0 12px;border-radius:999px;background:#f1f4ff;color:<?php echo esc_attr( $settings['primary_color'] ); ?>;font-weight:700;line-height:1.2;white-space:nowrap}
+            .eop-proposal-item__pill{display:inline-flex;align-items:center;min-height:34px;padding:0 13px;border-radius:999px;background:#edf2ff;color:<?php echo esc_attr( $settings['primary_color'] ); ?>;font-weight:800;line-height:1.2;white-space:nowrap}
             .eop-proposal-item__summary{display:grid;gap:6px;min-width:150px;justify-items:end;text-align:right}
             .eop-proposal-item__summary span{font-size:13px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;text-transform:uppercase;letter-spacing:.08em;font-weight:700}
-            .eop-proposal-item__summary strong{font-size:28px;line-height:1;color:<?php echo esc_attr( $settings['proposal_text_color'] ); ?>}
-            .eop-proposal-notes{margin-top:24px;padding:18px;border:1px solid <?php echo esc_attr( $settings['border_color'] ); ?>;border-radius:<?php echo esc_attr( max( 14, (int) $settings['border_radius'] - 2 ) ); ?>px;background:rgba(255,255,255,.7)}
+            .eop-proposal-item__summary strong{font-size:30px;line-height:1;color:<?php echo esc_attr( $settings['proposal_text_color'] ); ?>;letter-spacing:-.05em}
+            .eop-proposal-notes{margin-top:24px;padding:20px;border:1px solid rgba(219,226,255,.76);border-radius:24px;background:linear-gradient(180deg,rgba(255,255,255,.94),rgba(243,246,255,.88));box-shadow:0 12px 28px rgba(23,32,51,.06)}
             .eop-proposal-notes span{display:block;margin-bottom:8px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:13px;font-weight:700;letter-spacing:.06em;text-transform:uppercase}
             .eop-proposal-notes p{margin:0}
             .eop-proposal-totals{margin-top:24px;margin-left:auto;max-width:320px}
             .eop-proposal-total{display:flex;justify-content:space-between;padding:8px 0}
             .eop-proposal-total.is-grand{font-size:20px;font-weight:700;border-top:2px solid <?php echo esc_attr( $settings['primary_color'] ); ?>;margin-top:8px;padding-top:12px}
-            .eop-proposal-button{display:inline-flex;align-items:center;justify-content:center;min-height:48px;padding:0 22px;border:none;border-radius:<?php echo esc_attr( $settings['border_radius'] ); ?>px;background:<?php echo esc_attr( $settings['primary_color'] ); ?>;color:#fff;text-decoration:none;font-weight:700;cursor:pointer}
+            .eop-proposal-button{display:inline-flex;align-items:center;justify-content:center;min-height:52px;padding:0 24px;border:none;border-radius:18px;background:linear-gradient(135deg,<?php echo esc_attr( $settings['primary_color'] ); ?> 0%,#050a76 100%);color:#fff;text-decoration:none;font-weight:800;cursor:pointer;box-shadow:0 18px 38px rgba(0,3,75,.24);transition:transform .18s ease,box-shadow .18s ease,opacity .18s ease}
+            .eop-proposal-button:hover,.eop-proposal-button:focus{transform:translateY(-1px);box-shadow:0 22px 46px rgba(0,3,75,.28);color:#fff}
             .eop-proposal-actions{display:flex;flex-wrap:wrap;gap:12px;margin-top:28px}
-            .eop-proposal-note{margin-top:18px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>}
-            @media (max-width: 720px){.eop-proposal-wrap{font-size:15px}.eop-proposal-card{padding:22px}.eop-proposal-header{flex-direction:column;align-items:flex-start}.eop-proposal-title{font-size:<?php echo esc_attr( max( 26, $title_font_size - 8 ) ); ?>px}.eop-proposal-item{grid-template-columns:1fr}.eop-proposal-item__media{width:84px;height:84px}.eop-proposal-item__summary{justify-items:start;text-align:left}}
+            .eop-proposal-note{margin-top:14px;padding:0 2px;color:rgba(255,255,255,.86);font-weight:700}
+            .eop-proposal-button--secondary{background:rgba(255,255,255,.98);color:<?php echo esc_attr( $settings['primary_color'] ); ?>;border:1px solid rgba(219,226,255,.82);box-shadow:0 12px 28px rgba(0,3,75,.1)}
+            .eop-proposal-button--secondary:hover,.eop-proposal-button--secondary:focus{color:<?php echo esc_attr( $settings['primary_color'] ); ?>}
+            .eop-post-flow{position:relative;overflow:hidden;margin-top:34px;padding:28px;border:1px solid rgba(219,226,255,.76);border-radius:30px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(246,248,255,.98));box-shadow:0 26px 70px rgba(23,32,51,.12)}
+            .eop-post-flow::before{content:"";position:absolute;inset:0 auto auto 0;width:100%;height:210px;background:radial-gradient(circle at top right,rgba(255,255,255,.18) 0,rgba(255,255,255,0) 30%),linear-gradient(135deg,#050a2d 0%,#00034b 46%,#2148c4 100%)}
+            .eop-post-flow > *{position:relative;z-index:1}
+            .eop-post-flow__layout{display:grid;grid-template-columns:minmax(0,1fr) 320px;gap:22px;align-items:start}
+            .eop-post-flow__main{display:grid;gap:20px;align-content:start}
+            .eop-post-flow__sidebar{display:grid;gap:16px;position:sticky;top:24px}
+            .eop-post-flow__heading{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;margin-bottom:4px;padding:24px;border-radius:26px;background:linear-gradient(145deg,rgba(255,255,255,.12),rgba(255,255,255,.04));border:1px solid rgba(255,255,255,.14);backdrop-filter:blur(16px);box-shadow:0 22px 44px rgba(0,3,75,.24)}
+            .eop-post-flow__eyebrow{display:block;margin-bottom:10px;font-size:11px;font-weight:800;letter-spacing:.16em;text-transform:uppercase;color:rgba(255,255,255,.68)}
+            .eop-post-flow__title{margin:0;font-size:32px;line-height:1.02;letter-spacing:-.05em;color:#fff}
+            .eop-post-flow__badge{display:inline-flex;align-items:center;min-height:40px;padding:0 14px;border-radius:999px;background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.16);color:#fff;font-size:11px;font-weight:900;letter-spacing:.12em;text-transform:uppercase;white-space:nowrap}
+            .eop-post-flow__stage-intro{display:flex;justify-content:space-between;gap:18px;align-items:flex-start;padding:20px 22px;border-radius:24px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(243,246,255,.94));border:1px solid rgba(219,226,255,.76);box-shadow:0 14px 32px rgba(23,32,51,.06)}
+            .eop-post-flow__stage-copy{display:grid;gap:10px}
+            .eop-post-flow__stage-pills{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:10px;max-width:280px}
+            .eop-post-flow__stage-pill{display:inline-flex;align-items:center;min-height:38px;padding:0 14px;border-radius:999px;background:#edf2ff;color:<?php echo esc_attr( $settings['primary_color'] ); ?>;font-size:12px;font-weight:800;letter-spacing:.06em;text-transform:uppercase;text-align:center}
+            .eop-post-flow__stage-pill.is-muted{background:#fff7e7;color:#8f5b00}
+            .eop-post-flow__text{margin:0;color:<?php echo esc_attr( $settings['proposal_text_color'] ); ?>;font-size:28px;line-height:1.04;letter-spacing:-.04em;font-weight:800}
+            .eop-post-flow__helper{margin:0;max-width:64ch;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:15px;line-height:1.65}
+            .eop-post-flow__actions{display:flex;flex-wrap:wrap;gap:12px}
+            .eop-post-flow__contract{margin:0;padding:24px;border-radius:24px;border:1px solid rgba(219,226,255,.78);background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(243,246,255,.92));box-shadow:0 14px 34px rgba(23,32,51,.07)}
+            .eop-post-flow__contract h3{margin:0 0 14px;font-size:22px;line-height:1.1;letter-spacing:-.03em}
+            .eop-post-flow__contract p:last-child{margin-bottom:0}
+            .eop-post-flow__form{display:grid;gap:16px}
+            .eop-post-flow__field{display:grid;gap:8px;font-weight:700}
+            .eop-post-flow__field span{font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>}
+            .eop-post-flow__field input,.eop-post-flow__field textarea{width:100%;min-height:60px;padding:16px 18px;border:1px solid rgba(219,226,255,.86);border-radius:18px;background:#fff;color:<?php echo esc_attr( $settings['proposal_text_color'] ); ?>;font:inherit;box-shadow:inset 0 1px 0 rgba(255,255,255,.6);transition:border-color .18s ease,box-shadow .18s ease,transform .18s ease}
+            .eop-post-flow__field input:focus,.eop-post-flow__field textarea:focus{outline:none;border-color:<?php echo esc_attr( $settings['primary_color'] ); ?>;box-shadow:0 0 0 5px rgba(0,3,75,.08);transform:translateY(-1px)}
+            .eop-post-flow__field textarea{min-height:120px;resize:vertical}
+            .eop-post-flow__field small{color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-weight:400;text-transform:none;letter-spacing:0}
+            .eop-post-flow__checkbox{display:flex;gap:12px;align-items:flex-start;padding:16px 18px;border-radius:18px;background:#f7f9ff;border:1px solid rgba(219,226,255,.78);font-weight:800}
+            .eop-post-flow__checkbox input{margin-top:4px;accent-color:<?php echo esc_attr( $settings['primary_color'] ); ?>;transform:scale(1.08)}
+            .eop-post-flow__panel{padding:18px;border:1px solid rgba(219,226,255,.8);border-radius:22px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(246,248,255,.96));box-shadow:0 12px 30px rgba(23,32,51,.07)}
+            .eop-post-flow__panel-label{display:block;margin-bottom:12px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:11px;font-weight:900;letter-spacing:.14em;text-transform:uppercase}
+            .eop-post-flow__progress-list,.eop-post-flow__stats-grid{display:grid;gap:10px}
+            .eop-post-flow__progress-item{display:grid;grid-template-columns:42px minmax(0,1fr);gap:12px;align-items:start;padding:14px;border-radius:18px;background:#f8faff;border:1px solid transparent}
+            .eop-post-flow__progress-item strong{display:block;font-size:14px;line-height:1.25}
+            .eop-post-flow__progress-item small{display:block;margin-top:4px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:12px;line-height:1.45}
+            .eop-post-flow__progress-item.is-current{background:#eef4ff;border-color:rgba(0,3,75,.12);box-shadow:0 10px 24px rgba(0,3,75,.08)}
+            .eop-post-flow__progress-item.is-completed{background:#f3fbf4}
+            .eop-post-flow__progress-index{display:flex;align-items:center;justify-content:center;min-height:42px;border-radius:16px;background:#e6ebf9;color:<?php echo esc_attr( $settings['primary_color'] ); ?>;font-weight:900;box-shadow:inset 0 1px 0 rgba(255,255,255,.8)}
+            .eop-post-flow__progress-item.is-completed .eop-post-flow__progress-index{background:#dff3e3;color:#227547}
+            .eop-post-flow__progress-item.is-current .eop-post-flow__progress-index{background:<?php echo esc_attr( $settings['primary_color'] ); ?>;color:#fff}
+            .eop-post-flow__stat-card{padding:14px 16px;border-radius:18px;background:#f8faff;border:1px solid rgba(219,226,255,.84)}
+            .eop-post-flow__stat-card span{display:block;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:10px;font-weight:900;letter-spacing:.14em;text-transform:uppercase}
+            .eop-post-flow__stat-card strong{display:block;margin-top:7px;font-size:24px;line-height:1;letter-spacing:-.05em}
+            .eop-post-flow__stat-card small{display:block;margin-top:6px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:12px;line-height:1.4}
+            .eop-post-flow__documents-grid,.eop-post-flow__products-grid,.eop-post-flow__completion-grid{display:grid;gap:16px}
+            .eop-post-flow__documents-grid{grid-template-columns:repeat(auto-fit,minmax(250px,1fr))}
+            .eop-post-flow__products-grid{grid-template-columns:repeat(auto-fit,minmax(280px,1fr))}
+            .eop-post-flow__product-card{display:grid;grid-template-columns:88px minmax(0,1fr);gap:14px;padding:18px;border:1px solid rgba(219,226,255,.78);border-radius:22px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(243,246,255,.92));box-shadow:0 12px 28px rgba(23,32,51,.06)}
+            .eop-post-flow__product-card.is-locked{opacity:.86;background:#fbfcff}
+            .eop-post-flow__product-media{width:88px;height:88px;border-radius:20px;overflow:hidden;border:1px solid rgba(219,226,255,.8);box-shadow:0 8px 20px rgba(23,32,51,.08)}
+            .eop-post-flow__product-media img{width:100%;height:100%;display:block;object-fit:cover}
+            .eop-post-flow__product-copy{display:grid;gap:4px;align-content:start;margin-bottom:10px}
+            .eop-post-flow__product-copy span{font-size:13px;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>}
+            .eop-post-flow__upload-card{display:grid;gap:12px;padding:18px;border:1px solid rgba(219,226,255,.78);border-radius:22px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(243,246,255,.92));box-shadow:0 14px 30px rgba(23,32,51,.07)}
+            .eop-post-flow__upload-card strong{display:block;margin-bottom:2px;font-size:18px;line-height:1.12;letter-spacing:-.03em}
+            .eop-post-flow__upload-card small{color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;line-height:1.55}
+            .eop-post-flow__acceptance-card{display:grid;gap:18px;padding:24px;border-radius:26px;background:linear-gradient(180deg,#ffffff 0%,#f5f8ff 100%);border:1px solid rgba(219,226,255,.84);box-shadow:0 18px 38px rgba(23,32,51,.08)}
+            .eop-post-flow__acceptance-head{display:grid;gap:6px}
+            .eop-post-flow__acceptance-head strong{font-size:22px;line-height:1.05;letter-spacing:-.04em}
+            .eop-post-flow__acceptance-head small{color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>;font-size:14px;line-height:1.55}
+            .eop-post-flow__form--acceptance .eop-proposal-button{width:100%;min-height:58px;font-size:16px}
+            .eop-post-flow__completion-grid{grid-template-columns:repeat(auto-fit,minmax(240px,1fr));margin-bottom:18px}
+            .eop-post-flow__completion-item{display:flex;gap:12px;align-items:center;padding:16px;border:1px solid rgba(219,226,255,.8);border-radius:20px;background:linear-gradient(180deg,rgba(255,255,255,.98),rgba(243,246,255,.92));box-shadow:0 12px 28px rgba(23,32,51,.06)}
+            .eop-post-flow__completion-item img{width:54px;height:54px;border-radius:14px;object-fit:cover}
+            .eop-post-flow__completion-item p{margin:4px 0 0;color:<?php echo esc_attr( $settings['proposal_muted_color'] ); ?>}
+            @media (max-width: 920px){.eop-post-flow__layout{grid-template-columns:1fr}.eop-post-flow__sidebar{position:static}.eop-post-flow__stage-intro{flex-direction:column}.eop-post-flow__stage-pills{justify-content:flex-start;max-width:none}}
+            @media (max-width: 720px){.eop-proposal-wrap{font-size:15px;padding:0 14px 34px}.eop-proposal-card{padding:22px;border-radius:24px}.eop-proposal-card::before{height:164px}.eop-proposal-header{flex-direction:column;align-items:flex-start}.eop-proposal-brand{padding:16px 18px}.eop-proposal-title{font-size:<?php echo esc_attr( max( 28, $title_font_size - 8 ) ); ?>px}.eop-proposal-text{font-size:15px}.eop-proposal-item{grid-template-columns:1fr}.eop-proposal-item__media{width:84px;height:84px}.eop-proposal-item__summary{justify-items:start;text-align:left}.eop-post-flow{padding:18px;border-radius:24px}.eop-post-flow__heading{padding:18px;border-radius:20px}.eop-post-flow__title{font-size:26px}.eop-post-flow__badge{align-self:flex-start}.eop-post-flow__stage-intro,.eop-post-flow__acceptance-card,.eop-post-flow__contract,.eop-post-flow__panel{padding:18px}.eop-post-flow__text{font-size:23px}.eop-post-flow__checkbox{padding:14px 16px}.eop-post-flow__documents-grid{grid-template-columns:1fr}}
         </style>
         <div class="eop-proposal-wrap">
             <div class="eop-proposal-card">
@@ -188,9 +275,13 @@ class EOP_Public_Proposal {
                         <div class="eop-proposal-status">
                             <?php echo esc_html( $confirmed ? __( 'Proposta confirmada', EOP_TEXT_DOMAIN ) : __( 'Aguardando confirmacao', EOP_TEXT_DOMAIN ) ); ?>
                         </div>
+                        <?php if ( $confirmed && $flow_enabled && ! in_array( $flow_stage, array( 'inactive', 'completed' ), true ) ) : ?>
+                            <p class="eop-proposal-note"><?php echo esc_html( EOP_Post_Confirmation_Flow::get_stage_label( $flow_stage ) ); ?></p>
+                        <?php endif; ?>
                     </div>
                 </div>
 
+                <?php if ( ! $show_isolated_flow ) : ?>
                 <div class="eop-proposal-meta">
                     <p><strong><?php esc_html_e( 'Pedido', EOP_TEXT_DOMAIN ); ?>:</strong> #<?php echo esc_html( $order->get_id() ); ?></p>
                     <p><strong><?php esc_html_e( 'Cliente', EOP_TEXT_DOMAIN ); ?>:</strong> <?php echo esc_html( $customer_name ); ?></p>
@@ -238,7 +329,7 @@ class EOP_Public_Proposal {
                                                     printf(
                                                         '%1$s: %2$s',
                                                         esc_html( $column['label'] ),
-                                                        esc_html( wp_strip_all_tags( wc_price( $line_item['unit_price'] ) ) )
+                                                        esc_html( wp_strip_all_tags( wc_price( $line_item['display_unit_price'] ?? $line_item['unit_price'] ) ) )
                                                     );
                                                     ?>
                                                 </span>
@@ -248,8 +339,8 @@ class EOP_Public_Proposal {
                                                     printf(
                                                         '%1$s: %2$s (%3$s/%4$s)',
                                                         esc_html( $column['label'] ),
-                                                        esc_html( number_format_i18n( $line_item['discount_percent'], abs( $line_item['discount_percent'] - round( $line_item['discount_percent'] ) ) < 0.01 ? 0 : 2 ) . '%' ),
-                                                        esc_html( wp_strip_all_tags( wc_price( $line_item['discount_per_unit'] ) ) ),
+                                                        esc_html( number_format_i18n( $line_item['display_discount_percent'] ?? $line_item['discount_percent'], abs( ( $line_item['display_discount_percent'] ?? $line_item['discount_percent'] ) - round( $line_item['display_discount_percent'] ?? $line_item['discount_percent'] ) ) < 0.01 ? 0 : 2 ) . '%' ),
+                                                        esc_html( wp_strip_all_tags( wc_price( $line_item['display_discount_per_unit'] ?? $line_item['discount_per_unit'] ) ) ),
                                                         esc_html__( 'un.', EOP_TEXT_DOMAIN )
                                                     );
                                                     ?>
@@ -260,7 +351,7 @@ class EOP_Public_Proposal {
                                                     printf(
                                                         '%1$s: %2$s',
                                                         esc_html( $column['label'] ),
-                                                        esc_html( wp_strip_all_tags( wc_price( $line_item['discounted_unit_price'] ) ) )
+                                                        esc_html( wp_strip_all_tags( wc_price( $line_item['display_discounted_unit_price'] ?? $line_item['discounted_unit_price'] ) ) )
                                                     );
                                                     ?>
                                                 </span>
@@ -283,7 +374,7 @@ class EOP_Public_Proposal {
                             <?php if ( $show_line_total ) : ?>
                                 <div class="eop-proposal-item__summary">
                                     <span><?php echo esc_html( $item_labels['line_total'] ?? __( 'Total', EOP_TEXT_DOMAIN ) ); ?></span>
-                                    <strong><?php echo wp_kses_post( wc_price( $line_item['line_total'] ) ); ?></strong>
+                                    <strong><?php echo wp_kses_post( wc_price( $line_item['display_line_total'] ?? $line_item['line_total'] ) ); ?></strong>
                                 </div>
                             <?php endif; ?>
                         </article>
@@ -307,6 +398,7 @@ class EOP_Public_Proposal {
                         <p><?php echo esc_html( $customer_note ); ?></p>
                     </div>
                 <?php endif; ?>
+                <?php endif; ?>
 
                 <?php if ( '1' === $confirm_state ) : ?>
                     <p class="eop-proposal-note"><?php esc_html_e( 'Proposta confirmada com sucesso.', EOP_TEXT_DOMAIN ); ?></p>
@@ -325,6 +417,8 @@ class EOP_Public_Proposal {
                             </a>
                         <?php endif; ?>
                     </div>
+                <?php elseif ( $flow_enabled ) : ?>
+                    <?php echo EOP_Post_Confirmation_Flow::render_frontend_stage( $order, $line_items, $pdf_url ); ?>
                 <?php elseif ( $payment_url || $pdf_url ) : ?>
                     <div class="eop-proposal-actions">
                         <?php if ( $payment_url ) : ?>
diff --git a/includes/class-settings.php b/includes/class-settings.php
index ba0aab1..882e0cf 100644
--- a/includes/class-settings.php
+++ b/includes/class-settings.php
@@ -14,7 +14,7 @@ class EOP_Settings {
     }
 
     public static function get_defaults() {
-        return array(
+        $defaults = array(
             'flow_mode'                   => 'proposal',
             'discount_mode'               => 'both',
             'enable_checkout_confirmation' => 'no',
@@ -44,6 +44,157 @@ class EOP_Settings {
             'pdf_company_address'         => '',
             'pdf_footer_note'             => __( 'Documento gerado pelo Aireset Expresso Order.', EOP_TEXT_DOMAIN ),
         );
+
+        foreach ( self::get_post_confirmation_document_slots() as $slot ) {
+            $defaults[ 'post_confirmation_document_' . $slot . '_label' ]       = sprintf( __( 'Campo adicional %d', EOP_TEXT_DOMAIN ), $slot );
+            $defaults[ 'post_confirmation_document_' . $slot . '_placeholder' ] = '';
+        }
+
+        return array_merge(
+            $defaults,
+            array(
+                'enable_post_confirmation_flow'              => 'no',
+                'post_confirmation_contract_title'           => __( 'Leia e confirme o contrato abaixo', EOP_TEXT_DOMAIN ),
+                'post_confirmation_contract_body'            => __( 'Use este espaco para inserir o texto contratual que o cliente precisa ler e aceitar antes de continuar.', EOP_TEXT_DOMAIN ),
+                'post_confirmation_contract_checkbox_label'  => __( 'Li e aceito o contrato acima.', EOP_TEXT_DOMAIN ),
+                'post_confirmation_contract_name_label'      => __( 'Nome completo para aceite', EOP_TEXT_DOMAIN ),
+                'post_confirmation_contract_button_label'    => __( 'Confirmar e continuar', EOP_TEXT_DOMAIN ),
+                'post_confirmation_documents_title'          => __( 'Dados do pedido', EOP_TEXT_DOMAIN ),
+                'post_confirmation_signature_documents'      => array(),
+                'post_confirmation_documents_description'    => __( 'Os dados do cliente, documento e endereco sao aproveitados automaticamente do pedido WooCommerce.', EOP_TEXT_DOMAIN ),
+                'post_confirmation_documents_button_label'   => __( 'Atualizar dados', EOP_TEXT_DOMAIN ),
+                'post_confirmation_require_attachment'       => 'yes',
+                'post_confirmation_upload_title'             => __( 'Envie o arquivo solicitado', EOP_TEXT_DOMAIN ),
+                'post_confirmation_upload_description'       => __( 'Aceitamos arquivos JPG, PNG ou PDF.', EOP_TEXT_DOMAIN ),
+                'post_confirmation_upload_field_label'       => __( 'Arquivo do cliente', EOP_TEXT_DOMAIN ),
+                'post_confirmation_upload_button_label'      => __( 'Enviar arquivo', EOP_TEXT_DOMAIN ),
+                'post_confirmation_products_title'           => __( 'Personalize os nomes dos produtos', EOP_TEXT_DOMAIN ),
+                'post_confirmation_products_description'     => __( 'Informe como cada nome deve aparecer para os itens liberados.', EOP_TEXT_DOMAIN ),
+                'post_confirmation_products_button_label'    => __( 'Salvar personalizacao', EOP_TEXT_DOMAIN ),
+                'post_confirmation_locked_products'          => '',
+                'post_confirmation_completion_title'         => __( 'Etapa complementar concluida', EOP_TEXT_DOMAIN ),
+                'post_confirmation_completion_description'   => __( 'Recebemos suas informacoes e o pedido seguira para a equipe responsavel.', EOP_TEXT_DOMAIN ),
+            )
+        );
+    }
+
+    public static function get_post_confirmation_document_slots() {
+        return array();
+    }
+
+    public static function get_post_confirmation_document_fields( $settings = array() ) {
+        $settings = is_array( $settings ) ? wp_parse_args( $settings, self::get_defaults() ) : self::get_all();
+        $fields   = array();
+
+        foreach ( self::get_post_confirmation_document_slots() as $slot ) {
+            $label = trim( (string) ( $settings[ 'post_confirmation_document_' . $slot . '_label' ] ?? '' ) );
+
+            if ( '' === $label ) {
+                continue;
+            }
+
+            $fields[] = array(
+                'key'         => 'document_' . $slot,
+                'label'       => $label,
+                'placeholder' => (string) ( $settings[ 'post_confirmation_document_' . $slot . '_placeholder' ] ?? '' ),
+            );
+        }
+
+        return $fields;
+    }
+
+    public static function get_post_confirmation_signature_documents( $settings = array() ) {
+        $settings  = is_array( $settings ) ? wp_parse_args( $settings, self::get_defaults() ) : self::get_all();
+        $documents = $settings['post_confirmation_signature_documents'] ?? array();
+
+        return self::sanitize_signature_documents_collection( $documents );
+    }
+
+    public static function get_post_confirmation_contract_documents( $settings = array() ) {
+        $settings        = is_array( $settings ) ? wp_parse_args( $settings, self::get_defaults() ) : self::get_all();
+        $documents       = self::get_post_confirmation_signature_documents( $settings );
+        $legacy_contract = trim( (string) ( $settings['post_confirmation_contract_body'] ?? '' ) );
+
+        if ( '' === trim( wp_strip_all_tags( $legacy_contract ) ) ) {
+            return $documents;
+        }
+
+        array_unshift(
+            $documents,
+            array(
+                'key'           => 'contrato-principal',
+                'title'         => sanitize_text_field( $settings['post_confirmation_contract_title'] ?? __( 'Contrato principal', EOP_TEXT_DOMAIN ) ),
+                'description'   => __( 'Documento principal exibido na etapa de aceite.', EOP_TEXT_DOMAIN ),
+                'source_type'   => 'editor',
+                'body'          => wp_kses_post( $legacy_contract ),
+                'attachment_id' => 0,
+                'button_label'  => __( 'Baixar PDF', EOP_TEXT_DOMAIN ),
+                'view_label'    => __( 'Visualizar PDF', EOP_TEXT_DOMAIN ),
+            )
+        );
+
+        return self::sanitize_signature_documents_collection( $documents );
+    }
+
+    public static function get_post_confirmation_locked_product_selector_state( $settings = array() ) {
+        $settings = is_array( $settings ) ? wp_parse_args( $settings, self::get_defaults() ) : self::get_all();
+        $raw      = str_replace( array( "\r", "\n", ';' ), ',', (string) ( $settings['post_confirmation_locked_products'] ?? '' ) );
+        $tokens   = array_values( array_unique( array_filter( array_map( 'trim', explode( ',', $raw ) ) ) ) );
+        $options  = array();
+        $missing  = array();
+
+        foreach ( $tokens as $token ) {
+            $product = null;
+
+            if ( ctype_digit( $token ) ) {
+                $product = wc_get_product( absint( $token ) );
+            } else {
+                $product_ids = wc_get_products(
+                    array(
+                        'sku'    => sanitize_text_field( $token ),
+                        'limit'  => 1,
+                        'status' => array( 'publish', 'private' ),
+                        'return' => 'ids',
+                    )
+                );
+
+                if ( ! empty( $product_ids ) ) {
+                    $product = wc_get_product( (int) reset( $product_ids ) );
+                }
+            }
+
+            if ( ! $product instanceof WC_Product ) {
+                $missing[] = $token;
+                continue;
+            }
+
+            $product_id = $product->get_id();
+
+            if ( isset( $options[ $product_id ] ) ) {
+                continue;
+            }
+
+            $label = $product->get_name();
+
+            if ( $product->get_sku() ) {
+                $label .= ' [' . $product->get_sku() . ']';
+            }
+
+            if ( '' !== (string) $product->get_price() ) {
+                $label .= ' - ' . wp_strip_all_tags( wc_price( $product->get_price() ) );
+            }
+
+            $options[ $product_id ] = array(
+                'id'   => $product_id,
+                'text' => $label,
+            );
+        }
+
+        return array(
+            'options'         => array_values( $options ),
+            'missing_tokens'  => $missing,
+            'serialized_value' => implode( ',', $tokens ),
+        );
     }
 
     public static function get_all() {
@@ -75,8 +226,10 @@ class EOP_Settings {
     public static function sanitize_settings( $input ) {
         $defaults = self::get_defaults();
         $input    = is_array( $input ) ? $input : array();
+        $signature_documents           = self::sanitize_signature_documents_collection( $input['post_confirmation_signature_documents'] ?? $defaults['post_confirmation_signature_documents'] );
+        $signature_documents_submitted = array_key_exists( 'post_confirmation_signature_documents', $input );
 
-        return array(
+        $sanitized = array(
             'flow_mode'                    => in_array( $input['flow_mode'] ?? '', array( 'direct_order', 'proposal' ), true ) ? $input['flow_mode'] : $defaults['flow_mode'],
             'discount_mode'                => in_array( $input['discount_mode'] ?? '', array( 'percent', 'fixed', 'both' ), true ) ? $input['discount_mode'] : $defaults['discount_mode'],
             'enable_checkout_confirmation' => 'yes' === ( $input['enable_checkout_confirmation'] ?? 'no' ) ? 'yes' : 'no',
@@ -105,7 +258,79 @@ class EOP_Settings {
             'pdf_company_document'         => sanitize_text_field( $input['pdf_company_document'] ?? $defaults['pdf_company_document'] ),
             'pdf_company_address'          => sanitize_textarea_field( $input['pdf_company_address'] ?? $defaults['pdf_company_address'] ),
             'pdf_footer_note'              => sanitize_textarea_field( $input['pdf_footer_note'] ?? $defaults['pdf_footer_note'] ),
+            'enable_post_confirmation_flow'             => 'yes' === ( $input['enable_post_confirmation_flow'] ?? 'no' ) ? 'yes' : 'no',
+            'post_confirmation_contract_title'          => sanitize_text_field( $input['post_confirmation_contract_title'] ?? $defaults['post_confirmation_contract_title'] ),
+            'post_confirmation_contract_body'           => $signature_documents_submitted ? '' : wp_kses_post( $input['post_confirmation_contract_body'] ?? $defaults['post_confirmation_contract_body'] ),
+            'post_confirmation_contract_checkbox_label' => sanitize_text_field( $input['post_confirmation_contract_checkbox_label'] ?? $defaults['post_confirmation_contract_checkbox_label'] ),
+            'post_confirmation_contract_name_label'     => sanitize_text_field( $input['post_confirmation_contract_name_label'] ?? $defaults['post_confirmation_contract_name_label'] ),
+            'post_confirmation_contract_button_label'   => sanitize_text_field( $input['post_confirmation_contract_button_label'] ?? $defaults['post_confirmation_contract_button_label'] ),
+            'post_confirmation_signature_documents'     => $signature_documents,
+            'post_confirmation_documents_title'         => sanitize_text_field( $input['post_confirmation_documents_title'] ?? $defaults['post_confirmation_documents_title'] ),
+            'post_confirmation_documents_description'   => sanitize_textarea_field( $input['post_confirmation_documents_description'] ?? $defaults['post_confirmation_documents_description'] ),
+            'post_confirmation_documents_button_label'  => sanitize_text_field( $input['post_confirmation_documents_button_label'] ?? $defaults['post_confirmation_documents_button_label'] ),
+            'post_confirmation_require_attachment'      => 'yes' === ( $input['post_confirmation_require_attachment'] ?? 'yes' ) ? 'yes' : 'no',
+            'post_confirmation_upload_title'            => sanitize_text_field( $input['post_confirmation_upload_title'] ?? $defaults['post_confirmation_upload_title'] ),
+            'post_confirmation_upload_description'      => sanitize_textarea_field( $input['post_confirmation_upload_description'] ?? $defaults['post_confirmation_upload_description'] ),
+            'post_confirmation_upload_field_label'      => sanitize_text_field( $input['post_confirmation_upload_field_label'] ?? $defaults['post_confirmation_upload_field_label'] ),
+            'post_confirmation_upload_button_label'     => sanitize_text_field( $input['post_confirmation_upload_button_label'] ?? $defaults['post_confirmation_upload_button_label'] ),
+            'post_confirmation_products_title'          => sanitize_text_field( $input['post_confirmation_products_title'] ?? $defaults['post_confirmation_products_title'] ),
+            'post_confirmation_products_description'    => sanitize_textarea_field( $input['post_confirmation_products_description'] ?? $defaults['post_confirmation_products_description'] ),
+            'post_confirmation_products_button_label'   => sanitize_text_field( $input['post_confirmation_products_button_label'] ?? $defaults['post_confirmation_products_button_label'] ),
+            'post_confirmation_locked_products'         => sanitize_text_field( str_replace( array( "\r", "\n", ';' ), ',', (string) ( $input['post_confirmation_locked_products'] ?? $defaults['post_confirmation_locked_products'] ) ) ),
+            'post_confirmation_completion_title'        => sanitize_text_field( $input['post_confirmation_completion_title'] ?? $defaults['post_confirmation_completion_title'] ),
+            'post_confirmation_completion_description'  => sanitize_textarea_field( $input['post_confirmation_completion_description'] ?? $defaults['post_confirmation_completion_description'] ),
         );
+
+        foreach ( self::get_post_confirmation_document_slots() as $slot ) {
+            $sanitized[ 'post_confirmation_document_' . $slot . '_label' ]       = sanitize_text_field( $input[ 'post_confirmation_document_' . $slot . '_label' ] ?? $defaults[ 'post_confirmation_document_' . $slot . '_label' ] );
+            $sanitized[ 'post_confirmation_document_' . $slot . '_placeholder' ] = sanitize_text_field( $input[ 'post_confirmation_document_' . $slot . '_placeholder' ] ?? $defaults[ 'post_confirmation_document_' . $slot . '_placeholder' ] );
+        }
+
+        return $sanitized;
+    }
+
+    private static function sanitize_signature_documents_collection( $documents ) {
+        if ( ! is_array( $documents ) ) {
+            return array();
+        }
+
+        $sanitized = array();
+
+        foreach ( $documents as $index => $document ) {
+            if ( ! is_array( $document ) ) {
+                continue;
+            }
+
+            $title         = sanitize_text_field( $document['title'] ?? '' );
+            $description   = sanitize_text_field( $document['description'] ?? '' );
+            $source_type   = in_array( $document['source_type'] ?? 'editor', array( 'editor', 'attachment' ), true ) ? $document['source_type'] : 'editor';
+            $body          = wp_kses_post( $document['body'] ?? '' );
+            $attachment_id = absint( $document['attachment_id'] ?? 0 );
+            $button_label  = sanitize_text_field( $document['button_label'] ?? '' );
+            $view_label    = sanitize_text_field( $document['view_label'] ?? '' );
+            $key_source    = sanitize_title( $document['key'] ?? '' );
+
+            if ( '' === $title && '' === $description && '' === trim( wp_strip_all_tags( $body ) ) && 0 === $attachment_id ) {
+                continue;
+            }
+
+            if ( '' === $title ) {
+                $title = sprintf( __( 'Documento %d', EOP_TEXT_DOMAIN ), count( $sanitized ) + 1 );
+            }
+
+            $sanitized[] = array(
+                'key'           => '' !== $key_source ? $key_source : 'documento-' . ( $index + 1 ),
+                'title'         => $title,
+                'description'   => $description,
+                'source_type'   => $source_type,
+                'body'          => $body,
+                'attachment_id' => $attachment_id,
+                'button_label'  => '' !== $button_label ? $button_label : __( 'Baixar PDF', EOP_TEXT_DOMAIN ),
+                'view_label'    => '' !== $view_label ? $view_label : __( 'Visualizar PDF', EOP_TEXT_DOMAIN ),
+            );
+        }
+
+        return array_values( $sanitized );
     }
 
     private static function sanitize_color( $value, $fallback ) {
@@ -149,6 +374,21 @@ class EOP_Settings {
         return 'https://fonts.googleapis.com/css?family=' . str_replace( '%2B', '+', rawurlencode( $font_value ) ) . '&display=swap';
     }
 
+    public static function get_font_stylesheet_urls( $font_values = array() ) {
+        $font_values = is_array( $font_values ) ? $font_values : array( $font_values );
+        $urls        = array();
+
+        foreach ( $font_values as $font_value ) {
+            $url = self::get_font_stylesheet_url( (string) $font_value );
+
+            if ( '' !== $url ) {
+                $urls[ $url ] = $url;
+            }
+        }
+
+        return array_values( $urls );
+    }
+
     public static function register_submenu() {
         // self::$page_hook = add_submenu_page(
         //     'aireset',
@@ -172,11 +412,19 @@ class EOP_Settings {
         }
 
         wp_enqueue_media();
+        wp_enqueue_editor();
+
+        if ( function_exists( 'WC' ) && WC() ) {
+            $wc_version = defined( 'WC_VERSION' ) ? WC_VERSION : EOP_VERSION;
+            wp_enqueue_style( 'select2', WC()->plugin_url() . '/assets/css/select2.css', array(), $wc_version );
+            wp_enqueue_script( 'select2', WC()->plugin_url() . '/assets/js/select2/select2.full.min.js', array( 'jquery' ), $wc_version, true );
+        }
+
         wp_enqueue_style( 'eop-coloris', EOP_PLUGIN_URL . 'assets/css/coloris.min.css', array(), EOP_VERSION );
         wp_enqueue_style(
             'eop-settings-admin',
             EOP_PLUGIN_URL . 'assets/css/settings-admin.css',
-            array( 'eop-coloris' ),
+            array_filter( array( 'eop-coloris', wp_style_is( 'select2', 'registered' ) ? 'select2' : '' ) ),
             EOP_VERSION
         );
 
@@ -204,7 +452,7 @@ class EOP_Settings {
         wp_enqueue_script(
             'eop-settings-admin',
             EOP_PLUGIN_URL . 'assets/js/settings-admin.js',
-            array_filter( array( 'jquery', 'eop-coloris', 'media-editor', 'media-upload', file_exists( $font_js_path ) ? 'eop-fontselect' : '' ) ),
+            array_filter( array( 'jquery', 'eop-coloris', 'media-editor', 'media-upload', 'wp-editor', wp_script_is( 'select2', 'registered' ) ? 'select2' : '', file_exists( $font_js_path ) ? 'eop-fontselect' : '' ) ),
             EOP_VERSION,
             true
         );
@@ -224,6 +472,25 @@ class EOP_Settings {
                 'color_default'    => __( 'Padrao', EOP_TEXT_DOMAIN ),
                 'color_clear'      => __( 'Limpar', EOP_TEXT_DOMAIN ),
                 'color_close'      => __( 'Fechar', EOP_TEXT_DOMAIN ),
+                'ajax_url'         => admin_url( 'admin-ajax.php' ),
+                'nonce'            => wp_create_nonce( 'eop_nonce' ),
+                'locked_placeholder' => __( 'Busque produtos por nome ou SKU...', EOP_TEXT_DOMAIN ),
+                'locked_no_results'  => __( 'Nenhum produto encontrado.', EOP_TEXT_DOMAIN ),
+                'document_add'       => __( 'Adicionar documento', EOP_TEXT_DOMAIN ),
+                'document_remove'    => __( 'Remover documento', EOP_TEXT_DOMAIN ),
+                'document_media_title' => __( 'Selecionar PDF do documento', EOP_TEXT_DOMAIN ),
+                'document_media_button' => __( 'Usar este PDF', EOP_TEXT_DOMAIN ),
+                'document_pdf_empty' => __( 'Nenhum PDF anexado ainda.', EOP_TEXT_DOMAIN ),
+                'document_editor_placeholder' => __( 'Escreva ou cole aqui o texto completo do documento. A formatacao sera preservada no PDF gerado.', EOP_TEXT_DOMAIN ),
+                'document_type_editor' => __( 'Texto formatado', EOP_TEXT_DOMAIN ),
+                'document_type_attachment' => __( 'PDF anexado', EOP_TEXT_DOMAIN ),
+                'document_visual_label' => __( 'Visualizar no fluxo', EOP_TEXT_DOMAIN ),
+                'document_download_label' => __( 'Botao de download', EOP_TEXT_DOMAIN ),
+                'document_title_label' => __( 'Titulo do documento', EOP_TEXT_DOMAIN ),
+                'document_description_label' => __( 'Descricao curta', EOP_TEXT_DOMAIN ),
+                'document_source_label' => __( 'Origem do documento', EOP_TEXT_DOMAIN ),
+                'document_body_label' => __( 'Conteudo do documento', EOP_TEXT_DOMAIN ),
+                'document_attachment_label' => __( 'Arquivo PDF base', EOP_TEXT_DOMAIN ),
             )
         );
 
@@ -236,13 +503,39 @@ class EOP_Settings {
     public static function filter_admin_body_class( $classes ) {
         $page = isset( $_GET['page'] ) ? sanitize_key( wp_unslash( $_GET['page'] ) ) : '';
 
-        if ( 'eop-configuracoes' !== $page ) {
+        if ( ! in_array( $page, array( 'eop-configuracoes', 'eop-pedido-expresso-configuracoes', 'eop-pedido-expresso-aparencia', 'eop-pedido-expresso-fluxo' ), true ) ) {
             return $classes;
         }
 
         return trim( $classes . ' eop-settings-screen' );
     }
 
+    private static function normalize_admin_section( $section ) {
+        $section = sanitize_key( (string) $section );
+        $allowed = array( 'all', 'general', 'appearance', 'post-confirmation' );
+
+        return in_array( $section, $allowed, true ) ? $section : 'all';
+    }
+
+    private static function should_render_admin_section( $active_section, $section_key ) {
+        $active_section = self::normalize_admin_section( $active_section );
+        $section_key    = sanitize_key( (string) $section_key );
+
+        if ( 'all' === $active_section ) {
+            return true;
+        }
+
+        if ( 'post-confirmation' === $active_section ) {
+            return 'post-confirmation' === $section_key;
+        }
+
+        if ( 'appearance' === $active_section ) {
+            return 'appearance' === $section_key;
+        }
+
+        return 'general' === $section_key;
+    }
+
     public static function render_page() {
         if ( class_exists( 'EOP_Admin_Page' ) ) {
             wp_safe_redirect( EOP_Admin_Page::get_view_url( 'settings' ) );
@@ -256,13 +549,16 @@ class EOP_Settings {
         self::render_standalone_page();
     }
 
-    public static function render_embedded_page() {
+    public static function render_embedded_page( $section = 'all' ) {
         if ( ! current_user_can( 'manage_options' ) ) {
             wp_die( esc_html__( 'Acesso negado.', EOP_TEXT_DOMAIN ) );
         }
 
+        $section  = self::normalize_admin_section( $section );
         $settings = self::get_all();
         $pages    = get_pages();
+        $locked_selector = self::get_post_confirmation_locked_product_selector_state( $settings );
+        $signature_documents = self::get_post_confirmation_contract_documents( $settings );
         ?>
         <div class="eop-settings-page eop-settings-page--embedded">
             <form method="post" action="options.php" class="eop-settings-form">
@@ -273,6 +569,7 @@ class EOP_Settings {
                     </div>
                 <?php endif; ?>
                 <div class="eop-settings-sections">
+                        <?php if ( self::should_render_admin_section( $section, 'general' ) ) : ?>
                         <section class="eop-settings-card">
                             <h2><?php esc_html_e( 'Fluxo', EOP_TEXT_DOMAIN ); ?></h2>
                             <p><?php esc_html_e( 'Defina como a equipe comercial trabalha hoje e como o cliente recebe a proposta.', EOP_TEXT_DOMAIN ); ?></p>
@@ -339,7 +636,9 @@ class EOP_Settings {
                                 </div>
                             </div>
                         </section>
+                        <?php endif; ?>
 
+                        <?php if ( self::should_render_admin_section( $section, 'appearance' ) ) : ?>
                         <section class="eop-settings-card">
                             <h2><?php esc_html_e( 'Visual', EOP_TEXT_DOMAIN ); ?></h2>
                             <p><?php esc_html_e( 'Personalize a experiencia da equipe e do cliente com a identidade da marca.', EOP_TEXT_DOMAIN ); ?></p>
@@ -392,7 +691,9 @@ class EOP_Settings {
                                 </div>
                             </div>
                         </section>
+                        <?php endif; ?>
 
+                        <?php if ( self::should_render_admin_section( $section, 'appearance' ) ) : ?>
                         <section class="eop-settings-card">
                             <h2><?php esc_html_e( 'Visual da pagina do cliente', EOP_TEXT_DOMAIN ); ?></h2>
                             <p><?php esc_html_e( 'Deixe a proposta publica com uma identidade propria, sem depender do visual interno do vendedor.', EOP_TEXT_DOMAIN ); ?></p>
@@ -427,7 +728,259 @@ class EOP_Settings {
                                 </div>
                             </div>
                         </section>
+                        <?php endif; ?>
+
+                        <?php if ( self::should_render_admin_section( $section, 'post-confirmation' ) ) : ?>
+                        <section class="eop-settings-card">
+                            <h2><?php esc_html_e( 'Fluxo complementar apos a proposta', EOP_TEXT_DOMAIN ); ?></h2>
+                            <p><?php esc_html_e( 'Ative uma jornada adicional para contrato, dados do pedido, anexo e personalizacao dos produtos depois da confirmacao da proposta.', EOP_TEXT_DOMAIN ); ?></p>
+                            <div class="eop-settings-grid">
+                                <div class="eop-settings-field is-full">
+                                    <span><?php esc_html_e( 'Ativar fluxo complementar', EOP_TEXT_DOMAIN ); ?></span>
+                                    <div class="eop-settings-switch-shell">
+                                        <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[enable_post_confirmation_flow]" value="<?php echo esc_attr( $settings['enable_post_confirmation_flow'] ); ?>" />
+                                        <button
+                                            type="button"
+                                            class="eop-settings-switcher<?php echo 'yes' === $settings['enable_post_confirmation_flow'] ? ' is-enabled' : ''; ?>"
+                                            role="switch"
+                                            aria-checked="<?php echo 'yes' === $settings['enable_post_confirmation_flow'] ? 'true' : 'false'; ?>"
+                                            data-target-name="<?php echo esc_attr( self::OPTION_KEY ); ?>[enable_post_confirmation_flow]"
+                                            data-enabled-value="yes"
+                                            data-disabled-value="no"
+                                            aria-label="<?php esc_attr_e( 'Alternar fluxo complementar', EOP_TEXT_DOMAIN ); ?>"
+                                        >
+                                            <span class="eop-settings-switcher__label eop-settings-switcher__label--off">Off</span>
+                                            <span class="eop-settings-switcher__thumb" aria-hidden="true"></span>
+                                            <span class="eop-settings-switcher__label eop-settings-switcher__label--on">On</span>
+                                        </button>
+                                        <span class="eop-settings-switcher__status" aria-live="polite">
+                                            <?php echo 'yes' === $settings['enable_post_confirmation_flow'] ? esc_html__( 'Ativado', EOP_TEXT_DOMAIN ) : esc_html__( 'Desativado', EOP_TEXT_DOMAIN ); ?>
+                                        </span>
+                                    </div>
+                                    <small class="eop-settings-help"><?php esc_html_e( 'Quando ativo, o cliente passa por contrato, uso automatico dos dados do pedido, anexo e personalizacao dos produtos apos confirmar a proposta.', EOP_TEXT_DOMAIN ); ?></small>
+                                </div>
+
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_contract_title"><?php esc_html_e( 'Titulo do contrato', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_contract_title" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_contract_title]" value="<?php echo esc_attr( $settings['post_confirmation_contract_title'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_contract_name_label"><?php esc_html_e( 'Label do nome completo', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_contract_name_label" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_contract_name_label]" value="<?php echo esc_attr( $settings['post_confirmation_contract_name_label'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field is-full">
+                                        <label><?php esc_html_e( 'Textos e documentos do contrato', EOP_TEXT_DOMAIN ); ?></label>
+                                        <div class="eop-signature-documents" data-signature-documents data-option-key="<?php echo esc_attr( self::OPTION_KEY ); ?>" data-next-index="<?php echo esc_attr( count( $signature_documents ) ); ?>">
+                                            <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__sentinel]" value="1" />
+
+                                            <div class="eop-signature-documents__list" data-signature-documents-list>
+                                                <?php foreach ( $signature_documents as $index => $document ) : ?>
+                                                    <?php $attachment_name = ! empty( $document['attachment_id'] ) ? get_the_title( $document['attachment_id'] ) : ''; ?>
+                                                    <div class="eop-signature-document" data-signature-document data-index="<?php echo esc_attr( $index ); ?>">
+                                                        <div class="eop-signature-document__header">
+                                                            <strong><?php echo esc_html( $document['title'] ); ?></strong>
+                                                            <button type="button" class="button-link-delete" data-signature-document-remove><?php esc_html_e( 'Remover documento', EOP_TEXT_DOMAIN ); ?></button>
+                                                        </div>
+                                                        <div class="eop-signature-document__grid">
+                                                            <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][key]" value="<?php echo esc_attr( $document['key'] ); ?>" />
+                                                            <div class="eop-settings-field">
+                                                                <label><?php esc_html_e( 'Titulo do documento', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][title]" value="<?php echo esc_attr( $document['title'] ); ?>" />
+                                                            </div>
+                                                            <div class="eop-settings-field">
+                                                                <label><?php esc_html_e( 'Origem do documento', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <select name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][source_type]" data-signature-document-source>
+                                                                    <option value="editor" <?php selected( $document['source_type'], 'editor' ); ?>><?php esc_html_e( 'Texto formatado', EOP_TEXT_DOMAIN ); ?></option>
+                                                                    <option value="attachment" <?php selected( $document['source_type'], 'attachment' ); ?>><?php esc_html_e( 'PDF anexado', EOP_TEXT_DOMAIN ); ?></option>
+                                                                </select>
+                                                            </div>
+                                                            <div class="eop-settings-field is-full">
+                                                                <label><?php esc_html_e( 'Descricao curta', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][description]" value="<?php echo esc_attr( $document['description'] ); ?>" />
+                                                            </div>
+                                                            <div class="eop-settings-field">
+                                                                <label><?php esc_html_e( 'Botao de visualizacao', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][view_label]" value="<?php echo esc_attr( $document['view_label'] ); ?>" />
+                                                            </div>
+                                                            <div class="eop-settings-field">
+                                                                <label><?php esc_html_e( 'Botao de download', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][button_label]" value="<?php echo esc_attr( $document['button_label'] ); ?>" />
+                                                            </div>
+                                                            <div class="eop-settings-field is-full eop-signature-document__panel<?php echo 'attachment' === $document['source_type'] ? '' : ' is-hidden'; ?>" data-signature-document-panel="attachment">
+                                                                <label><?php esc_html_e( 'Arquivo PDF base', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][attachment_id]" value="<?php echo esc_attr( absint( $document['attachment_id'] ) ); ?>" data-signature-document-attachment-id />
+                                                                <div class="eop-signature-document__attachment-shell">
+                                                                    <div class="eop-signature-document__attachment-name" data-signature-document-attachment-name><?php echo esc_html( $attachment_name ? $attachment_name : __( 'Nenhum PDF anexado ainda.', EOP_TEXT_DOMAIN ) ); ?></div>
+                                                                    <div class="eop-signature-document__attachment-actions">
+                                                                        <button type="button" class="button button-secondary" data-signature-document-attachment-select><?php echo $attachment_name ? esc_html__( 'Trocar PDF', EOP_TEXT_DOMAIN ) : esc_html__( 'Selecionar PDF', EOP_TEXT_DOMAIN ); ?></button>
+                                                                        <button type="button" class="button button-link-delete<?php echo $attachment_name ? '' : ' is-hidden'; ?>" data-signature-document-attachment-remove><?php esc_html_e( 'Remover PDF', EOP_TEXT_DOMAIN ); ?></button>
+                                                                    </div>
+                                                                </div>
+                                                            </div>
+                                                            <div class="eop-settings-field is-full eop-signature-document__panel<?php echo 'editor' === $document['source_type'] ? '' : ' is-hidden'; ?>" data-signature-document-panel="editor">
+                                                                <label><?php esc_html_e( 'Conteudo do documento', EOP_TEXT_DOMAIN ); ?></label>
+                                                                <textarea id="eop_signature_document_body_<?php echo esc_attr( $index ); ?>" class="eop-signature-document__editor" data-signature-document-editor name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][<?php echo esc_attr( $index ); ?>][body]"><?php echo esc_textarea( $document['body'] ); ?></textarea>
+                                                            </div>
+                                                        </div>
+                                                    </div>
+                                                <?php endforeach; ?>
+                                            </div>
+
+                                            <button type="button" class="button button-secondary eop-signature-documents__add" data-signature-document-add><?php esc_html_e( 'Adicionar documento', EOP_TEXT_DOMAIN ); ?></button>
+                                        </div>
+                                        <script type="text/template" id="eop-signature-document-template">
+                                            <div class="eop-signature-document" data-signature-document data-index="__INDEX__">
+                                                <div class="eop-signature-document__header">
+                                                    <strong><?php esc_html_e( 'Novo documento', EOP_TEXT_DOMAIN ); ?></strong>
+                                                    <button type="button" class="button-link-delete" data-signature-document-remove><?php esc_html_e( 'Remover documento', EOP_TEXT_DOMAIN ); ?></button>
+                                                </div>
+                                                <div class="eop-signature-document__grid">
+                                                    <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][key]" value="" />
+                                                    <div class="eop-settings-field">
+                                                        <label><?php esc_html_e( 'Titulo do documento', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][title]" value="" />
+                                                    </div>
+                                                    <div class="eop-settings-field">
+                                                        <label><?php esc_html_e( 'Origem do documento', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <select name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][source_type]" data-signature-document-source>
+                                                            <option value="editor"><?php esc_html_e( 'Texto formatado', EOP_TEXT_DOMAIN ); ?></option>
+                                                            <option value="attachment"><?php esc_html_e( 'PDF anexado', EOP_TEXT_DOMAIN ); ?></option>
+                                                        </select>
+                                                    </div>
+                                                    <div class="eop-settings-field is-full">
+                                                        <label><?php esc_html_e( 'Descricao curta', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][description]" value="" />
+                                                    </div>
+                                                    <div class="eop-settings-field">
+                                                        <label><?php esc_html_e( 'Botao de visualizacao', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][view_label]" value="<?php echo esc_attr__( 'Visualizar PDF', EOP_TEXT_DOMAIN ); ?>" />
+                                                    </div>
+                                                    <div class="eop-settings-field">
+                                                        <label><?php esc_html_e( 'Botao de download', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <input type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][button_label]" value="<?php echo esc_attr__( 'Baixar PDF', EOP_TEXT_DOMAIN ); ?>" />
+                                                    </div>
+                                                    <div class="eop-settings-field is-full eop-signature-document__panel is-hidden" data-signature-document-panel="attachment">
+                                                        <label><?php esc_html_e( 'Arquivo PDF base', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][attachment_id]" value="" data-signature-document-attachment-id />
+                                                        <div class="eop-signature-document__attachment-shell">
+                                                            <div class="eop-signature-document__attachment-name" data-signature-document-attachment-name><?php esc_html_e( 'Nenhum PDF anexado ainda.', EOP_TEXT_DOMAIN ); ?></div>
+                                                            <div class="eop-signature-document__attachment-actions">
+                                                                <button type="button" class="button button-secondary" data-signature-document-attachment-select><?php esc_html_e( 'Selecionar PDF', EOP_TEXT_DOMAIN ); ?></button>
+                                                                <button type="button" class="button button-link-delete is-hidden" data-signature-document-attachment-remove><?php esc_html_e( 'Remover PDF', EOP_TEXT_DOMAIN ); ?></button>
+                                                            </div>
+                                                        </div>
+                                                    </div>
+                                                    <div class="eop-settings-field is-full eop-signature-document__panel" data-signature-document-panel="editor">
+                                                        <label><?php esc_html_e( 'Conteudo do documento', EOP_TEXT_DOMAIN ); ?></label>
+                                                        <textarea id="eop_signature_document_body___INDEX__" class="eop-signature-document__editor" data-signature-document-editor name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_signature_documents][__INDEX__][body]"></textarea>
+                                                    </div>
+                                                </div>
+                                            </div>
+                                        </script>
+                                        <small class="eop-settings-help">
+                                            <?php esc_html_e( 'Monte quantos textos ou PDFs quiser. Cada item vira um PDF do contrato e fica salvo no pedido para leitura, download e assinatura externa.', EOP_TEXT_DOMAIN ); ?>
+                                        </small>
+                                        <?php if ( class_exists( 'EOP_Post_Confirmation_Flow' ) && method_exists( 'EOP_Post_Confirmation_Flow', 'get_contract_placeholder_tokens' ) ) : ?>
+                                            <small class="eop-settings-help">
+                                                <?php
+                                                printf(
+                                                    esc_html__( 'Placeholders disponiveis: %s', EOP_TEXT_DOMAIN ),
+                                                    esc_html( implode( ', ', EOP_Post_Confirmation_Flow::get_contract_placeholder_tokens() ) )
+                                                );
+                                                ?>
+                                            </small>
+                                        <?php endif; ?>
+                                    </div>
+                                <div class="eop-settings-field is-full">
+                                    <label for="eop_post_confirmation_contract_checkbox_label"><?php esc_html_e( 'Texto do aceite', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_contract_checkbox_label" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_contract_checkbox_label]" value="<?php echo esc_attr( $settings['post_confirmation_contract_checkbox_label'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_contract_button_label"><?php esc_html_e( 'Botao do contrato', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_contract_button_label" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_contract_button_label]" value="<?php echo esc_attr( $settings['post_confirmation_contract_button_label'] ); ?>" />
+                                </div>
+
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_locked_products"><?php esc_html_e( 'Produtos bloqueados', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_locked_products" type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_locked_products]" value="<?php echo esc_attr( $locked_selector['serialized_value'] ); ?>" />
+                                    <select id="eop_post_confirmation_locked_products_selector" class="eop-settings-product-selector" data-target-input="#eop_post_confirmation_locked_products" multiple>
+                                        <?php foreach ( $locked_selector['options'] as $option ) : ?>
+                                            <option value="<?php echo esc_attr( $option['id'] ); ?>" selected="selected"><?php echo esc_html( $option['text'] ); ?></option>
+                                        <?php endforeach; ?>
+                                    </select>
+                                    <small class="eop-settings-help"><?php esc_html_e( 'Busque produtos por nome ou SKU para bloquear a alteracao do nome na etapa final.', EOP_TEXT_DOMAIN ); ?></small>
+                                    <?php if ( ! empty( $locked_selector['missing_tokens'] ) ) : ?>
+                                        <small class="eop-settings-help"><?php echo esc_html( sprintf( __( 'Tokens antigos preservados ate a proxima atualizacao desta lista: %s', EOP_TEXT_DOMAIN ), implode( ', ', $locked_selector['missing_tokens'] ) ) ); ?></small>
+                                    <?php endif; ?>
+                                </div>
+
+                                <div class="eop-settings-field is-full">
+                                    <span><?php esc_html_e( 'Exigir anexo', EOP_TEXT_DOMAIN ); ?></span>
+                                    <div class="eop-settings-switch-shell">
+                                        <input type="hidden" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_require_attachment]" value="<?php echo esc_attr( $settings['post_confirmation_require_attachment'] ); ?>" />
+                                        <button
+                                            type="button"
+                                            class="eop-settings-switcher<?php echo 'yes' === $settings['post_confirmation_require_attachment'] ? ' is-enabled' : ''; ?>"
+                                            role="switch"
+                                            aria-checked="<?php echo 'yes' === $settings['post_confirmation_require_attachment'] ? 'true' : 'false'; ?>"
+                                            data-target-name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_require_attachment]"
+                                            data-enabled-value="yes"
+                                            data-disabled-value="no"
+                                            aria-label="<?php esc_attr_e( 'Alternar anexo obrigatorio', EOP_TEXT_DOMAIN ); ?>"
+                                        >
+                                            <span class="eop-settings-switcher__label eop-settings-switcher__label--off">Off</span>
+                                            <span class="eop-settings-switcher__thumb" aria-hidden="true"></span>
+                                            <span class="eop-settings-switcher__label eop-settings-switcher__label--on">On</span>
+                                        </button>
+                                        <span class="eop-settings-switcher__status" aria-live="polite">
+                                            <?php echo 'yes' === $settings['post_confirmation_require_attachment'] ? esc_html__( 'Obrigatorio', EOP_TEXT_DOMAIN ) : esc_html__( 'Opcional', EOP_TEXT_DOMAIN ); ?>
+                                        </span>
+                                    </div>
+                                </div>
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_upload_title"><?php esc_html_e( 'Titulo do anexo', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_upload_title" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_upload_title]" value="<?php echo esc_attr( $settings['post_confirmation_upload_title'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_upload_field_label"><?php esc_html_e( 'Label do campo de arquivo', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_upload_field_label" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_upload_field_label]" value="<?php echo esc_attr( $settings['post_confirmation_upload_field_label'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field is-full">
+                                    <label for="eop_post_confirmation_upload_description"><?php esc_html_e( 'Descricao do anexo', EOP_TEXT_DOMAIN ); ?></label>
+                                    <textarea id="eop_post_confirmation_upload_description" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_upload_description]"><?php echo esc_textarea( $settings['post_confirmation_upload_description'] ); ?></textarea>
+                                </div>
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_upload_button_label"><?php esc_html_e( 'Botao do anexo', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_upload_button_label" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_upload_button_label]" value="<?php echo esc_attr( $settings['post_confirmation_upload_button_label'] ); ?>" />
+                                </div>
+
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_products_title"><?php esc_html_e( 'Titulo da personalizacao', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_products_title" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_products_title]" value="<?php echo esc_attr( $settings['post_confirmation_products_title'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_products_button_label"><?php esc_html_e( 'Botao da personalizacao', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_products_button_label" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_products_button_label]" value="<?php echo esc_attr( $settings['post_confirmation_products_button_label'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field is-full">
+                                    <label for="eop_post_confirmation_products_description"><?php esc_html_e( 'Descricao da personalizacao', EOP_TEXT_DOMAIN ); ?></label>
+                                    <textarea id="eop_post_confirmation_products_description" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_products_description]"><?php echo esc_textarea( $settings['post_confirmation_products_description'] ); ?></textarea>
+                                </div>
+
+                                <div class="eop-settings-field">
+                                    <label for="eop_post_confirmation_completion_title"><?php esc_html_e( 'Titulo da conclusao', EOP_TEXT_DOMAIN ); ?></label>
+                                    <input id="eop_post_confirmation_completion_title" type="text" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_completion_title]" value="<?php echo esc_attr( $settings['post_confirmation_completion_title'] ); ?>" />
+                                </div>
+                                <div class="eop-settings-field is-full">
+                                    <label for="eop_post_confirmation_completion_description"><?php esc_html_e( 'Descricao da conclusao', EOP_TEXT_DOMAIN ); ?></label>
+                                    <textarea id="eop_post_confirmation_completion_description" name="<?php echo esc_attr( self::OPTION_KEY ); ?>[post_confirmation_completion_description]"><?php echo esc_textarea( $settings['post_confirmation_completion_description'] ); ?></textarea>
+                                </div>
+
+                            </div>
+                        </section>
+                        <?php endif; ?>
 
+                        <?php if ( self::should_render_admin_section( $section, 'general' ) ) : ?>
                         <section class="eop-settings-card">
                             <h2><?php esc_html_e( 'PDF nativo', EOP_TEXT_DOMAIN ); ?></h2>
                             <p><?php esc_html_e( 'Defina os dados exibidos pelo gerador interno de PDF do plugin.', EOP_TEXT_DOMAIN ); ?></p>
@@ -450,7 +1003,9 @@ class EOP_Settings {
                                 </div>
                             </div>
                         </section>
+                        <?php endif; ?>
 
+                        <?php if ( self::should_render_admin_section( $section, 'appearance' ) ) : ?>
                         <section class="eop-settings-card">
                             <h2><?php esc_html_e( 'Textos', EOP_TEXT_DOMAIN ); ?></h2>
                             <p><?php esc_html_e( 'Refine a narrativa do painel de vendedor e da proposta publica.', EOP_TEXT_DOMAIN ); ?></p>
@@ -481,8 +1036,9 @@ class EOP_Settings {
                                 </div>
                             </div>
                         </section>
+                        <?php endif; ?>
                 </div>
-                <div class="eop-settings-submit">
+                <div class="eop-settings-submit eop-admin-submitbar">
                     <?php submit_button( __( 'Salvar alteracoes', EOP_TEXT_DOMAIN ), 'primary large', 'submit', false ); ?>
                 </div>
             </form>
diff --git a/package.json b/package.json
index 5ca5b9c..51e66b4 100644
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "aireset-expresso-order",
-  "version": "1.1.48",
+  "version": "1.1.59",
   "private": true,
   "scripts": {
     "build:css": "sass.cmd assets/scss/frontend/style.scss assets/css/frontend.css --style=expanded --no-source-map && sass.cmd assets/scss/admin/style.scss assets/css/admin.css --style=expanded --no-source-map && sass.cmd assets/scss/admin/settings.scss assets/css/settings-admin.css --style=expanded --no-source-map",
diff --git a/templates/admin-page.php b/templates/admin-page.php
index 85528d0..0c1dca4 100644
--- a/templates/admin-page.php
+++ b/templates/admin-page.php
@@ -4,7 +4,7 @@ defined( 'ABSPATH' ) || exit;
 $settings            = EOP_Settings::get_all();
 $font_css            = method_exists( 'EOP_Settings', 'get_font_css_family' ) ? EOP_Settings::get_font_css_family( $settings['font_family'] ) : "'Segoe UI', sans-serif";
 $order_statuses      = function_exists( 'wc_get_order_statuses' ) ? wc_get_order_statuses() : array();
-$initial_view        = EOP_Admin_Page::normalize_view( isset( $_GET['view'] ) ? wp_unslash( $_GET['view'] ) : '' );
+$initial_view        = class_exists( 'EOP_Admin_Page' ) ? EOP_Admin_Page::normalize_view( isset( $_GET['view'] ) ? wp_unslash( $_GET['view'] ) : '' ) : 'new-order';
 $license_manager     = class_exists( 'EOP_License_Manager' ) ? EOP_License_Manager::get_instance() : null;
 $can_manage_settings = current_user_can( 'manage_options' );
 $pdf_tabs            = class_exists( 'EOP_PDF_Admin_Page' ) ? EOP_PDF_Admin_Page::get_accessible_tabs() : array();
@@ -13,6 +13,13 @@ $pdf_preview_args    = array(
     'document'      => isset( $_GET['document'] ) && 'proposal' === sanitize_key( wp_unslash( $_GET['document'] ) ) ? 'proposal' : 'order',
     'preview_order' => absint( $_GET['preview_order'] ?? 0 ),
 );
+$is_operation_page   = 'new-order' === $initial_view;
+$is_orders_page      = 'orders' === $initial_view;
+$is_pdf_page         = 'pdf' === $initial_view;
+$is_settings_page    = 'settings' === $initial_view;
+$is_appearance_page  = 'appearance' === $initial_view;
+$is_post_flow_page   = 'post-confirmation' === $initial_view;
+$is_license_page     = 'license' === $initial_view;
 ?>
 <style>
     .eop-admin-spa {
@@ -41,34 +48,34 @@ $pdf_preview_args    = array(
             </div>
 
             <nav class="eop-admin-spa__nav" aria-label="<?php esc_attr_e( 'Navegacao do admin do Pedido Expresso', EOP_TEXT_DOMAIN ); ?>">
-                <button type="button" class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo 'new-order' === $initial_view ? ' is-active' : ''; ?>" data-eop-view-target="new-order" aria-selected="<?php echo 'new-order' === $initial_view ? 'true' : 'false'; ?>">
+                <a class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo $is_operation_page ? ' is-active' : ''; ?>" href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'new-order' ) ); ?>" data-eop-view-target="new-order" aria-selected="<?php echo $is_operation_page ? 'true' : 'false'; ?>">
                     <span class="eop-admin-spa-nav__icon dashicons dashicons-cart" aria-hidden="true"></span>
-                    <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Novo pedido', EOP_TEXT_DOMAIN ); ?></span>
-                </button>
-                <button type="button" class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo 'orders' === $initial_view ? ' is-active' : ''; ?>" data-eop-view-target="orders" aria-selected="<?php echo 'orders' === $initial_view ? 'true' : 'false'; ?>">
+                    <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Operacao', EOP_TEXT_DOMAIN ); ?></span>
+                </a>
+                <a class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo $is_orders_page ? ' is-active' : ''; ?>" href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'orders' ) ); ?>" data-eop-view-target="orders" aria-selected="<?php echo $is_orders_page ? 'true' : 'false'; ?>">
                     <span class="eop-admin-spa-nav__icon dashicons dashicons-list-view" aria-hidden="true"></span>
                     <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Pedidos', EOP_TEXT_DOMAIN ); ?></span>
-                </button>
+                </a>
                 <?php if ( ! empty( $pdf_tabs ) ) : ?>
-                    <div class="eop-admin-spa-nav__group eop-admin-spa-nav__group--pdf<?php echo 'pdf' === $initial_view ? ' is-open' : ''; ?>">
+                    <div class="eop-admin-spa-nav__group eop-admin-spa-nav__group--pdf<?php echo $is_pdf_page ? ' is-open' : ''; ?>">
                         <button
                             type="button"
-                            class="eop-pdv-nav__item eop-admin-spa-nav__item eop-admin-spa-nav__group-toggle<?php echo 'pdf' === $initial_view ? ' is-active' : ''; ?>"
+                            class="eop-pdv-nav__item eop-admin-spa-nav__item eop-admin-spa-nav__group-toggle<?php echo $is_pdf_page ? ' is-active' : ''; ?>"
                             data-eop-view-target="pdf"
                             data-eop-nav-toggle="pdf"
-                            aria-selected="<?php echo 'pdf' === $initial_view ? 'true' : 'false'; ?>"
-                            aria-expanded="<?php echo 'pdf' === $initial_view ? 'true' : 'false'; ?>"
+                            aria-selected="<?php echo $is_pdf_page ? 'true' : 'false'; ?>"
+                            aria-expanded="<?php echo $is_pdf_page ? 'true' : 'false'; ?>"
                         >
                             <span class="eop-admin-spa-nav__icon dashicons dashicons-media-document" aria-hidden="true"></span>
                             <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'PDF', EOP_TEXT_DOMAIN ); ?></span>
                             <span class="eop-admin-spa-nav__group-arrow dashicons dashicons-arrow-down-alt2" aria-hidden="true"></span>
                         </button>
 
-                        <div class="eop-admin-spa-nav__submenu"<?php echo 'pdf' === $initial_view ? '' : ' hidden'; ?>>
+                        <div class="eop-admin-spa-nav__submenu"<?php echo $is_pdf_page ? '' : ' hidden'; ?>>
                             <?php foreach ( $pdf_tabs as $pdf_tab_key => $pdf_tab_label ) : ?>
                                 <a
-                                    class="eop-admin-spa-nav__submenu-item<?php echo 'pdf' === $initial_view && $current_pdf_tab === $pdf_tab_key ? ' is-active' : ''; ?>"
-                                    href="<?php echo esc_url( EOP_PDF_Admin_Page::get_tab_url( $pdf_tab_key, $pdf_preview_args ) ); ?>"
+                                    class="eop-admin-spa-nav__submenu-item<?php echo $is_pdf_page && $current_pdf_tab === $pdf_tab_key ? ' is-active' : ''; ?>"
+                                    href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'pdf', array_merge( $pdf_preview_args, array( 'pdf_tab' => $pdf_tab_key ) ) ) ); ?>"
                                     data-eop-pdf-tab="<?php echo esc_attr( $pdf_tab_key ); ?>"
                                 >
                                     <span class="eop-admin-spa-nav__submenu-label"><?php echo esc_html( $pdf_tab_label ); ?></span>
@@ -76,21 +83,24 @@ $pdf_preview_args    = array(
                             <?php endforeach; ?>
                         </div>
                     </div>
-                <?php else : ?>
-                    <button type="button" class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo 'pdf' === $initial_view ? ' is-active' : ''; ?>" data-eop-view-target="pdf" aria-selected="<?php echo 'pdf' === $initial_view ? 'true' : 'false'; ?>">
-                        <span class="eop-admin-spa-nav__icon dashicons dashicons-media-document" aria-hidden="true"></span>
-                        <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'PDF', EOP_TEXT_DOMAIN ); ?></span>
-                    </button>
                 <?php endif; ?>
                 <?php if ( $can_manage_settings ) : ?>
-                    <button type="button" class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo 'settings' === $initial_view ? ' is-active' : ''; ?>" data-eop-view-target="settings" aria-selected="<?php echo 'settings' === $initial_view ? 'true' : 'false'; ?>">
+                    <a class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo $is_settings_page ? ' is-active' : ''; ?>" href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'settings' ) ); ?>" data-eop-view-target="settings" aria-selected="<?php echo $is_settings_page ? 'true' : 'false'; ?>">
                         <span class="eop-admin-spa-nav__icon dashicons dashicons-admin-generic" aria-hidden="true"></span>
-                        <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Configuracoes', EOP_TEXT_DOMAIN ); ?></span>
-                    </button>
-                    <button type="button" class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo 'license' === $initial_view ? ' is-active' : ''; ?>" data-eop-view-target="license" aria-selected="<?php echo 'license' === $initial_view ? 'true' : 'false'; ?>">
+                        <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Configuracoes gerais', EOP_TEXT_DOMAIN ); ?></span>
+                    </a>
+                    <a class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo $is_appearance_page ? ' is-active' : ''; ?>" href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'appearance' ) ); ?>" data-eop-view-target="appearance" aria-selected="<?php echo $is_appearance_page ? 'true' : 'false'; ?>">
+                        <span class="eop-admin-spa-nav__icon dashicons dashicons-art" aria-hidden="true"></span>
+                        <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Aparencia e painel', EOP_TEXT_DOMAIN ); ?></span>
+                    </a>
+                    <a class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo $is_post_flow_page ? ' is-active' : ''; ?>" href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'post-confirmation' ) ); ?>" data-eop-view-target="post-confirmation" aria-selected="<?php echo $is_post_flow_page ? 'true' : 'false'; ?>">
+                        <span class="eop-admin-spa-nav__icon dashicons dashicons-controls-repeat" aria-hidden="true"></span>
+                        <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Fluxo complementar', EOP_TEXT_DOMAIN ); ?></span>
+                    </a>
+                    <a class="eop-pdv-nav__item eop-admin-spa-nav__item<?php echo $is_license_page ? ' is-active' : ''; ?>" href="<?php echo esc_url( EOP_Admin_Page::get_view_url( 'license' ) ); ?>" data-eop-view-target="license" aria-selected="<?php echo $is_license_page ? 'true' : 'false'; ?>">
                         <span class="eop-admin-spa-nav__icon dashicons dashicons-admin-network" aria-hidden="true"></span>
                         <span class="eop-admin-spa-nav__name"><?php esc_html_e( 'Licenca', EOP_TEXT_DOMAIN ); ?></span>
-                    </button>
+                    </a>
                 <?php endif; ?>
             </nav>
         </aside>
@@ -98,7 +108,7 @@ $pdf_preview_args    = array(
         <div class="eop-admin-spa__content">
             <div id="eop-notices"></div>
 
-            <section class="eop-pdv-view<?php echo 'new-order' === $initial_view ? ' is-active' : ''; ?>" data-eop-view="new-order"<?php echo 'new-order' === $initial_view ? '' : ' hidden'; ?>>
+            <section class="eop-pdv-view<?php echo $is_operation_page ? ' is-active' : ''; ?>" data-eop-view="new-order"<?php echo $is_operation_page ? '' : ' hidden'; ?>>
                 <input type="hidden" id="eop-edit-order-id" value="0" />
 
                 <div class="eop-admin-view-header">
@@ -307,12 +317,54 @@ $pdf_preview_args    = array(
                                 <?php esc_html_e( 'Finalizar e Gerar PDF', EOP_TEXT_DOMAIN ); ?>
                             </button>
                         </div>
+
+                        <div class="eop-card eop-post-flow-card" id="eop-post-flow-card">
+                            <div class="eop-post-flow-card__head">
+                                <div>
+                                    <h2><?php esc_html_e( 'Fluxo complementar do cliente', EOP_TEXT_DOMAIN ); ?></h2>
+                                    <p id="eop-post-flow-subtitle"><?php esc_html_e( 'O resumo complementar aparece quando um pedido existente entra em modo de edicao.', EOP_TEXT_DOMAIN ); ?></p>
+                                </div>
+                                <span class="eop-post-flow-badge is-inactive" id="eop-post-flow-badge"><?php esc_html_e( 'Inativo', EOP_TEXT_DOMAIN ); ?></span>
+                            </div>
+
+                            <div class="eop-post-flow-card__stats" id="eop-post-flow-stats"></div>
+
+                            <div class="eop-post-flow-card__section">
+                                <h3><?php esc_html_e( 'Contrato', EOP_TEXT_DOMAIN ); ?></h3>
+                                <p id="eop-post-flow-contract"><?php esc_html_e( 'Nenhum aceite registrado.', EOP_TEXT_DOMAIN ); ?></p>
+                            </div>
+
+                            <div class="eop-post-flow-card__section">
+                                <h3><?php esc_html_e( 'Documentos para assinatura', EOP_TEXT_DOMAIN ); ?></h3>
+                                <div class="eop-post-flow-list" id="eop-post-flow-signature-documents"></div>
+                            </div>
+
+                            <div class="eop-post-flow-card__section">
+                                <h3><?php esc_html_e( 'Dados do pedido', EOP_TEXT_DOMAIN ); ?></h3>
+                                <div class="eop-post-flow-list" id="eop-post-flow-order-data"></div>
+                            </div>
+
+                            <div class="eop-post-flow-card__section">
+                                <h3><?php esc_html_e( 'Anexo', EOP_TEXT_DOMAIN ); ?></h3>
+                                <div class="eop-post-flow-list" id="eop-post-flow-attachment"></div>
+                            </div>
+
+                            <div class="eop-post-flow-card__section">
+                                <h3><?php esc_html_e( 'Produtos', EOP_TEXT_DOMAIN ); ?></h3>
+                                <div class="eop-post-flow-list" id="eop-post-flow-products"></div>
+                            </div>
+
+                            <div class="eop-post-flow-card__actions">
+                                <a class="eop-btn" id="eop-post-flow-public-link" href="#" target="_blank" rel="noopener" hidden><?php esc_html_e( 'Abrir link publico', EOP_TEXT_DOMAIN ); ?></a>
+                                <a class="eop-btn eop-btn-secondary" id="eop-post-flow-pdf-link" href="#" target="_blank" rel="noopener" hidden><?php esc_html_e( 'Baixar PDF complementar', EOP_TEXT_DOMAIN ); ?></a>
+                            </div>
+                        </div>
                     </div>
                 </div>
                 </div>
             </section>
 
-            <section class="eop-pdv-view<?php echo 'orders' === $initial_view ? ' is-active' : ''; ?>" data-eop-view="orders"<?php echo 'orders' === $initial_view ? '' : ' hidden'; ?>>
+            <section class="eop-pdv-view<?php echo $is_orders_page ? ' is-active' : ''; ?>" data-eop-view="orders"<?php echo $is_orders_page ? '' : ' hidden'; ?>>
                 <div class="eop-admin-view-header">
                     <div class="eop-admin-view-copy">
                         <span class="eop-admin-view-kicker"><?php esc_html_e( 'Gestao comercial', EOP_TEXT_DOMAIN ); ?></span>
@@ -351,6 +403,15 @@ $pdf_preview_args    = array(
                                     <?php endforeach; ?>
                                 </select>
                             </div>
+                            <div class="eop-field">
+                                <label for="eop-orders-flow-filter"><?php esc_html_e( 'Fluxo complementar', EOP_TEXT_DOMAIN ); ?></label>
+                                <select id="eop-orders-flow-filter">
+                                    <option value="any"><?php esc_html_e( 'Todos', EOP_TEXT_DOMAIN ); ?></option>
+                                    <option value="active"><?php esc_html_e( 'Com fluxo ativo', EOP_TEXT_DOMAIN ); ?></option>
+                                    <option value="pending"><?php esc_html_e( 'Pendentes', EOP_TEXT_DOMAIN ); ?></option>
+                                    <option value="completed"><?php esc_html_e( 'Concluidos', EOP_TEXT_DOMAIN ); ?></option>
+                                </select>
+                            </div>
                         </div>
                     </div>
 
@@ -361,10 +422,10 @@ $pdf_preview_args    = array(
                 </div>
             </section>
 
-            <section class="eop-pdv-view<?php echo 'pdf' === $initial_view ? ' is-active' : ''; ?>" data-eop-view="pdf"<?php echo 'pdf' === $initial_view ? '' : ' hidden'; ?>>
+            <section class="eop-pdv-view<?php echo $is_pdf_page ? ' is-active' : ''; ?>" data-eop-view="pdf"<?php echo $is_pdf_page ? '' : ' hidden'; ?>>
                 <div class="eop-admin-panel-head">
                     <h2><?php esc_html_e( 'PDF', EOP_TEXT_DOMAIN ); ?></h2>
-                    <p><?php esc_html_e( 'Configure documentos, preview e comportamento do modulo PDF sem sair do shell original do Pedido Expresso.', EOP_TEXT_DOMAIN ); ?></p>
+                    <p><?php esc_html_e( 'Continue no SPA, mas com o modulo PDF isolado visualmente e sem disputar espaco com configuracoes operacionais.', EOP_TEXT_DOMAIN ); ?></p>
                 </div>
                 <div class="eop-admin-view-main">
                 <?php
@@ -376,17 +437,37 @@ $pdf_preview_args    = array(
             </section>
 
             <?php if ( $can_manage_settings ) : ?>
-                <section class="eop-pdv-view<?php echo 'settings' === $initial_view ? ' is-active' : ''; ?>" data-eop-view="settings"<?php echo 'settings' === $initial_view ? '' : ' hidden'; ?>>
+                <section class="eop-pdv-view<?php echo $is_settings_page ? ' is-active' : ''; ?>" data-eop-view="settings"<?php echo $is_settings_page ? '' : ' hidden'; ?>>
+                    <div class="eop-admin-panel-head">
+                        <h2><?php esc_html_e( 'Configuracoes gerais', EOP_TEXT_DOMAIN ); ?></h2>
+                        <p><?php esc_html_e( 'Concentre o que muda o fluxo operacional: modo comercial, desconto, checkout, paginas publicas e dados institucionais do PDF nativo.', EOP_TEXT_DOMAIN ); ?></p>
+                    </div>
+                    <div class="eop-admin-view-main">
+                    <?php EOP_Settings::render_embedded_page( 'general' ); ?>
+                    </div>
+                </section>
+
+                <section class="eop-pdv-view<?php echo $is_appearance_page ? ' is-active' : ''; ?>" data-eop-view="appearance"<?php echo $is_appearance_page ? '' : ' hidden'; ?>>
+                    <div class="eop-admin-panel-head">
+                        <h2><?php esc_html_e( 'Aparencia e painel', EOP_TEXT_DOMAIN ); ?></h2>
+                        <p><?php esc_html_e( 'Separe identidade visual, tipografia, textos do shell interno e visual da proposta publica em uma area dedicada.', EOP_TEXT_DOMAIN ); ?></p>
+                    </div>
+                    <div class="eop-admin-view-main">
+                    <?php EOP_Settings::render_embedded_page( 'appearance' ); ?>
+                    </div>
+                </section>
+
+                <section class="eop-pdv-view<?php echo $is_post_flow_page ? ' is-active' : ''; ?>" data-eop-view="post-confirmation"<?php echo $is_post_flow_page ? '' : ' hidden'; ?>>
                     <div class="eop-admin-panel-head">
-                        <h2><?php esc_html_e( 'Configuracoes do fluxo', EOP_TEXT_DOMAIN ); ?></h2>
-                        <p><?php esc_html_e( 'Ajuste comportamento comercial, identidade visual e textos sem sair do admin do plugin.', EOP_TEXT_DOMAIN ); ?></p>
+                        <h2><?php esc_html_e( 'Fluxo complementar', EOP_TEXT_DOMAIN ); ?></h2>
+                        <p><?php esc_html_e( 'Gerencie contrato, documentos, anexo e personalizacao pos-proposta em uma pagina propria, sem misturar com identidade visual ou licenca.', EOP_TEXT_DOMAIN ); ?></p>
                     </div>
                     <div class="eop-admin-view-main">
-                    <?php EOP_Settings::render_embedded_page(); ?>
+                    <?php EOP_Settings::render_embedded_page( 'post-confirmation' ); ?>
                     </div>
                 </section>
 
-                <section class="eop-pdv-view<?php echo 'license' === $initial_view ? ' is-active' : ''; ?>" data-eop-view="license"<?php echo 'license' === $initial_view ? '' : ' hidden'; ?>>
+                <section class="eop-pdv-view<?php echo $is_license_page ? ' is-active' : ''; ?>" data-eop-view="license"<?php echo $is_license_page ? '' : ' hidden'; ?>>
                     <div class="eop-admin-panel-head">
                         <h2><?php esc_html_e( 'Licenca', EOP_TEXT_DOMAIN ); ?></h2>
                         <p><?php esc_html_e( 'Consulte a validade da assinatura e administre a ativacao do plugin sem sair do painel.', EOP_TEXT_DOMAIN ); ?></p>
diff --git a/templates/orders-edit.php b/templates/orders-edit.php
index bd176fe..eb43e51 100644
--- a/templates/orders-edit.php
+++ b/templates/orders-edit.php
@@ -186,6 +186,48 @@ $order_id = absint( $_GET['order_id'] ?? 0 );
             </div>
         </div>
 
+        <div class="eop-card eop-post-flow-card" id="eop-post-flow-card">
+            <div class="eop-post-flow-card__head">
+                <div>
+                    <h2><?php esc_html_e( 'Fluxo complementar do cliente', EOP_TEXT_DOMAIN ); ?></h2>
+                    <p id="eop-post-flow-subtitle"><?php esc_html_e( 'Carregando dados estruturados da proposta...', EOP_TEXT_DOMAIN ); ?></p>
+                </div>
+                <span class="eop-post-flow-badge is-inactive" id="eop-post-flow-badge"><?php esc_html_e( 'Inativo', EOP_TEXT_DOMAIN ); ?></span>
+            </div>
+
+            <div class="eop-post-flow-card__stats" id="eop-post-flow-stats"></div>
+
+            <div class="eop-post-flow-card__section">
+                <h3><?php esc_html_e( 'Contrato', EOP_TEXT_DOMAIN ); ?></h3>
+                <p id="eop-post-flow-contract"><?php esc_html_e( 'Nenhum aceite registrado.', EOP_TEXT_DOMAIN ); ?></p>
+            </div>
+
+            <div class="eop-post-flow-card__section">
+                <h3><?php esc_html_e( 'Documentos para assinatura', EOP_TEXT_DOMAIN ); ?></h3>
+                <div class="eop-post-flow-list" id="eop-post-flow-signature-documents"></div>
+            </div>
+
+            <div class="eop-post-flow-card__section">
+                <h3><?php esc_html_e( 'Dados do pedido', EOP_TEXT_DOMAIN ); ?></h3>
+                <div class="eop-post-flow-list" id="eop-post-flow-order-data"></div>
+            </div>
+
+            <div class="eop-post-flow-card__section">
+                <h3><?php esc_html_e( 'Anexo', EOP_TEXT_DOMAIN ); ?></h3>
+                <div class="eop-post-flow-list" id="eop-post-flow-attachment"></div>
+            </div>
+
+            <div class="eop-post-flow-card__section">
+                <h3><?php esc_html_e( 'Produtos', EOP_TEXT_DOMAIN ); ?></h3>
+                <div class="eop-post-flow-list" id="eop-post-flow-products"></div>
+            </div>
+
+            <div class="eop-post-flow-card__actions">
+                <a class="button" id="eop-post-flow-public-link" href="#" target="_blank" rel="noopener" hidden><?php esc_html_e( 'Abrir link publico', EOP_TEXT_DOMAIN ); ?></a>
+                <a class="button button-secondary" id="eop-post-flow-pdf-link" href="#" target="_blank" rel="noopener" hidden><?php esc_html_e( 'Baixar PDF complementar', EOP_TEXT_DOMAIN ); ?></a>
+            </div>
+        </div>
+
         <!-- Seção 4: Ações -->
         <div class="eop-card eop-actions">
             <div class="eop-field">
diff --git a/templates/pdf-admin-page.php b/templates/pdf-admin-page.php
index fd62308..4b1abe5 100644
--- a/templates/pdf-admin-page.php
+++ b/templates/pdf-admin-page.php
@@ -4,6 +4,7 @@ defined( 'ABSPATH' ) || exit;
 $plugin_settings = EOP_Settings::get_all();
 $pdf_settings    = EOP_PDF_Settings::get_all();
 $embedded        = ! empty( $embedded );
+$default_tab     = isset( $default_tab ) ? (string) $default_tab : 'general';
 $font_css        = method_exists( 'EOP_Settings', 'get_font_css_family' ) ? EOP_Settings::get_font_css_family( $plugin_settings['font_family'] ) : "'Segoe UI', sans-serif";
 $tabs            = EOP_PDF_Admin_Page::get_accessible_tabs();
 $tab             = EOP_PDF_Admin_Page::normalize_tab( EOP_PDF_Admin_Page::get_current_tab(), $default_tab );
@@ -15,13 +16,22 @@ if ( ! isset( $tabs[ $tab ] ) ) {
 $document        = isset( $_GET['document'] ) && 'proposal' === sanitize_key( wp_unslash( $_GET['document'] ) ) ? 'proposal' : 'order';
 $recent_orders   = EOP_Document_Manager::get_recent_orders( 20 );
 $preview_order   = EOP_Document_Manager::get_preview_order( absint( $_GET['preview_order'] ?? 0 ) );
-$preview_document = $preview_order instanceof WC_Order ? EOP_Document_Manager::get_document_type_for_order( $preview_order ) : $document;
+$detected_preview_document = $preview_order instanceof WC_Order ? EOP_Document_Manager::get_document_type_for_order( $preview_order ) : $document;
+$preview_document = $document;
 $editing_label   = 'proposal' === $document ? __( 'Proposta', EOP_TEXT_DOMAIN ) : __( 'Pedido', EOP_TEXT_DOMAIN );
 $preview_label   = 'proposal' === $preview_document ? __( 'Proposta', EOP_TEXT_DOMAIN ) : __( 'Pedido', EOP_TEXT_DOMAIN );
-$preview_notice  = $preview_order instanceof WC_Order && $preview_document !== $document;
+$detected_preview_label = 'proposal' === $detected_preview_document ? __( 'Proposta', EOP_TEXT_DOMAIN ) : __( 'Pedido', EOP_TEXT_DOMAIN );
+$preview_notice  = $preview_order instanceof WC_Order && $detected_preview_document !== $document;
 $preview_allowed = $preview_order instanceof WC_Order && 'no' === $pdf_settings['advanced_html_output'];
 $preview_mode    = empty( $_GET['preview_order'] ) ? __( 'Atualmente mostrando o ultimo pedido', EOP_TEXT_DOMAIN ) : __( 'Atualmente mostrando o pedido selecionado', EOP_TEXT_DOMAIN );
-$preview_pdf_url = $preview_order instanceof WC_Order ? EOP_Document_Manager::get_pdf_document_url( $preview_order, $preview_document ) : '';
+$preview_pdf_url = $preview_order instanceof WC_Order ? EOP_Document_Manager::get_pdf_preview_document_url( $preview_order, $preview_document ) : '';
+$document_style_settings = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_style_settings( $document ) : array();
+$document_style_groups   = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_style_group_definitions() : array();
+$document_block_defs     = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_block_definitions() : array();
+$document_spacing_defs   = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_document_spacing_definitions() : array();
+$document_typography     = is_array( $document_style_settings['typography'] ?? null ) ? $document_style_settings['typography'] : array();
+$document_blocks         = is_array( $document_style_settings['blocks'] ?? null ) ? $document_style_settings['blocks'] : array();
+$document_spacing        = is_array( $document_style_settings['spacing'] ?? null ) ? $document_style_settings['spacing'] : array();
 $preview_xml     = $preview_order instanceof WC_Order && class_exists( 'EOP_Document_Manager' ) ? EOP_Document_Manager::get_edocument_xml_preview( $preview_order, $preview_document ) : '';
 $preview_xml_url = $preview_order instanceof WC_Order ? wp_nonce_url( add_query_arg( array( 'action' => 'eop_download_edoc_xml', 'order_id' => $preview_order->get_id(), 'document' => $preview_document ), admin_url( 'admin-post.php' ) ), 'eop_download_edoc_xml' ) : '';
 $danger_zone_enabled = 'yes' === ( $pdf_settings['advanced_danger_zone'] ?? 'no' );
@@ -31,7 +41,16 @@ $pdf_action_notice = isset( $_GET['eop_pdf_action'] ) ? sanitize_key( wp_unslash
 $order_page_url  = EOP_Page_Installer::get_page_url( 'order' );
 $proposal_url    = EOP_Page_Installer::get_page_url( 'proposal' );
 $current_tab_label = isset( $tabs[ $tab ] ) ? $tabs[ $tab ] : __( 'PDF', EOP_TEXT_DOMAIN );
+$tab_descriptions = array(
+    'general'       => __( 'Ajuste exibicao, template e dados compartilhados da loja sem sair da mesma tela.', EOP_TEXT_DOMAIN ),
+    'documents'     => __( 'Controle numeracao, acesso e colunas do documento que sera gerado para pedido ou proposta.', EOP_TEXT_DOMAIN ),
+    'edocuments'    => __( 'Configure XML, UBL, logs e identificadores do documento eletronico com foco operacional.', EOP_TEXT_DOMAIN ),
+    'advanced'      => __( 'Revise cache, renderer, pretty links e comportamentos tecnicos do modulo PDF.', EOP_TEXT_DOMAIN ),
+    'documentation' => __( 'Consulte os campos disponiveis e o efeito de cada configuracao antes de ajustar o layout.', EOP_TEXT_DOMAIN ),
+);
+$current_tab_description = isset( $tab_descriptions[ $tab ] ) ? $tab_descriptions[ $tab ] : __( 'Configure o modulo PDF sem desperdiçar espaco visual.', EOP_TEXT_DOMAIN );
 $woo_general_url = admin_url( 'admin.php?page=wc-settings&tab=general' );
+$shared_business_url = admin_url( 'admin.php?page=aireset-default&view=business' );
 $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings::get_documentation_sections() : array();
 ?>
 <style>
@@ -76,6 +95,17 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
     <?php endif; ?>
 
     <div class="eop-pdf-admin__shell" data-eop-pdf-shell data-eop-pdf-tab-current="<?php echo esc_attr( $tab ); ?>">
+        <div class="eop-pdf-admin__shell-head">
+            <div class="eop-pdf-admin__shell-copy">
+                <strong><?php echo esc_html( $current_tab_label ); ?></strong>
+                <span><?php echo esc_html( $current_tab_description ); ?></span>
+            </div>
+            <button type="button" class="eop-pdf-admin__preview-toggle" data-eop-pdf-preview-toggle aria-expanded="false" aria-controls="eop-pdf-preview-drawer">
+                <span class="dashicons dashicons-visibility" aria-hidden="true"></span>
+                <span><?php esc_html_e( 'Abrir preview', EOP_TEXT_DOMAIN ); ?></span>
+            </button>
+        </div>
+
         <div class="eop-pdf-admin__sidebar">
             <?php if ( 'general' === $tab ) : ?>
                 <form method="post" action="options.php" class="eop-pdf-admin__form">
@@ -137,7 +167,7 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                     <details class="eop-pdf-admin__section" open>
                         <summary><?php esc_html_e( 'Informacoes sobre a loja', EOP_TEXT_DOMAIN ); ?></summary>
                         <div class="eop-pdf-admin__section-note">
-                            <p><?php printf( wp_kses_post( __( 'Endereco, cidade, estado, CEP e pais sao sincronizados com o WooCommerce. Para alterar esses dados, edite em <a href="%s">WooCommerce > Configuracoes > Geral</a>.', EOP_TEXT_DOMAIN ) ), esc_url( $woo_general_url ) ); ?></p>
+                            <p><?php printf( wp_kses_post( __( 'Nome da loja, telefone, e-mail, CNPJ e endereco compartilham a mesma base do Aireset Default. Ao salvar aqui, os dados sao sincronizados com <a href="%1$s">Aireset Default > Dados da Empresa</a> e com <a href="%2$s">WooCommerce > Configuracoes > Geral</a>.', EOP_TEXT_DOMAIN ) ), esc_url( $shared_business_url ), esc_url( $woo_general_url ) ); ?></p>
                         </div>
                         <div class="eop-pdf-admin__grid">
                             <div class="eop-settings-field is-full">
@@ -169,39 +199,39 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                                 <input id="eop_shop_name" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_name]" value="<?php echo esc_attr( $pdf_settings['shop_name'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_address_line_1"><?php esc_html_e( 'Endereco da loja, linha 1', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_shop_address_line_1" class="eop-settings-field__readonly" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_address_line_1]" value="<?php echo esc_attr( $pdf_settings['shop_address_line_1'] ); ?>" readonly aria-readonly="true" />
+                                <label for="eop_shop_address_line_1"><?php esc_html_e( 'Endereco linha 1', EOP_TEXT_DOMAIN ); ?></label>
+                                <input id="eop_shop_address_line_1" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_address_line_1]" value="<?php echo esc_attr( $pdf_settings['shop_address_line_1'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_address_line_2"><?php esc_html_e( 'Endereco da loja, linha 2', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_shop_address_line_2" class="eop-settings-field__readonly" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_address_line_2]" value="<?php echo esc_attr( $pdf_settings['shop_address_line_2'] ); ?>" readonly aria-readonly="true" />
+                                <label for="eop_shop_address_line_2"><?php esc_html_e( 'Endereco linha 2', EOP_TEXT_DOMAIN ); ?></label>
+                                <input id="eop_shop_address_line_2" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_address_line_2]" value="<?php echo esc_attr( $pdf_settings['shop_address_line_2'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
                                 <label for="eop_shop_city"><?php esc_html_e( 'Cidade', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_shop_city" class="eop-settings-field__readonly" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_city]" value="<?php echo esc_attr( $pdf_settings['shop_city'] ); ?>" readonly aria-readonly="true" />
+                                <input id="eop_shop_city" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_city]" value="<?php echo esc_attr( $pdf_settings['shop_city'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_state"><?php esc_html_e( 'Estado', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_shop_state" class="eop-settings-field__readonly" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_state]" value="<?php echo esc_attr( $pdf_settings['shop_state'] ); ?>" readonly aria-readonly="true" />
+                                <label for="eop_shop_state"><?php esc_html_e( 'Estado (UF)', EOP_TEXT_DOMAIN ); ?></label>
+                                <input id="eop_shop_state" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_state]" value="<?php echo esc_attr( $pdf_settings['shop_state'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_postcode"><?php esc_html_e( 'CEP', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_shop_postcode" class="eop-settings-field__readonly" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_postcode]" value="<?php echo esc_attr( $pdf_settings['shop_postcode'] ); ?>" readonly aria-readonly="true" />
+                                <label for="eop_shop_postcode"><?php esc_html_e( 'Endereco (CEP)', EOP_TEXT_DOMAIN ); ?></label>
+                                <input id="eop_shop_postcode" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_postcode]" value="<?php echo esc_attr( $pdf_settings['shop_postcode'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_country"><?php esc_html_e( 'Pais', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_shop_country" class="eop-settings-field__readonly" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_country]" value="<?php echo esc_attr( $pdf_settings['shop_country'] ); ?>" readonly aria-readonly="true" />
+                                <label for="eop_shop_country"><?php esc_html_e( 'Pais (ISO2)', EOP_TEXT_DOMAIN ); ?></label>
+                                <input id="eop_shop_country" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_country]" value="<?php echo esc_attr( $pdf_settings['shop_country'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_phone"><?php esc_html_e( 'Telefone da loja', EOP_TEXT_DOMAIN ); ?></label>
+                                <label for="eop_shop_phone"><?php esc_html_e( 'Telefone', EOP_TEXT_DOMAIN ); ?></label>
                                 <input id="eop_shop_phone" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_phone]" value="<?php echo esc_attr( $pdf_settings['shop_phone'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_email"><?php esc_html_e( 'E-mail da loja', EOP_TEXT_DOMAIN ); ?></label>
+                                <label for="eop_shop_email"><?php esc_html_e( 'Email de contato', EOP_TEXT_DOMAIN ); ?></label>
                                 <input id="eop_shop_email" type="email" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_email]" value="<?php echo esc_attr( $pdf_settings['shop_email'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
-                                <label for="eop_shop_vat_number"><?php esc_html_e( 'Documento / VAT', EOP_TEXT_DOMAIN ); ?></label>
+                                <label for="eop_shop_vat_number"><?php esc_html_e( 'CNPJ', EOP_TEXT_DOMAIN ); ?></label>
                                 <input id="eop_shop_vat_number" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[shop_vat_number]" value="<?php echo esc_attr( $pdf_settings['shop_vat_number'] ); ?>" />
                             </div>
                             <div class="eop-settings-field">
@@ -227,7 +257,7 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                         </div>
                     </details>
 
-                    <div class="eop-pdf-admin__actions"><?php submit_button( __( 'Salvar configuracoes gerais', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
+                    <div class="eop-pdf-admin__actions eop-admin-submitbar"><?php submit_button( __( 'Salvar configuracoes gerais', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
                 </form>
             <?php elseif ( 'documents' === $tab ) : ?>
                 <form method="post" action="options.php" class="eop-pdf-admin__form">
@@ -464,6 +494,9 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                     </details>
                     <details class="eop-pdf-admin__section" open>
                         <summary><?php esc_html_e( 'Visual do documento', EOP_TEXT_DOMAIN ); ?></summary>
+                        <div class="eop-pdf-admin__section-note">
+                            <p><?php esc_html_e( 'Configure tipografia, altura de linha, peso, estilo, cores, fundos e espacamentos por bloco. O preview lateral e a geracao de PDF passam a usar esse mesmo perfil.', EOP_TEXT_DOMAIN ); ?></p>
+                        </div>
                         <div class="eop-pdf-admin__grid">
                             <div class="eop-settings-field">
                                 <label for="eop_doc_header_background_color"><?php esc_html_e( 'Cor de fundo do cabecalho da tabela', EOP_TEXT_DOMAIN ); ?></label>
@@ -485,30 +518,66 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                                 <label for="eop_doc_border_color"><?php esc_html_e( 'Cor das bordas e divisorias', EOP_TEXT_DOMAIN ); ?></label>
                                 <input id="eop_doc_border_color" type="text" class="eop-color-field" data-default-color="#e3e8f1" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_border_color]" value="<?php echo esc_attr( $pdf_settings[ $document . '_border_color' ] ); ?>" />
                             </div>
-                            <div class="eop-settings-field">
-                                <label for="eop_doc_title_font_size"><?php esc_html_e( 'Tamanho do titulo do documento', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_doc_title_font_size" type="number" min="8" max="72" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_title_font_size]" value="<?php echo esc_attr( $pdf_settings[ $document . '_title_font_size' ] ); ?>" />
-                            </div>
-                            <div class="eop-settings-field">
-                                <label for="eop_doc_meta_font_size"><?php esc_html_e( 'Tamanho dos textos de apoio', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_doc_meta_font_size" type="number" min="8" max="72" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_meta_font_size]" value="<?php echo esc_attr( $pdf_settings[ $document . '_meta_font_size' ] ); ?>" />
-                            </div>
-                            <div class="eop-settings-field">
-                                <label for="eop_doc_table_header_font_size"><?php esc_html_e( 'Tamanho da fonte do cabecalho da tabela', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_doc_table_header_font_size" type="number" min="8" max="72" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_table_header_font_size]" value="<?php echo esc_attr( $pdf_settings[ $document . '_table_header_font_size' ] ); ?>" />
-                            </div>
-                            <div class="eop-settings-field">
-                                <label for="eop_doc_table_body_font_size"><?php esc_html_e( 'Tamanho da fonte do corpo da tabela', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_doc_table_body_font_size" type="number" min="8" max="72" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_table_body_font_size]" value="<?php echo esc_attr( $pdf_settings[ $document . '_table_body_font_size' ] ); ?>" />
-                            </div>
-                            <div class="eop-settings-field">
-                                <label for="eop_doc_totals_font_size"><?php esc_html_e( 'Tamanho da fonte dos totais', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_doc_totals_font_size" type="number" min="8" max="72" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_totals_font_size]" value="<?php echo esc_attr( $pdf_settings[ $document . '_totals_font_size' ] ); ?>" />
-                            </div>
-                            <div class="eop-settings-field">
-                                <label for="eop_doc_note_font_size"><?php esc_html_e( 'Tamanho da fonte das observacoes e rodape', EOP_TEXT_DOMAIN ); ?></label>
-                                <input id="eop_doc_note_font_size" type="number" min="8" max="72" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_note_font_size]" value="<?php echo esc_attr( $pdf_settings[ $document . '_note_font_size' ] ); ?>" />
-                            </div>
+                            <?php foreach ( $document_block_defs as $block_key => $block_def ) : ?>
+                                <div class="eop-settings-field">
+                                    <label for="eop_doc_<?php echo esc_attr( $block_key ); ?>"><?php echo esc_html( $block_def['label'] ); ?></label>
+                                    <input id="eop_doc_<?php echo esc_attr( $block_key ); ?>" type="text" class="eop-color-field" data-default-color="<?php echo esc_attr( $block_def['default'] ); ?>" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $block_key ); ?>]" value="<?php echo esc_attr( $document_blocks[ $block_key ] ?? $block_def['default'] ); ?>" />
+                                </div>
+                            <?php endforeach; ?>
+                        </div>
+
+                        <div class="eop-pdf-admin__grid eop-pdf-admin__grid--spacing">
+                            <?php foreach ( $document_spacing_defs as $spacing_key => $spacing_def ) : ?>
+                                <div class="eop-settings-field">
+                                    <label for="eop_doc_<?php echo esc_attr( $spacing_key ); ?>"><?php echo esc_html( $spacing_def['label'] ); ?></label>
+                                    <input id="eop_doc_<?php echo esc_attr( $spacing_key ); ?>" type="number" min="0" max="80" step="1" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $spacing_key ); ?>]" value="<?php echo esc_attr( $document_spacing[ $spacing_key ] ?? $spacing_def['default'] ); ?>" />
+                                </div>
+                            <?php endforeach; ?>
+                        </div>
+
+                        <div class="eop-pdf-admin__style-groups">
+                            <?php foreach ( $document_style_groups as $group_key => $group_def ) : ?>
+                                <?php $group_values = $document_typography[ $group_key ] ?? array(); ?>
+                                <div class="eop-pdf-admin__style-card">
+                                    <div class="eop-pdf-admin__style-card-header">
+                                        <strong><?php echo esc_html( $group_def['label'] ); ?></strong>
+                                        <span><?php esc_html_e( 'Tipografia por elemento', EOP_TEXT_DOMAIN ); ?></span>
+                                    </div>
+                                    <div class="eop-pdf-admin__style-card-grid">
+                                        <div class="eop-settings-field is-full">
+                                            <label for="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_family"><?php esc_html_e( 'Fonte', EOP_TEXT_DOMAIN ); ?></label>
+                                            <input id="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_family" class="select_font eop-font-field" type="text" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $group_key ); ?>_font_family]" value="<?php echo esc_attr( $group_values['font_family'] ?? '' ); ?>" />
+                                        </div>
+                                        <div class="eop-settings-field">
+                                            <label for="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_size"><?php esc_html_e( 'Font size', EOP_TEXT_DOMAIN ); ?></label>
+                                            <input id="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_size" type="number" min="8" max="72" step="1" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $group_key ); ?>_font_size]" value="<?php echo esc_attr( $group_values['font_size'] ?? $group_def['default_font_size'] ); ?>" />
+                                        </div>
+                                        <div class="eop-settings-field">
+                                            <label for="eop_doc_<?php echo esc_attr( $group_key ); ?>_line_height"><?php esc_html_e( 'Altura da linha', EOP_TEXT_DOMAIN ); ?></label>
+                                            <input id="eop_doc_<?php echo esc_attr( $group_key ); ?>_line_height" type="number" min="0.8" max="2.5" step="0.05" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $group_key ); ?>_line_height]" value="<?php echo esc_attr( $group_values['line_height'] ?? $group_def['default_line_height'] ); ?>" />
+                                        </div>
+                                        <div class="eop-settings-field">
+                                            <label for="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_style"><?php esc_html_e( 'Estilo da fonte', EOP_TEXT_DOMAIN ); ?></label>
+                                            <select id="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_style" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $group_key ); ?>_font_style]">
+                                                <option value="normal" <?php selected( $group_values['font_style'] ?? $group_def['default_font_style'], 'normal' ); ?>><?php esc_html_e( 'Normal', EOP_TEXT_DOMAIN ); ?></option>
+                                                <option value="italic" <?php selected( $group_values['font_style'] ?? $group_def['default_font_style'], 'italic' ); ?>><?php esc_html_e( 'Italico', EOP_TEXT_DOMAIN ); ?></option>
+                                            </select>
+                                        </div>
+                                        <div class="eop-settings-field">
+                                            <label for="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_weight"><?php esc_html_e( 'Peso da fonte', EOP_TEXT_DOMAIN ); ?></label>
+                                            <select id="eop_doc_<?php echo esc_attr( $group_key ); ?>_font_weight" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $group_key ); ?>_font_weight]">
+                                                <?php foreach ( array( '300', '400', '500', '600', '700', '800' ) as $font_weight ) : ?>
+                                                    <option value="<?php echo esc_attr( $font_weight ); ?>" <?php selected( $group_values['font_weight'] ?? $group_def['default_font_weight'], $font_weight ); ?>><?php echo esc_html( $font_weight ); ?></option>
+                                                <?php endforeach; ?>
+                                            </select>
+                                        </div>
+                                        <div class="eop-settings-field">
+                                            <label for="eop_doc_<?php echo esc_attr( $group_key ); ?>_text_color"><?php esc_html_e( 'Cor do texto', EOP_TEXT_DOMAIN ); ?></label>
+                                            <input id="eop_doc_<?php echo esc_attr( $group_key ); ?>_text_color" type="text" class="eop-color-field" data-default-color="<?php echo esc_attr( $group_values['text_color'] ?? '#172033' ); ?>" name="<?php echo esc_attr( EOP_PDF_Settings::OPTION_KEY ); ?>[<?php echo esc_attr( $document ); ?>_<?php echo esc_attr( $group_key ); ?>_text_color]" value="<?php echo esc_attr( $group_values['text_color'] ?? '#172033' ); ?>" />
+                                        </div>
+                                    </div>
+                                </div>
+                            <?php endforeach; ?>
                         </div>
                     </details>
                     <details class="eop-pdf-admin__section" open>
@@ -544,7 +613,7 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                             </div>
                         </div>
                     </details>
-                    <div class="eop-pdf-admin__actions"><?php submit_button( __( 'Salvar configuracoes do documento', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
+                    <div class="eop-pdf-admin__actions eop-admin-submitbar"><?php submit_button( __( 'Salvar configuracoes do documento', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
                 </form>
             <?php elseif ( 'edocuments' === $tab ) : ?>
                 <form method="post" action="options.php" class="eop-pdf-admin__form">
@@ -626,7 +695,7 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                         </div>
                     <?php endif; ?>
 
-                    <div class="eop-pdf-admin__actions"><?php submit_button( __( 'Salvar configuracoes eletrônicas', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
+                    <div class="eop-pdf-admin__actions eop-admin-submitbar"><?php submit_button( __( 'Salvar configuracoes eletrônicas', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
                 </form>
             <?php elseif ( 'advanced' === $tab ) : ?>
                 <form method="post" action="options.php" class="eop-pdf-admin__form">
@@ -731,7 +800,7 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                         <?php endif; ?>
                     </details>
 
-                    <div class="eop-pdf-admin__actions"><?php submit_button( __( 'Salvar configuracoes avancadas', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
+                    <div class="eop-pdf-admin__actions eop-admin-submitbar"><?php submit_button( __( 'Salvar configuracoes avancadas', EOP_TEXT_DOMAIN ), 'primary', 'submit', false ); ?></div>
                 </form>
             <?php else : ?>
                 <div class="eop-pdf-admin__documentation">
@@ -787,11 +856,6 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
             <?php endif; ?>
         </div>
 
-        <button type="button" class="eop-pdf-admin__preview-toggle" data-eop-pdf-preview-toggle aria-expanded="false" aria-controls="eop-pdf-preview-drawer">
-            <span class="dashicons dashicons-visibility" aria-hidden="true"></span>
-            <span><?php esc_html_e( 'Abrir preview', EOP_TEXT_DOMAIN ); ?></span>
-        </button>
-
         <div class="eop-pdf-admin__preview-backdrop" data-eop-pdf-preview-close></div>
 
         <aside id="eop-pdf-preview-drawer" class="eop-pdf-admin__preview-drawer" aria-hidden="true">
@@ -836,7 +900,7 @@ $documentation_sections = class_exists( 'EOP_PDF_Settings' ) ? EOP_PDF_Settings:
                 <div class="eop-pdf-admin__preview-body">
                     <?php if ( $preview_notice ) : ?>
                         <div class="eop-pdf-admin__notice eop-pdf-admin__notice--preview-mismatch">
-                            <p><?php printf( esc_html__( 'O pedido selecionado para preview gera %1$s, mas voce esta editando as configuracoes de %2$s.', EOP_TEXT_DOMAIN ), esc_html( $preview_label ), esc_html( $editing_label ) ); ?></p>
+                            <p><?php printf( esc_html__( 'O pedido selecionado gera %1$s, mas a previa desta tela esta forcada para %2$s para refletir a configuracao que voce esta editando.', EOP_TEXT_DOMAIN ), esc_html( $detected_preview_label ), esc_html( $editing_label ) ); ?></p>
                         </div>
                     <?php endif; ?>
                     <?php if ( $preview_allowed ) : ?>
